"use client";

// TODO(3-11): Full implementation — Radix Dialog full-screen lightbox with filename + label info.
// This is a placeholder stub that satisfies imports from OrderDrawerPhotos.

import type { Photo } from "@/lib/photos/types";

interface Props {
  photo: Photo | null;
  onClose: () => void;
}

export function PhotoLightbox({ photo: _photo, onClose: _onClose }: Props) {
  return null;
}
