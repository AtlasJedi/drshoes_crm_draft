---
task_id: 0b-7-fixup
plan: docs/superpowers/plans/2026-05-08-milestone-00b-auth-rbac.md
milestone: 0B
agent: opus (inline, main session)
start_utc: 2026-05-08T13:25:00Z
end_utc: 2026-05-08T13:27:00Z
status: DONE
commit_sha: (set after commit)
notes: |
  Follow-up to original 0b-7 commits 21bcbdf + fd10ee7.
  Addresses the one Important issue from the code-quality review:
  AnonymousAuthenticationToken bypassed the early-return guard in
  RbacService.check() because Spring Security 6 returns
  isAuthenticated() == true for anonymous principals.
  Inline fixup per dispatch protocol §5 (no subagent for trivial edits).
---

# Task 0b-7-fixup — Reject AnonymousAuthenticationToken explicitly in RbacService

## Files changed

- `backend/app/src/main/java/com/drshoes/app/auth/rbac/RbacService.java` (modified, +5 / -1):
  - Imported `org.springframework.security.authentication.AnonymousAuthenticationToken`.
  - `check()` early-return guard now: `if (auth == null || auth instanceof AnonymousAuthenticationToken || !auth.isAuthenticated()) return false;`
  - Comment explains the Spring Security 6 trap (anonymous tokens report `isAuthenticated() == true`).
- `backend/app/src/test/java/com/drshoes/app/auth/rbac/RbacServiceTest.java` (modified, +13 / -0):
  - New test `anonymous_token_is_rejected_even_though_isAuthenticated_is_true`.
  - Constructs a real `AnonymousAuthenticationToken` with `ROLE_ANONYMOUS`, asserts `isAuthenticated() == true` (sanity / trap-confirmation), then asserts three capability methods return false.

## Commands run

```
# Focused
cd backend && mvn -B -pl app -am test -Dtest=RbacServiceTest -Dsurefire.failIfNoSpecifiedTests=false
# exit 0 — Tests run: 4, Failures: 0, Errors: 0, Skipped: 0

# Full suite regression
mvn -B -pl app -am test -Dsurefire.failIfNoSpecifiedTests=false
# exit 0 — Tests run: 28, Failures: 0, Errors: 0, Skipped: 0
```

## Test summary

- 28/28 green (was 27, +1 new anonymous-token test).
- Defense-in-depth verified: even before this fix the role-name match would have rejected `ROLE_ANONYMOUS` (no `ROLE_OWNER` / `ROLE_EMPLOYEE` match). Fix makes the rejection explicit at the early-return guard, so the intent of the code matches the runtime behaviour.

## Decisions / deviations

- Chose **option 1** from the reviewer's recommendations (add `instanceof AnonymousAuthenticationToken` check) over option 2 (remove the misleading `!isAuthenticated()` branch). Option 1 keeps the early-return defense and makes anonymous rejection explicit, which is more robust than relying solely on the role-name match.
- Did not address the two Minor follow-ups (`prePostEnabled = true` redundancy; cleanup of stale anonymous-null-only test): the redundancy is harmless and the original `anonymous_can_do_nothing` (null-only) test is kept for null-safety coverage alongside the new explicit-anonymous-token test.

## Logging / metadata wired

- No new log lines. Existing DEBUG `op=rbacCheck capability=... actor=anonymous role=anonymous outcome=false` covers the rejected-anonymous case; behaviour unchanged from log perspective.

## Concerns

- **Hardening debt unchanged:** `server.servlet.session.cookie.secure=false` (carried from 0b-4) still pending before milestone-0b tag.

## Next action

- Move to task `0b-8` per plan (Audit log table + AuditLogAspect).
