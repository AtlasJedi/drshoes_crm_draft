# Dispatch Log: 10-8

**Task:** LocationFormModal component + tests + page.tsx wiring
**UTC:** 2026-05-16T12:32:00Z
**Milestone:** 10
**Plan:** docs/superpowers/plans/2026-05-16-m10-notes-and-locations.md

## Files Modified

- `apps/web/app/(admin)/admin/settings/miejsca/_components/LocationFormModal.tsx` — created (design export + 2 integration fixes)
- `apps/web/app/(admin)/admin/settings/miejsca/_components/__tests__/LocationFormModal.test.tsx` — created (3 cases)
- `apps/web/app/(admin)/admin/settings/miejsca/page.tsx` — added import + replaced commented placeholders with live mounts

## Integration Fixes Applied

1. `createLocation(trimmed)` — bare string, not `{ name: trimmed }` (matches lib/locations.ts signature)
2. `err?.code === "location_name_conflict"` — matches `StorageLocationExceptionHandler.java` from 10-3, not design file's `"duplicate_name"`

## Test Summary

- 3/3 LocationFormModal tests GREEN
- Full vitest: 537/0/0/0 (baseline 534 + 3)

## Commit SHA

bdfddff

## Status

APPROVED — combined single-stage (pure UI, anti-bloat directive)
