# 9-5 — `Pill.tsx` primitive

**Status:** completed
**HEAD before:** 990b453
**HEAD after:** 84d0a59 (files committed in 9-13 Button session; index.ts pre-wired in 3583aec Splatter session)
**Started:** 2026-05-15T214231Z
**Completed:** 2026-05-15T214600Z

## Files touched
- Create: `packages/ui/src/components/Pill.tsx` — OrderStatus pill with STATUS_META (Polish labels + CSS class), 38 LOC
- Create: `packages/ui/src/components/__tests__/Pill.test.tsx` — 3 tests covering all 7 statuses (labels + CSS class + base class)
- Modify: `packages/ui/src/index.ts` — `export * from "./components/Pill"` appended

## Commands run
- `pnpm --filter @drshoes/web test -- Pill.test` → RED (file not found)
- Created `Pill.test.tsx`
- `pnpm --filter @drshoes/web test -- Pill.test` → RED (import error — Pill.tsx not yet created)
- Created `Pill.tsx`
- Appended barrel export to `index.ts`
- `pnpm --filter @drshoes/web test -- Pill.test` → GREEN: 3/3 passed
- `pnpm --filter @drshoes/web test` → GREEN: 61 files, 379 passed
- `pnpm --filter @drshoes/web typecheck` → 0 errors

## Decisions
- CSS-class-driven colour approach (not inline style from `orderStatusColor`): keeps design editable in globals.css, decoupled from the TS token values. Each status gets a `pill-*` modifier class that maps to the correct colour per globals.css `.pill-*` rules.
- Note: Pill.tsx and Pill.test.tsx were bundled into commit `84d0a59` (feat(ui): add Button primitive [milestone:9][task:9-13]) because parallel-session work from task 9-13 had already staged those files. The `export *` barrel line was pre-wired in `3583aec` (task 9-7 Splatter). The implementation is correct and tests pass.
- No existing `<Pill>` call-sites found in `apps/web` — migration step not needed.
- No `log.debug` calls — per note D in plan header (no logging in primitives).

## Test summary
3/3 tests GREEN:
- "renders the Polish label for each status" — covers all 7 OrderStatus values
- "applies the correct CSS class for each status" — all 7 pill-* class checks
- "has .pill base class always" — base class invariant
