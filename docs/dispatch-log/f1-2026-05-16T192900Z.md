# Dispatch Log — f1 — 2026-05-16T19:29:00Z

**Task:** F1 — Frontend: dim helpers + drawer redesign
**Plan:** docs/superpowers/plans/2026-05-16-m11-fix-batch.md § Task F1
**Milestone:** m11

---

## Summary

Frontend drawer redesign per Feature 2 + 3 of the spec:
- New `lib/orders/dim.ts` helpers (`daysInShop`, `isUrgent`) — TypeScript mirror of backend `OrderUrgency`.
- `OrderDto` + `OrderListRow` now carry `urgent: boolean` (mirrors backend field shipped by B2).
- `OrderDrawerCoreFields.tsx` rewritten:
  - Wykonawca FieldRow deleted; `users` prop dropped from Props.
  - Planowany odbiór date input replaced with "Czas w warsztacie" read-only display (days from receivedAt; null states render "—"; pink + "pilne" suffix when urgent).
  - Wycena field is now a read-only `<p>` showing formatted `quotedPriceCents` (items are the only writer post-B1).
- `OrderDrawer.tsx` and `apps/web/app/(admin)/admin/orders/page.tsx` stop passing `users` down to the drawer core fields.

Sub-agent (`ae55c329135cea4aa`) did the bulk of the work; controller finished the test-pass loop and committed.

---

## Files Touched

### Production
- `apps/web/lib/orders/dim.ts` (new)
- `apps/web/lib/orders/types.ts` — `urgent: boolean` added to OrderDto + OrderListRow
- `apps/web/app/(admin)/admin/orders/_components/OrderDrawerCoreFields.tsx` — full rewrite per plan Step 6
- `apps/web/app/(admin)/admin/orders/_components/OrderDrawer.tsx` — dropped users prop on inner CoreFields
- `apps/web/app/(admin)/admin/orders/page.tsx` — stopped sending `users` to drawer

### Tests
- `apps/web/lib/orders/__tests__/dim.test.ts` (new, 8 tests)
- `apps/web/app/(admin)/admin/orders/_components/__tests__/OrderDrawerCoreFields.test.tsx` (new, 4 tests)
- `apps/web/app/(admin)/admin/orders/_components/__tests__/OrderDrawer.test.tsx` — updated to match new CoreFields contract

### Hygiene fixups (pre-existing breakage surfaced)
- `apps/web/app/(admin)/admin/orders/_components/kanban/__tests__/useKanbanDnd.test.tsx` — two `changeStatus` assertions updated for 4-arg signature (`sendTriggers` boolean added in earlier commit `d184209` but tests weren't bumped). Not F1's regression, just unblocking the suite.

---

## Test Summary

| Suite | Before | After |
|---|---|---|
| apps/web vitest | 3 failed / 590 total | 0 failed / 590 total |

New tests: 12 (8 dim + 4 OrderDrawerCoreFields).

---

## Decisions

- `listUsersServer` left intact in `page.tsx` — it's still consumed by other admin routes; only the drawer-bound prop chain was pruned.
- "Czas w warsztacie" pluralization: Polish heuristic — 1 → "dzień", 2-4 → "dni", else "dni" (matches Polish "ilość dni" forms; OrderUrgency on backend doesn't care about plurals — frontend handles display).
- text-magenta class is applied only when `urgent` is true AND days is non-null (both conditions guarded in the JSX).

---

## Commands Run

```
pnpm test app/.../dim.test.ts                                    # 8/8 GREEN
pnpm test app/.../OrderDrawerCoreFields.test.tsx                 # 4/4 GREEN
pnpm test                                                        # 590/590 GREEN
```

---

## Commit SHA

`<filled by commit step>`
