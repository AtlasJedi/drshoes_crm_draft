# Dispatch Log — v2-J

**Task:** v2-J — Kanban tile click→drawer + ClientOrdersRows vitest
**UTC:** 20260517T094424Z
**Milestone:** v2-fixes

## Files Modified

| File | Change |
|------|--------|
| `apps/web/app/(admin)/admin/orders/kanban/page.tsx` | Added `searchParams` prop, fetches `getOrderServer(orderId)` in parallel with board+triggers, mounts `<OrderDrawer>` when `drawerOrder` is non-null |
| `apps/web/app/(admin)/admin/clients/[id]/zlecenia/_components/__tests__/ClientOrdersRows.test.tsx` | NEW — 5 vitest cases: rows render as role=button, click pushes ?orderId, Enter/Space keys activate, existing searchParams preserved |

## Root Cause / Motivation

KanbanCard already had `openDrawer` + `router.push(?orderId=...)` wired (from prior session), and `PointerSensor` with `distance: 8` was already configured in KanbanBoard.tsx. The missing piece was the server page not reading `searchParams` + not mounting `<OrderDrawer>` — so clicking a card pushed the URL but nothing consumed it.

Calendar page was already fully wired (`orderId` → `getOrderServer` → `<OrderDrawer>`). No changes needed there.

## dnd-kit Activation Strategy

`PointerSensor` with `activationConstraint: { distance: 8 }` was already set in `KanbanBoard.tsx`. Any pointer movement under 8px CSS-pixels registers as a click; movements ≥ 8px activate the drag. No sensor config changes needed.

## Calendar Close Investigation

`usePathname()` in OrderDrawer close handler (added in v2-I) fully fixes the close-redirect issue. Playwright verification confirmed:
- Calendar close: returns to `/admin/orders/calendar?mode=month` (mode param preserved)
- Kanban close: returns to `/admin/orders/kanban`
- Client zlecenia close: returns to `/admin/clients/.../zlecenia`

No further fix was needed beyond the `usePathname()` change.

## Test Results

| Suite | Before | After |
|-------|--------|--------|
| Frontend vitest test files | 93 | 94 |
| Frontend vitest tests | 589 | 594 |

## Playwright Verification

| Scenario | Result |
|----------|--------|
| Kanban card click → URL `?orderId=` + drawer visible | PASS |
| Kanban drawer close → stays on `/admin/orders/kanban` | PASS |
| Calendar chip click → drawer visible | PASS |
| Calendar drawer close → stays on `/admin/orders/calendar?mode=month` | PASS |
| Client zlecenia row click → URL `?orderId=` + drawer visible | PASS |
| Client zlecenia drawer close → stays on `/admin/clients/.../zlecenia` | PASS |

## Commit SHA

_To be filled after commit._
