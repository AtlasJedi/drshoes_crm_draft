# Dispatch Log — Task 9-36: `<Hero>`

**Milestone:** 9  
**Task:** 9-36  
**UTC:** 2026-05-15T120000Z  
**Commit SHA:** b6e732b  
**Status:** completed

## Files created

- `apps/web/app/(public)/_components/Hero.tsx` — thin orchestrator (71 LOC): section shell, Tape row, CTA buttons, bottom-right Sticker+scroll cue
- `apps/web/app/(public)/_components/HeroBackground.tsx` — full-bleed PhImg placeholder + two Splatter overlays (30 LOC)
- `apps/web/app/(public)/_components/HeroHeadline.tsx` — display h1 + tag-font tagline (34 LOC)
- `apps/web/app/(public)/_components/__tests__/Hero.test.tsx` — 5 vitest assertions
- `apps/web/app/(public)/_components/__tests__/__snapshots__/Hero.test.tsx.snap` — snapshot baseline

## Files modified

- `packages/ui/src/components/Splatter.tsx` — added `data-testid="splatter"` to `<svg>` element to support querySelectorAll in tests

## Commands run

```
pnpm --filter @drshoes/web exec vitest run --reporter=verbose
# → 509 tests passed (0 failed)
wc -l Hero.tsx HeroBackground.tsx HeroHeadline.tsx
# → 71 / 30 / 34 (all ≤ 80)
```

## Test summary

5/5 GREEN:
- snapshot
- renders h1 heading
- two Splatter elements present (data-testid="splatter")
- "Zamów custom" → href="#zamow"
- "Oddaj buty do naprawy" → href="#zamow"

## Decisions

- Split at 113 LOC into three files: Hero (orchestrator) + HeroBackground (bg+splatters) + HeroHeadline (h1+tagline) per ≤80 LOC granulate directive.
- `data-testid="splatter"` added to Splatter SVG — non-breaking addition, no other tests affected.
- Button's native `href` prop used (renders as `<a>`) rather than `asChild` — Button already supports href→<a> rendering, no Radix asChild needed.
- `I.sprayCan` is a pre-rendered JSX element, not a component; used inline in Button children.
- Not wired into `page.tsx` — that is 9-40's responsibility per plan.
