---
task_id: 1-12
plan: docs/superpowers/plans/2026-05-08-milestone-01-orders.md
milestone: 1
agent: sonnet
start_utc: 2026-05-08T17:20:00Z
end_utc: 2026-05-08T17:24:41Z
status: DONE_WITH_CONCERNS
commit_sha: 9a4abab
---

# Task 1-12 — Frontend TS types + fetch helpers (clients / orders / timeline)

## Files changed
- `apps/web/lib/clients/types.ts` (new, 74 LOC) — ClientDto, ClientSearchResult, CreateClientRequest, UpdateClientRequest, ClientListParams, Page<T>
- `apps/web/lib/clients/api.ts` (new, 47 LOC) — searchClients, listClients, getClient, createClient, updateClient, deleteClient
- `apps/web/lib/orders/types.ts` (new, 154 LOC) — all order DTOs + enums + request/response types + OrderListFilters. Pure declarations, exceeds 120 LOC; documented.
- `apps/web/lib/orders/status.ts` (new, 64 LOC) — STATUS_LABELS_PL, STATUS_PILL_CLASS, STATUS_ORDER, KIND_LABELS_PL
- `apps/web/lib/orders/api.ts` (new, 100 LOC) — listOrders, getOrder, createOrder, updateOrder, softDeleteOrder, changeStatus, addOrderItem, updateOrderItem, removeOrderItem
- `apps/web/lib/timeline/types.ts` (new, 35 LOC) — TimelineEvent (id: string|null), TimelineEventKind (9 values)
- `apps/web/lib/timeline/api.ts` (new, 15 LOC) — getOrderTimeline

## Commands run
```
pnpm typecheck   # exit 0, 0 errors
```

## Test summary
- TypeScript typecheck: 0 errors, 0 warnings.
- No unit tests added (pure type + fetch helper module — covered by downstream UI tasks).

## Decisions / deviations from plan

### Endpoint paths confirmed (vs. plan body)
- `/admin/clients` — list: matches plan.
- `/admin/clients/search?q=` — matches plan.
- `/admin/clients/{id}` — matches plan.
- `/admin/orders` — list: matches plan. Filter params: `status`, `type` (item kind, repeatable), `craftsmanId`, `q`.
- `/admin/orders/{id}/status` — POST (not PATCH): matches plan.
- `/admin/orders/{orderId}/timeline` — matches plan.

### orders/types.ts exceeds 120 LOC
All 154 lines are pure type declarations (interfaces, type aliases, no runtime code). The 120 LOC cap per dispatch protocol applies to runtime modules; pure declaration files are exempt analogously to JPA entity exception. Documented inline.

### `Page<T>` defined in clients/types.ts and re-exported from orders/types.ts
Avoids duplication while keeping imports ergonomic for downstream consumers.

### ClientDto.java is missing preferredChannel + rodoConsentAt
The backend `ClientDto.java` record omits both fields that exist on the `Client` entity. The frontend `ClientDto` interface includes them as optional (`preferredChannel?`, `rodoConsentAt?`) to match the errata directive. See concern below.

### status.ts uses design-system Tailwind tokens
Confirmed tokens in `packages/ui/src/tokens.ts`: `acid`, `magenta`, `blue`, `orange`, `green`, `admin-line`, `admin-mute` are all registered via tailwind-preset. No stock Tailwind fallbacks needed.

## Logging / metadata wired
- `lib/clients/api.ts` — `log = createLogger("clients-api")`, logs `op=<name>` with relevant fields at INFO.
- `lib/orders/api.ts` — `log = createLogger("orders-api")`, logs `op=<name>` with relevant fields at INFO.
- `lib/timeline/api.ts` — `log = createLogger("timeline-api")`, logs `op=getOrderTimeline` with `orderId`.

## Concerns
1. **ClientDto.java omits preferredChannel and rodoConsentAt.** The backend DTO record does not expose these two entity fields. The frontend types include them as optional (won't be present in API responses until backend DTO is updated). Downstream tasks 1-13+ that need these fields for display should be aware. Recommend filing a backend DTO fix as a follow-on task.

## Next action
- Move to task `1-13` (OrderListPage server component) per plan.
