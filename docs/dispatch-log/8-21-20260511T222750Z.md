# Dispatch Log — Task 8-21

## Task
- **ID:** 8-21
- **Title:** Playwright setup in `apps/web` (config + smoke spec + .gitignore)
- **Plan:** `docs/superpowers/plans/2026-05-11-milestone-08-demo-ready.md`
- **Lines:** 3790–3919
- **Review type:** Combined single-stage

## Files changed

| File | Description |
|------|-------------|
| `apps/web/package.json` | Added `@playwright/test ^1.60.0` to devDependencies; added `"test:e2e": "playwright test"` script |
| `pnpm-lock.yaml` | Updated lockfile after pnpm install |
| `apps/web/playwright.config.ts` | New — Playwright config: testDir=./e2e, Chromium only, baseURL=http://localhost:3000, workers=1, headless, outputDir=test-results/ |
| `apps/web/e2e/.gitignore` | New — ignores `test-results/` and `playwright-report/` |
| `apps/web/e2e/_smoke.spec.ts` | New — smoke spec: `page.goto("/admin/login")` + `getByText("Dr Shoes — Logowanie").toBeVisible()` |
| `.gitignore` | Added `apps/web/test-results/` and `apps/web/playwright-report/` entries at repo root |

Key symbols:
- `apps/web/playwright.config.ts`: `export default defineConfig({ testDir: "./e2e", ... })`
- `apps/web/e2e/_smoke.spec.ts:13`: `test("login page loads with Dr Shoes title", ...)`

## Commands run

| Command | Result |
|---------|--------|
| `pnpm --filter=web add -D @playwright/test` | PASS — installed ^1.60.0 (chromium v1223) |
| `pnpm --filter=web exec playwright install chromium` | PASS — binaries to ~/.cache/ms-playwright (not in repo) |
| `pnpm exec playwright test --list` (from apps/web/) | PASS — "1 test in 1 file" |
| `pnpm --filter @drshoes/web typecheck` | PASS |
| `pnpm --filter @drshoes/web lint` | PASS (--max-warnings=0) |

## Test summary

- Frontend vitest baseline: **188/0/0/0** (unchanged — vitest not re-run, Playwright setup does not affect it)
- Backend: **390/0/0/0** (untouched)
- Playwright `--list`: **1 test** discovered — `[chromium] › _smoke.spec.ts:13:5 › login page loads with Dr Shoes title`
- Live smoke run: **not run — owner runs live** (requires `make demo` stack)

## Decisions

1. **Root `.gitignore` additions**: The plan specified `e2e/.gitignore` for `test-results/` and `playwright-report/`. However, `playwright.config.ts` sets `outputDir: "test-results/"` relative to `apps/web/` (not inside `e2e/`), so the root `.gitignore` also gets `apps/web/test-results/` and `apps/web/playwright-report/` to ensure no artifacts leak into git regardless of which directory playwright writes to. Minor out-of-plan expansion, zero risk.
2. **Playwright version**: `^1.60.0` resolved — plan did not pin a version, pnpm resolved latest stable.
3. **Browser binaries location**: Installed to `~/Library/Caches/ms-playwright/chromium-1223` (macOS system cache), not in the repo tree. No binary files staged.
4. **`retries: 0` on CI**: Per plan errata note — zero retries in CI so flaky specs are caught fast, not masked. Local gets 2 retries for timing jitter.
5. **`workers: 1`**: Per plan — serialized execution required for the demo-flow spec in 8-22 (login → create → advance state machine).

## Review

**Verdict: APPROVED**

Checklist:
- [x] `@playwright/test` installed as devDependency in `apps/web/package.json` only (not root)
- [x] `"test:e2e": "playwright test"` script added
- [x] `playwright.config.ts` matches plan spec exactly
- [x] `e2e/.gitignore` created with correct entries
- [x] `e2e/_smoke.spec.ts` matches plan spec exactly
- [x] Browser binaries NOT committed (in OS cache)
- [x] `pnpm exec playwright test --list` → 1 test, config parses
- [x] typecheck: PASS
- [x] lint (--max-warnings=0): PASS
- [x] Root `.gitignore` updated for Playwright artifacts

## Commit SHA
- Implementation: `1f62fea`

## Follow-ups
- 8-22 (TWO-STAGE) writes the full demo-flow E2E spec. `e2e/_smoke.spec.ts` is the anchor; the demo flow spec will be a separate file (e.g. `e2e/demo-flow.spec.ts`).
- Owner runs `make demo && pnpm --filter=web test:e2e` to verify the live smoke spec against a real stack.

## Subagent token budget
- Estimated total_tokens: ~18,000
