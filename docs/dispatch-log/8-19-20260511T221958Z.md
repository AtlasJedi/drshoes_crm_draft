# Dispatch Log — Task 8-19

## Task
- **ID:** 8-19
- **Title:** `ReportIssueButton` component + vitest (combined single-stage)
- **Plan file:** `docs/superpowers/plans/2026-05-11-milestone-08-demo-ready.md`
- **Plan lines:** 3306–3583
- **Review type:** Combined single-stage (UI component, ≤80 LOC)

## Files changed
- `apps/web/components/admin/ReportIssueButton.tsx` (new, ~115 LOC including blank lines/comments)
  - `ReportIssueButton({ user })` — Client Component using Radix Dialog
  - `readTraceId()` — reads active OTel span trace id or returns ZERO_TRACE
  - `handleOpen()` — fires fetch("/api/health") warm-up when trace id is zero, then opens dialog
  - `buildPayload()` — assembles `{ traceId, url, user, userAgent, capturedAt, jaegerUrl }`
  - `handleCopy()` — writes JSON payload to clipboard, toggles "Skopiowano ✓" for 2500ms
- `apps/web/components/admin/__tests__/ReportIssueButton.test.tsx` (new, ~90 LOC)
  - 5 vitest+RTL cases: render, clipboard copy + JSON shape, Jaeger link href, /api/health warm-up, email in modal

## Commands run
| Command | Result |
|---|---|
| `pnpm --filter=web test -- --run ReportIssueButton` (RED) | 1 file failed — module not found ✓ |
| `pnpm --filter=web test -- --run ReportIssueButton` (GREEN) | 5/5 passed ✓ |
| `pnpm --filter=web typecheck` | pass (after fixing `mock.calls` type cast via `as unknown as string[][]` + optional chaining) |
| `pnpm --filter=web lint` | pass (0 warnings) |
| `pnpm --filter=web test -- --run` (full suite) | 185/0/0/0 across 39 files ✓ |

## Test summary
| Stage | Backend | Frontend vitest |
|---|---|---|
| Before | 390/0/0/0 | 180/0/0/0 |
| After | 390/0/0/0 (untouched) | 185/0/0/0 |

## Decisions
1. **`mock.calls` type cast:** Vitest types `mock.calls` as `readonly unknown[][]`, making index access return `unknown`. Used `as unknown as string[][]` with optional chaining (`calls[0]?.[0] ?? ""`) to satisfy strict TypeScript without disabling lint rules.
2. **LOC:** Component is ~75 meaningful LOC (within 80 budget). With blank lines and JSDoc comment it is ~115 total lines in file — accepted per plan note ("~75 LOC" refers to meaningful lines).
3. **No out-of-plan changes:** All behaviour strictly per plan spec.

## Review

**Verdict: APPROVED**

Acceptance checklist:
- [x] "Zgłoś problem" button renders
- [x] Clicking opens Radix Dialog with traceId, URL, user email
- [x] "Kopiuj JSON" copies valid JSON payload with all required fields
- [x] JSON shape: `{ traceId, url, user, userAgent, capturedAt, jaegerUrl }`
- [x] "Otwórz w Jaeger" link has `href="http://localhost:16686/trace/<traceId>"`
- [x] Zero trace id triggers `fetch("/api/health")` warm-up
- [x] Component ≤ 80 meaningful LOC
- [x] All 5 vitest cases green
- [x] Typecheck clean
- [x] Lint clean (0 warnings)
- [x] Polish UI copy, English code/comments

## Commit SHA
- Implementation: `bc459bf`

## Follow-ups
- Task 8-20 will wire `ReportIssueButton` into the admin sidebar — passes `user` prop from session.

## Subagent token budget
- Estimated `total_tokens`: ~18,000
