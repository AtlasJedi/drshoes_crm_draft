# Dispatch Log — task 10-diff

**Task:** Polish field-diff notes for PATCH audit  
**Milestone:** 10  
**UTC:** 2026-05-16T11:00:43Z  
**Status:** completed

## Files modified

- `backend/app/src/main/java/com/drshoes/app/order/OrderService.java`
  — injected `UserRepository`, compute `OrderUpdateDiff.computePolish` before mutations, set `audit.diffNote` request attribute
- `backend/app/src/main/java/com/drshoes/app/audit/AuditLogAspect.java`
  — `persistHttp` falls back to `audit.diffNote` when `HasAuditNote` is absent

## Files created

- `backend/app/src/main/java/com/drshoes/app/order/OrderUpdateDiff.java`
  — pure static helper: computes Polish diff string for all updateable fields
- `backend/app/src/test/java/com/drshoes/app/order/OrderUpdateDiffTest.java`
  — 8 unit tests (no Spring context)
- `backend/app/src/test/java/com/drshoes/app/order/api/OrderUpdateAuditDiffIntegrationTest.java`
  — 3 ITs via MockMvc + Testcontainers Postgres

## Commands run

```
mvn test -pl app -Dtest="OrderUpdateDiffTest"          # 8/0/0/0 GREEN
mvn test -pl app -Dtest="OrderUpdateAuditDiffIntegrationTest"  # 3/0/0/0 GREEN
mvn test -pl app                                        # 452/0/0/0 GREEN
```

## Test summary

| Suite | Result |
|---|---|
| OrderUpdateDiffTest (unit) | 8 / 0 / 0 / 0 |
| OrderUpdateAuditDiffIntegrationTest | 3 / 0 / 0 / 0 |
| Full backend suite | 452 / 0 / 0 / 0 |

Baseline before task: 441. Net new: +11.

## Decisions

- `HasAuditNote` note from operator still takes priority over diff note (explicit beats computed).
- `audit.locationFrom` / `audit.locationTo` path from task 10-5 unchanged — orthogonal attributes.
- `currentStorageLocationId` skipped in diff per spec (proto column, M10 deferred).
- `userNameResolver` in `OrderService` uses injected `UserRepository.findById`.

## Commit SHA

b81cce2
