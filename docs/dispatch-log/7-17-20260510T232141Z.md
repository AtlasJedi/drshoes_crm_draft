---
task_id: 7-17
plan: docs/superpowers/plans/2026-05-10-milestone-07-clients.md
milestone: 7
agent: main (inline — small mechanical refactor)
start_utc: 2026-05-10T19:48:30Z
end_utc: 2026-05-10T19:49:30Z
status: DONE
commit_sha: ad4babb
---

# Task 7-17 — Sidebar real nav + active-state wiring

## Files changed
- `apps/web/components/admin/AdminSidebarNav.tsx` (new, 60 LOC — CC, usePathname active-state)
- `apps/web/components/admin/AdminSidebar.tsx` (modified — delegates nav to AdminSidebarNav; stays SC)

## Commands run
```
cd apps/web
pnpm vitest run
  # 172 passed
pnpm lint
  # eslint . --max-warnings=0 — exit 0
pnpm tsc --noEmit
  # 6 pre-existing errors (7-6/7-7/7-8 typed-routes), no new ones
```

## Test summary
- Tests run: full suite 172/0/0/0 (no new tests added — sidebar nav is testable via existing route smoke tests + manual UAT).

## Decisions / deviations from plan
- Plan's NavLink used a single `matchPrefix` string with a comment noting Dashboard `===` would fire before `startsWith` — but JS `||` short-circuit means startsWith would still match `/admin/orders` against prefix `/admin`. Replaced with an explicit `exact?: boolean` prop. Cleaner contract, same effect.
- Used `pathname.startsWith(href + "/")` to avoid `/admin/orders` matching against a hypothetical `/admin/order` link.
- Skipped manual UAT walkthrough (plan Step 5) per owner direction — automated suite + lint + tsc are sufficient for stub-tier work.
- Did NOT create `milestone-7` git tag — owner will tag after reviewing.

## Logging / metadata wired
- `AdminSidebarNav` logs at DEBUG: `op=AdminSidebarNav.render` on each render.

## Concerns
- **6 pre-existing tsc errors** (carried from 7-6/7-7/7-8) — Next 16 typed-routes `RouteImpl<...>` not satisfied by raw string templates in `ClientListSearchBox`, `ClientListTable`, `clients/page.tsx`. Hygiene carry-forward, blocks `pnpm tsc --noEmit` from being green but does NOT block builds (Next compiles fine). Fix in a dedicated hygiene pass.
- `AdminSidebarNav.tsx` uses `href as Route` cast to dodge the same typed-routes issue. Acceptable for `/admin/*` static routes; if hygiene pass narrows this, update consistently.

## Next action
- Owner inspects M7 closure → owner creates `milestone-7` local tag → ROADMAP.md bump.
