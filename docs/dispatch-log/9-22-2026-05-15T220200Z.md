# Dispatch Log — Task 9-22

**Task:** `<FreshReservationsPanel>` NEW + finalise Dashboard page layout
**Milestone:** 9 — Design parity
**UTC:** 2026-05-15T22:02:00Z
**Review type:** combined single-stage
**Status:** APPROVED

## Files created / modified

| File | Action |
|------|--------|
| `apps/web/app/(admin)/admin/_components/FreshReservationsPanel.tsx` | created — 69 LOC |
| `apps/web/app/(admin)/admin/_components/__tests__/FreshReservationsPanel.test.tsx` | created — 5 tests |
| `apps/web/app/(admin)/admin/page.tsx` | modified — 3-col lower row, FreshReservationsPanel import |

## Commands run

```
pnpm --filter web test -- --run _components/__tests__/FreshReservationsPanel
# RED: module not found (expected)

pnpm --filter web test -- --run _components/__tests__/FreshReservationsPanel
# GREEN: 5/5 passed

pnpm --filter web test -- --run
# GREEN: 407/407 passed (64 test files)
```

## Test summary

- 5 new tests: renders heading, renders placeholder client names, renders 3 otwórz buttons, renders product name, empty state
- All 407 vitest tests GREEN

## Decisions

- Used `admin-card` CSS class directly (consistent with ReadyForPickupPanel + RecentMessagesPanel) instead of `<AdminCard>` wrapper — both are equivalent; direct class avoids extra import
- PhImg `label=""` (empty) to suppress default "IMG" label on 40×40 placeholder
- Dashboard lower row changed from `grid-cols-[1.2fr_1fr]` to `grid-cols-[1.2fr_1fr_1fr]`
- FreshReservationsPanel not wrapped in `<Suspense>` (static data, no async boundary needed)

## Backend gap (M10)

Backend `GET /api/admin/sklep/reservations?limit=3&sort=createdAt,desc` does not exist.
Task 9-34 (V016 product_reservation table + ProductReservationController) must land first.
After 9-34: wire panel via `rows` prop or internal fetch. `TODO(M10)` comment in source.

## Commit

`c16a5e0` feat(dashboard): add FreshReservationsPanel (placeholder data, M10 wiring deferred) [milestone:9][task:9-22]
