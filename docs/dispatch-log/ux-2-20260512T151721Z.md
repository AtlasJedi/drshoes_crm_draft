# Dispatch Log — ux-2

**Task:** Calendar Week + Day views with received + pickup markers
**UTC:** 2026-05-12T15:17:21Z
**Commit:** 62ee793

## Files Modified

| File | Change |
|------|--------|
| `backend/app/src/main/java/com/drshoes/app/order/api/CalendarController.java` | `toDto()` always populates `receivedAt` (removed `includePickup ? null : …` guard) |
| `backend/app/src/main/java/com/drshoes/app/order/dto/CalendarResponseDto.java` | Javadoc updated: `receivedAt` now always non-null for both scheduled and unscheduled |
| `backend/app/src/test/java/com/drshoes/app/order/api/CalendarControllerIntegrationTest.java` | Added `scheduledEntryHasReceivedAtPopulated` test |
| `apps/web/lib/calendar/types.ts` | Comment updated to reflect new contract |
| `apps/web/lib/calendar/window.ts` | New: pure date-math helpers (mondayOfWeek, weekDays, weekWindow, dayWindow, isoToDateStr) |
| `apps/web/lib/calendar/__tests__/window.test.ts` | New: 19 unit tests covering all window.ts exports |
| `apps/web/app/(admin)/admin/orders/calendar/page.tsx` | Full rewrite: mode toggle → Links (not disabled buttons), ?mode=month\|week\|day + ?date= routing, per-mode grid rendering, per-mode navigation (prev/next in days/weeks/months) |
| `apps/web/app/(admin)/admin/orders/_components/calendar/CalendarWeekGrid.tsx` | New: 7-column ISO-week grid, acid dot for receivedAt, magenta dot for plannedPickupAt |
| `apps/web/app/(admin)/admin/orders/_components/calendar/CalendarDayGrid.tsx` | New: single-day two-section card (Przyjęte / Do odbioru) |
| `apps/web/app/(admin)/admin/orders/_components/calendar/__tests__/CalendarWeekGrid.test.tsx` | New: 8 render/behaviour tests |
| `apps/web/app/(admin)/admin/orders/calendar/__tests__/page.test.tsx` | Updated: replaced disabled-button test with enabled-link tests; added week-grid and day-grid mode tests; added mocks for CalendarWeekGrid and CalendarDayGrid |

## Commands Run

```
# Backend test
mvn -pl app -am -Dsurefire.failIfNoSpecifiedTests=false -Dtest=CalendarControllerIntegrationTest test
# Result: Tests run: 8, Failures: 0, Errors: 0

# Frontend tests
cd apps/web && pnpm vitest run --reporter=dot
# Result: Tests 265 passed (265); 2 pre-existing Playwright e2e files skipped
```

## Test Summary

- Backend: **8/8** CalendarControllerIntegrationTest (incl. new `scheduledEntryHasReceivedAtPopulated`)
- Frontend vitest: **265/265** passing
  - `lib/calendar/__tests__/window.test.ts` — 19 new tests (mondayOfWeek, weekDays, weekWindow, dayWindow, isoToDateStr)
  - `_components/calendar/__tests__/CalendarWeekGrid.test.tsx` — 8 new tests
  - `calendar/__tests__/page.test.tsx` — 3 replaced + 2 new tests (7 total in that file)

## Decisions

1. **receivedAt always populated on scheduled entries** — the `toDto()` method's `includePickup ? null : o.getReceivedAt()` conditional was the root cause. Changed to unconditional `o.getReceivedAt()`. Javadoc updated in both DTO and types.ts.
2. **No date-fns** — not in `package.json`; used built-in Date arithmetic in `window.ts`.
3. **EmptyState only shown when scheduled is empty** — unchanged from month view; week/day grids render naturally with empty buckets (show "—" placeholder per cell).
4. **DemoOrderFactory.java** — had a pre-existing compile failure from ux-4 adding `quotedPriceCents`/`advancePaidCents` to `CreateOrderRequest`. Fixed by adding `null, null` args. Verified backend compiles and all 8 tests pass.
5. **BezTerminuPanel shown in all modes** — unscheduled orders are always relevant regardless of calendar mode.

## Notes

- The 2 Playwright e2e files (`_smoke.spec.ts`, `demo-flow.spec.ts`) fail in the vitest runner because Playwright's `test()` cannot be called from vitest. This is a pre-existing configuration issue, not introduced by ux-2.
- Week navigation uses 7-day jumps; day navigation uses 1-day jumps; month navigation unchanged.
