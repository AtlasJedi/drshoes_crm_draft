---
task_id: 5-12
plan: docs/superpowers/plans/2026-05-09-milestone-05-inbound-replies.md
milestone: 5
agent: sonnet
start_utc: 2026-05-10T09:46:16Z
end_utc: 2026-05-10T12:00:00Z
status: DONE
commit_sha: 1a95398
---

# Task 5-12 — `lib/messaging` frontend types + API extensions

## Files changed
- `apps/web/lib/messaging/types.ts` (modified — +35 LOC: ThreadFilter, MessageThreadDto, ThreadDetailDto, SendReplyRequest, SendNewRequest)
- `apps/web/lib/messaging/api.ts` (modified — +55 LOC + import additions: listThreads, getThread, sendReply, markThreadRead, assignUnmatched, discardUnmatched, sendNewToClient; Channel added to import block)

## Commands run
```
pnpm --filter web typecheck   # exit 0, 0 errors
# (pnpm lint skipped — next lint deprecated in Next 16, see M3 part 3 carry-forward)
# (pnpm test: echo placeholder only — no web unit tests configured)
```

## Test summary
- TypeScript typecheck: 0 errors
- No unit tests for lib modules (test runner is playwright placeholder per package.json); no new tests needed per plan (types + fetch wrappers only)

## Decisions / deviations from plan
- **Channel explicitly imported** in api.ts: plan said "if Channel is not in scope, add it" — it was not (previously only imported via type-only imports of derived types), so Channel added to import block explicitly.
- All 7 new API functions exported with `log.info("op=...")` entries using the existing `messagingLog`-pattern logger (named `messaging.api`).
- No deviations from plan's TS shapes — verified field-for-field against `MessageThreadDto.java`, `ThreadDetailDto.java`, `ThreadReplyController.java`, `ThreadMutationController.java`, `ClientMessageController.java`, `SendReplyRequest.java`.

## Logging / metadata wired
- All 7 new functions log via `createLogger("messaging.api")` at INFO with `op=<funcName>` and relevant identifiers.

## Concerns
- (none)

## Next action
- Move to task 5-13 (MessagesNavItem sidebar + unread polling + OrderDrawerMessages NIT fixups).
