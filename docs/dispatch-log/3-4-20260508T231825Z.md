---
task_id: 3-4
plan: docs/superpowers/plans/2026-05-09-milestone-03-photos.md
milestone: 3
agent: sonnet
start_utc: 2026-05-08T23:15:00Z
end_utc: 2026-05-08T23:22:53Z
status: DONE
commit_sha: f8e9138
---

# Task 3-4 — Retrofit MessagesController to use AdminPrincipal.userId()

## Files changed
- `backend/app/src/main/java/com/drshoes/app/messaging/api/MessagesController.java` (modified)
- `backend/app/src/test/java/com/drshoes/app/messaging/api/MessagesControllerActorTest.java` (new, 82 LOC)
- `backend/app/src/test/java/com/drshoes/app/AdminWebTestBase.java` (modified — loginAsOwner/loginAsEmployee now return UUID and inject AdminPrincipal)

## Commands run
```
mvn -pl app -am test -Dtest=MessagesControllerActorTest  # exit 0 (1/0/0/0)
mvn -pl app -am test                                      # exit 0 (154/0/0/1)
```

## Test summary
- Target test: 1 run, 0 failures, 0 errors, 0 skipped
- Full suite: 154 run, 0 failures, 0 errors, 1 skipped (@Disabled PhotoRepositoryTest from 3-1)

## Decisions / deviations from plan
- Plan test imports `com.drshoes.app.messaging.domain.MessageRepository` and `AuditEventRepository` — neither exists. Used actual packages: `com.drshoes.app.messaging.repository.MessageRepository` and `com.drshoes.app.audit.AuditLogRepository`.
- Plan test calls helper methods `createClientAndReturnId(name, phone, email)` and `createOrderAndReturnId(clientId)` which don't exist in `AdminWebTestBase`. Inlined client+order creation in the test (same pattern as `MessagesControllerIntegrationTest`).
- `AdminWebTestBase.loginAsOwner()` changed return type `void` → `UUID` and now injects a real `AdminPrincipal`-backed `UsernamePasswordAuthenticationToken` via `SecurityMockMvcRequestPostProcessors.authentication()`. All 40+ existing callers that ignore the return value compile unchanged.
- `loginAsEmployee()` got the same treatment for consistency (also returns UUID now).
- Both `list` and `send` endpoints retrofitted (plan gave discretion; consistent is better).

## Logging / metadata wired
- `MessagesController.send` logs at INFO: `op=messages.send actor={email} actorId={uuid} orderId={} templateId={} channel={} outcome=ok`
- `MessagesController.list` logs at INFO: `op=messages.list actor={email} orderId={} outcome=ok`
- `AuditLogAspect` (unchanged) also logs `actorId={}` from the AdminPrincipal in SecurityContext.

## Concerns
- none

## Next action
- Move to task 3-5 per plan (Photo JPA entity + PhotoRepository).
