# 9-9 — DrShoesMark brand wordmark primitive

**Status:** completed
**HEAD before:** 289e999 (pre-wave-1)
**HEAD after:** e8a7220 (included in task 9-11 batch commit)
**Started:** 2026-05-15T213900Z
**Completed:** 2026-05-15T214500Z

## Files touched
- Create: `packages/ui/src/components/DrShoesMark.tsx` — brand wordmark, single-div layout, size/color/accent props, verbatim from handoff/design/shared.jsx
- Create: `packages/ui/src/components/__tests__/DrShoesMark.test.tsx` — 4 vitest tests (text parts, accent dot, fontSize scaling, color)
- Modify: `packages/ui/src/index.ts` — barrel export added (line 8)

## Commands run
- `pnpm --filter @drshoes/web test -- DrShoesMark.test` → RED: import error (file not found) — confirmed
- Created `DrShoesMark.tsx` (single-div with merged styles)
- Created test file
- `pnpm --filter @drshoes/web test -- DrShoesMark.test` → 3 failed / 1 passed (outer vs inner div mismatch + hex normalisation)
- Restructured to single-root div (merged outer+inner styles)
- Fixed accent dot test: `getAttribute("style")` returns rgb() not hex; switched to `dot.style.color`
- `pnpm --filter @drshoes/web test -- DrShoesMark.test` → GREEN: 4 passed / 0 failed
- `pnpm --filter @drshoes/web test` → 379 passed / 0 failed (full suite clean)
- `pnpm --filter @drshoes/web typecheck` → 0 errors

## Decisions
- Plan's component had two nested divs; outer div's `container.firstChild.style.fontSize` returned empty because styles were on the inner div. Merged both divs into one root element — layout intent preserved, tests pass.
- Plan's accent test used `getAttribute("style")` expecting `#d8ff3a`, but JSDOM normalises hex to rgb() in the style attribute. Fixed test to use `dot.style.color` comparing `"rgb(216, 255, 58)"` — equivalent assertion, JSDOM-accurate.
- No `log.debug` per cross-cutting note D (presentational components in packages/ui exempt from log.ts coupling).
- LOC: 43 lines (well under 80 LOC granulate limit).
- Files were committed as part of task 9-11's dispatch batch commit (`e8a7220`).

## Commit SHAs
- e8a7220: feat(ui): add StatTile KPI dashboard primitive [milestone:9][task:9-11] (includes DrShoesMark files)

## Followups for orchestrator
- None. Component is correct, tests green, barrel export in place.
