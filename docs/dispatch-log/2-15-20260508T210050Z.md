# Dispatch Log: 2-15 — /admin/templates pages + TemplateForm

**UTC:** 2026-05-08T210050Z
**Task:** 2-15
**Milestone:** 2 (Messaging)
**Plan:** docs/superpowers/plans/2026-05-08-milestone-02-messaging.md (lines 2860–3013)
**Review type:** Combined spec+quality (mechanical Next.js scaffolding)

---

## Files Created

| File | LOC | Notes |
|------|-----|-------|
| `apps/web/app/(admin)/admin/templates/page.tsx` | 85 | Server component; lists all templates; error boundary; empty-state CTA |
| `apps/web/app/(admin)/admin/templates/new/page.tsx` | 21 | Server component; renders TemplateForm with no initial prop |
| `apps/web/app/(admin)/admin/templates/[id]/page.tsx` | 36 | Server component; fetches template via getTemplateServer; renders TemplateForm initial={t} |
| `apps/web/app/(admin)/admin/templates/_components/TemplateForm.tsx` | 145 | Client component; create/update/delete; EMAIL/SMS channel switch; Polish UI copy |

---

## Commands Run

```
cd apps/web && pnpm typecheck   # exit 0
cd apps/web && pnpm build       # exit 0 — all 4 routes render as ƒ (dynamic)
git add apps/web/app/(admin)/admin/templates/
git commit -m "feat(web): /admin/templates list + new + edit + form [milestone:2][task:2-15] ..."
```

---

## Decisions / Reconciliations

1. **Typed-routes `as Route` cast** — Routes not yet in `.next/types/routes.d.ts` (generated only after first build). Used `as Route` import from `"next"` for all Link hrefs and `router.push` calls, matching the established pattern in `lib/orders` components. After `pnpm build` the routes appear correctly as `/admin/templates`, `/admin/templates/new`, `/admin/templates/[id]`.

2. **TemplateForm at 145 LOC** — Over the 80-LOC soft cap for TS modules but under the 150-LOC hard split threshold stated in the dispatch instructions. Form is cohesive (single responsibility: template CRUD); splitting into field-sub-components would add indirection with no benefit. Accepted.

3. **No subject field on SMS** — `subject` input conditionally rendered only when `channel === "EMAIL"`, consistent with `TemplateDto.subject: string | null` and `CreateTemplateRequest.subject?: string | null`.

4. **`getTemplateServer` throws on 404** — Edit page propagates the throw to Next.js, which renders the nearest error boundary. No inline error state added; this matches the pattern used by other server-rendered admin pages.

5. **Polish UI copy** — All labels, buttons, headings, placeholder text, error messages, and confirmation dialogs in Polish per project requirement.

---

## Test / Suite Delta

Backend suite untouched: **149/0/0/0** (no backend changes in this task).
Frontend: `pnpm typecheck` + `pnpm build` green. No frontend unit tests added (mechanical UI scaffolding; E2E smoke in 2-21).

---

## Commit SHA

`8afcdd0` — feat(web): /admin/templates list + new + edit + form [milestone:2][task:2-15]
