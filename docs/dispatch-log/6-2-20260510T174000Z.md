---
task_id: 6-2
plan: docs/superpowers/plans/2026-05-10-milestone-06-orders-dashboard.md
milestone: 6
agent: sonnet
start_utc: 2026-05-10T17:20:00Z
end_utc: 2026-05-10T17:40:00Z
status: DONE
commit_sha: bc9d54c
---

# Task 6-2 — MessageRouter split + sendRetry/send dedup

## Files changed
- `backend/app/src/main/java/com/drshoes/app/messaging/service/MessageRecipientResolver.java` (new, 39 LOC)
- `backend/app/src/test/java/com/drshoes/app/messaging/service/MessageRecipientResolverTest.java` (new, 96 LOC — 6 unit tests)
- `backend/app/src/main/java/com/drshoes/app/messaging/service/MessageRouter.java` (modified — 272 LOC → 192 LOC)

## Commands run
```
# Step 2: RED — compile failure confirmed
cd backend && mvn -pl app -am -B test -Dsurefire.failIfNoSpecifiedTests=false -Dtest=MessageRecipientResolverTest
# → FAILURE: cannot find symbol class MessageRecipientResolver

# Step 4: GREEN — resolver tests pass
cd backend && mvn -pl app -am -B test -Dsurefire.failIfNoSpecifiedTests=false -Dtest=MessageRecipientResolverTest
# → Tests run: 6, Failures: 0, Errors: 0, Skipped: 0

# Step 7: integration tests pass after MessageRouter refactor
cd backend && mvn -pl app -am -B test -Dsurefire.failIfNoSpecifiedTests=false -Dtest=MessageRouterIntegrationTest
# → Tests run: 3, Failures: 0, Errors: 0, Skipped: 0

# Step 8: full suite
cd backend && mvn -pl app -am -B verify
# → Tests run: 310, Failures: 0, Errors: 0, Skipped: 0  BUILD SUCCESS
```

## Test summary
- **Before (6-1 baseline):** 304/0/0/0
- **After:** 310/0/0/0
- Net change: +6 new unit tests (MessageRecipientResolverTest), 0 failures

## Decisions / deviations from plan

- **LOC deviation:** Plan stated ≤ 130 LOC post-split for MessageRouter. The plan's own reference implementation (lines 593–770 of the plan file) produces 192 LOC when written with Javadoc, blank lines, and proper formatting. The 192 LOC is the exact plan reference implementation — no extra content added. The 30% reduction (272 → 192) achieves the structural goals. Noted in commit message.
- **unknownChannelThrowsIllegalArgument test fix:** Plan's test code stubbed `clients.findById(clientId)` in the WHATSAPP test, but `Channel.valueOf("WHATSAPP")` succeeds (WHATSAPP is a valid enum) and the `default` branch fires before any repository call. Mockito's strict mode flagged UnnecessaryStubbing. Removed the stub. Added a second test `completelyUnknownChannelThrowsIllegalArgument` to cover the `Channel.valueOf()` failure path — total 6 tests vs plan's 5.
- **Resolver class signature:** `MessageRecipientResolver.resolve(UUID clientId, String channel) → String (nullable)` — exactly as specified.
- **No RetryReason enum:** The plan mentioned "RetryReason enum (or equivalent)" as a send/sendRetry dedup mechanism, but the reference implementation in the plan does not actually introduce a RetryReason enum — it deduplicates via `buildOutbound()` helper instead. Followed the plan's reference implementation (no enum).
- **ClientRepository dropped from MessageRouter:** Confirmed — `ClientRepository` is no longer injected into `MessageRouter`. The resolver owns it.

## Logging / metadata wired
- `MessageRouter` retains all existing INFO/WARN log lines with key=value fields (op=, outcome=, orderId=, channel=, etc.) — no logging changes.
- `MessageRecipientResolver` is a pure logic component; no logging added (lookup result is not worth an INFO line — callers log the outcome).

## Concerns
- None.

## Next action
- Move to task 6-3 (V013 message_thread uniqueness migration + @NotNull validation) per plan.
