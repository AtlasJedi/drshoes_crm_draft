# 9-13 — Button primitive

**Status:** completed
**HEAD before:** b814167
**HEAD after:** (see commit SHA below)
**Started:** 2026-05-15T194138Z
**Completed:** 2026-05-15T194500Z

## Files touched
- Create: `packages/ui/src/components/Button.tsx` — graffiti-style button, 68 LOC, primary/acid/pink/paper/ghost variants + sm/md/lg sizes, renders as `<a>` when href provided
- Create: `packages/ui/src/components/__tests__/Button.test.tsx` — 10 tests covering all variants, href→<a> fallback, onClick, disabled, default <button>
- Modify: `packages/ui/src/index.ts` — append `export * from "./components/Button"`

## Commands run
- pnpm --filter @drshoes/web test -- Button.test → RED: 0 tests / 1 failed (import error, Button.tsx not found)
- (create Button.tsx + add barrel export)
- pnpm --filter @drshoes/web test -- Button.test → GREEN: 15 passed (10 Button + 5 tokens)
- pnpm --filter @drshoes/web typecheck → 0 errors
- pnpm --filter @drshoes/web test → 379 passed / 0 failed (was 304; +75 from packages/ui tests now resolving)

## Decisions
- `resolve.alias` was already set in vitest.config.ts (by a prior task) to map `@testing-library/react` to apps/web node_modules — Button test worked without config changes
- `SIZE_CLASS` record used `lg: ""` as no-op so the size prop is extensible without breaking the API
- No `log.debug` per cross-cutting note D (presentational component, no coupling to apps/web lib/log.ts)
- Full suite went from 304 to 379 tests passing — the vitest alias fix from a previous task unblocked all pending packages/ui tests

## Commit SHAs
- (see git log after commit)

## Followups for orchestrator
- None for this task. 9-14 (icons.tsx) is next in Wave 1.
