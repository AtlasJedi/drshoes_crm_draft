# Dispatch Log — Task 8-20

## Task
- **ID:** 8-20
- **Title:** Sidebar wire-up for `ReportIssueButton`
- **Plan file:** `docs/superpowers/plans/2026-05-11-milestone-08-demo-ready.md`
- **Plan lines:** 3584–3789
- **Review type:** Combined single-stage

## Files Changed

- `apps/web/components/admin/AdminSidebarNav.tsx` — added `Props { userEmail: string }`, added `import ReportIssueButton`, rendered `<ReportIssueButton user={userEmail} />` inside `<div className="border-t border-admin-line mt-4 pt-3">` as last nav element (lines 43–68)
- `apps/web/components/admin/AdminSidebar.tsx` — updated `<AdminSidebarNav />` call to `<AdminSidebarNav userEmail={me.email} />` (line 14)
- `apps/web/components/admin/__tests__/AdminSidebarNav.test.tsx` — created; 3 vitest+RTL cases: button presence, modal open on click, email shown in modal

## Commands Run

```
pnpm --filter @drshoes/web typecheck   → PASS (clean)
pnpm --filter @drshoes/web lint        → PASS (0 warnings)
pnpm --filter @drshoes/web test --run  → PASS 188/0/0/0
```

## Test Summary

| | Before | After |
|---|---|---|
| Test files | 39 | 40 |
| Tests pass | 185 | 188 |
| Fail | 0 | 0 |
| Error | 0 | 0 |
| Skip | 0 | 0 |

## Decisions

- Only one call site confirmed (`AdminSidebar.tsx:14`) via `grep -rn AdminSidebarNav apps/web/`. Adding `userEmail: string` as a required prop is safe — no other callers.
- `ReportIssueButton` placed inside `<div className="border-t border-admin-line mt-4 pt-3">` separator per plan spec, matching the visual separator pattern used by section headers.
- Test mocks `@opentelemetry/api` (required because `ReportIssueButton` imports `trace` at module level). Mock provided `getActiveSpan` returning a span with `traceId: "abc123"` — matches the plan's test snippet exactly.
- `navigator.clipboard` stubbed via `Object.defineProperty` with `configurable: true`.
- No out-of-plan changes made.

## Review

**Verdict: APPROVED**

Checklist:
- [x] `AdminSidebarNav` accepts `userEmail: string` prop
- [x] `ReportIssueButton` imported and rendered as last nav element with border-t separator
- [x] `AdminSidebar` passes `me.email` as `userEmail`
- [x] 3 vitest cases: button presence, modal open, email in modal — all GREEN
- [x] Full suite 188/0/0/0 — no regressions
- [x] Typecheck clean
- [x] Lint clean (0 warnings)
- [x] No ad-hoc `console.log` (uses `lib/log.ts`)
- [x] No layout invention — placement specified by plan

## Commit SHA

- Implementation: `eb23485`

## Follow-ups

None from this task.

## Subagent Token Budget

- `total_tokens`: ~18,000 (estimated)
