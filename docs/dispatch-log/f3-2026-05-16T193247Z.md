# Dispatch Log — Task F3

**Task:** Frontend — "Pilne" filter preset + q-rewrite
**UTC:** 2026-05-16T193247Z
**Plan:** docs/superpowers/plans/2026-05-16-m11-fix-batch.md § Task F3
**Branch:** main
**HEAD before:** 63c2ae5

## Files modified

| File | Change |
|------|--------|
| `apps/web/lib/orders/types.ts` | Added `urgent?: boolean` to `OrderListFilters` interface |
| `apps/web/lib/orders/api-server.ts` | Pass `urgent=true` query param in `buildQuery` when filter is set |
| `apps/web/app/(admin)/admin/orders/page.tsx` | Added `urgent?: string` to `SearchParams`; coerce to boolean; pass to `listOrdersServer`; pass `rows` to `SavedFilterPresets` |
| `apps/web/app/(admin)/admin/orders/_components/SavedFilterPresets.tsx` | Added `rows` prop, `pilneActive`/`pilneCount` derivation, `togglePilne` handler, "Pilne" chip with pink color |
| `apps/web/app/(admin)/admin/orders/_components/OrdersFilters.tsx` | Added q-rewrite: "pilne" / "pilne na …" → `?urgent=true`, removes `q`; normal search removes `urgent` |
| `apps/web/app/(admin)/admin/orders/_components/__tests__/OrdersFilters.test.tsx` | New: 5 vitest cases covering q-rewrite logic |

## Commands run

```
cd apps/web && pnpm vitest run
```

## Test summary

- Before: 597 tests / 90 files passing
- After: 602 tests / 95 files passing (+5 new tests, all green)

## Decisions

- Pilne chip count derived from current page rows only (acceptable per spec — no separate fetch)
- `togglePilne` preserves existing search params except `urgent` and `page` (reset pagination on filter change)
- q-rewrite uses case-insensitive match; triggers on exact "pilne" or prefix "pilne na "
- `hasAnyFilter` updated to include "urgent" so "Wszystkie" reset chip appears when urgent filter is active

## Commit SHA

(see git log)
