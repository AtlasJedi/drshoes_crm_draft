# Dispatch log — Task F2
**Task:** Frontend — Miejsce chip + urgent highlight across list/kanban/calendar
**UTC:** 2026-05-16T193314Z
**Plan:** docs/superpowers/plans/2026-05-16-m11-fix-batch.md § Task F2

## Files touched

| Action | File |
|--------|------|
| Created | `apps/web/app/(admin)/admin/orders/_components/LocationChip.tsx` |
| Modified | `apps/web/app/(admin)/admin/orders/_components/OrderDrawerHeader.tsx` |
| Modified | `apps/web/app/(admin)/admin/orders/_components/OrdersTable.tsx` |
| Modified | `apps/web/app/(admin)/admin/orders/_components/kanban/KanbanCard.tsx` |
| Modified | `apps/web/app/(admin)/admin/orders/_components/calendar/CalendarCell.tsx` |
| Modified | `apps/web/lib/kanban/types.ts` |
| Created | `apps/web/app/(admin)/admin/orders/_components/__tests__/OrdersTable.test.tsx` |

## Steps executed

1. **LocationChip created** — inline style extracted verbatim from OrderDrawerHeader (acid background `var(--acid, #d8ff3a)`, 1.5px ink border, t-mono class, 📍 glyph). Two variants: `md` (11px, 2px 8px padding) and `sm` (10px, 1px 6px padding).
2. **OrderDrawerHeader updated** — inline chip JSX replaced with `<LocationChip name={location ?? null} />`. Behaviour identical; aria-label preserved inside LocationChip.
3. **OrdersTable — location cell** — `{row.location ?? "—"}` replaced with `<LocationChip name={row.location} variant="sm" />`. Empty cell when null (no "—" fallback).
4. **OrdersTable — urgent row** — `<tr>` gains `className={row.urgent ? "bg-magenta/10 border-l-2 border-magenta" : ""}`. Existing onClick/onKeyDown/tabIndex untouched.
5. **KanbanCard — urgent class** — replaced `border-l-[3px] border-l-[var(--pink)]` with `border-2 border-magenta bg-magenta/5` per spec.
6. **KanbanCard — LocationChip** — `card.location` optional field consumed; renders `<LocationChip name={card.location} variant="sm" />` in a `mt-1` div between description and bottom row.
7. **KanbanCardDto type** — added `location?: string | null` to frontend type so card.location compiles without cast. Backend DTO does not yet expose this field (no-op until backend ships it).
8. **CalendarCell — urgent tint** — per-order `<button>` gains `border-magenta bg-magenta/10` classes when `order.urgent`. Status-colour background suppressed for urgent orders (magenta takes precedence).
9. **OrdersTable.test.tsx created** — 3 cases: urgent row has `magenta` in className, non-urgent does not; LocationChip renders when location set; empty cell when null.

## Commands run

```
cd apps/web && pnpm test
```

## Test summary

```
Test Files  96 passed (96)
Tests       605 passed (605)
```

All green. No regressions.

## Decisions

- KanbanCardDto `location` typed as optional (`string | null | undefined`) because the backend KanbanCardDto record does not yet include the field. The chip renders nothing when undefined/null — safe for current prod, automatically activates once backend ships.
- CalendarCell urgent orders: status colour fully overridden by magenta tint (cleaner visual hierarchy). Non-urgent orders unchanged.
- Container rebuild deferred per plan instructions (V1 sweep will trigger it).

## Commit SHA

(written after commit below)
