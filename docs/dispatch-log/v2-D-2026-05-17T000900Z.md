# Dispatch Log: v2-D — Dashboard money tiles

**Task:** v2-D  
**Milestone:** v2-fixes  
**UTC timestamp:** 2026-05-17T00:09:00Z  
**Commit SHA:** ab92886

## Files modified

| File | Change |
|---|---|
| `backend/app/src/main/java/com/drshoes/app/order/domain/OrderRepository.java` | Added `sumTotalPriceByStatusIn` + `sumTotalPricePickedUpBetween` JPQL queries |
| `backend/app/src/main/java/com/drshoes/app/dashboard/dto/DashboardKpiDto.java` | Extended record with 4 new fields: `inProgressMoneyCents`, `inProgressMoneyFormatted`, `pickedUpMoneyMonthCents`, `pickedUpMoneyMonthFormatted` |
| `backend/app/src/main/java/com/drshoes/app/dashboard/api/DashboardController.java` | Populated new fields; extended structured log line with `inProgressMoney` + `pickedUpMonth` |
| `backend/app/src/test/java/com/drshoes/app/dashboard/api/DashboardKpiControllerIntegrationTest.java` | Added 2 new tests: `inProgressMoneyAggregatesWRealizacjiAndPrzyjete` + `pickedUpMoneyMonthExcludesLastMonthOrders` |
| `apps/web/lib/dashboard/types.ts` | Added 4 new fields to `DashboardKpiDto` interface |
| `apps/web/app/(admin)/admin/_components/KpiTilesRow.tsx` | Replaced tiles 3+4 with `kpi-tile-in-progress-money` + `kpi-tile-picked-up-money` |
| `apps/web/app/(admin)/admin/_components/__tests__/KpiTilesRow.test.tsx` | Updated fixture and assertions for new tile testids |

## Test summary

**Backend** (`DashboardKpiControllerIntegrationTest`): 7/7 GREEN (5 pre-existing + 2 new)
- `inProgressMoneyAggregatesWRealizacjiAndPrzyjete`: asserts sum of W_REALIZACJI + PRZYJETE = 35000 cents
- `pickedUpMoneyMonthExcludesLastMonthOrders`: asserts this-month WYDANE = 40000 cents; last-month WYDANE excluded

**Frontend** (`KpiTilesRow.test.tsx`): 6/6 GREEN
- All tile testid, label, value, and accent assertions updated

**Full suite status at commit:**
- Backend: 490 total, 7 new KPI tests all GREEN; 3 pre-existing failures from v2-C/v2-E partial landings (ClientMessageController, ThreadReplyController, TemplateContextBuilderTest) — not introduced by v2-D
- Frontend vitest: 608 total, 606 GREEN; 2 pre-existing failures in KanbanBoardWrapper.test.tsx (v2-A) — not introduced by v2-D

## Decisions

- Kept the existing 5 DTO fields (`todayIntakeCount`, `monthRevenueCents`, `monthRevenueFormatted`) intact for backward compat; FE ignores unused fields
- `sumTotalPricePickedUpBetween` uses JPQL with fully-qualified enum literal for the WYDANE status check
- Month window uses Europe/Warsaw timezone (same as existing `monthStart`/`monthEnd` vars in controller)
