# Dispatch Log: 9-21 — Restyle ReadyForPickupPanel + RecentMessagesPanel

**Task:** 9-21
**Plan:** docs/superpowers/plans/2026-05-15-milestone-09-design-parity.md
**UTC:** 2026-05-15T20:02:56Z
**Status:** completed
**Review:** combined single-stage

## Files Modified

- `apps/web/app/(admin)/admin/_components/ReadyForPickupPanel.tsx` — rewritten: `<div class="admin-card">` → `<AdminCard padding={22}>`, pink span badge → `<Tape angle={-2}>`, added `<PhImg>` 44×44 thumbnail + bold clientName, added `<Pill status>` per row
- `apps/web/app/(admin)/admin/_components/RecentMessagesPanel.tsx` — rewritten: `<div class="admin-card">` → `<AdminCard padding={22}>`, channel moved from plain `t-mono` div to bordered chip (`bg-[var(--ink)] text-[var(--paper)] border border-[var(--ink)]`); circular avatar already present, preserved with style-prop form
- `apps/web/app/(admin)/admin/_components/__tests__/ReadyForPickupPanel.test.tsx` — updated: added `clientName` bold test + Tape `/czeka/` test (6 tests total, was 4)
- `apps/web/app/(admin)/admin/_components/__tests__/RecentMessagesPanel.test.tsx` — updated: added `initial avatar` + `channel chip` assertions (7 tests total, was 5)

## Commands Run

```
pnpm --filter web test -- --run "_components/__tests__/ReadyForPickupPanel"
# Result: 6/6 GREEN

pnpm --filter web test -- --run "_components/__tests__/RecentMessagesPanel"
# Result: 7/7 GREEN

pnpm --filter web test -- --run
# Result: 407/407 GREEN (64 test files)
```

## Test Summary

- ReadyForPickupPanel: 6/6 pass
- RecentMessagesPanel: 7/7 pass
- Full suite: 407/407, 0 failures

## Decisions

- `PhImg` receives `label=""` (empty string) because these are placeholder thumbnails; when real photo URLs ship in M10 the component will be swapped for `<Image>`.
- `Tape` uses default `color="acid"` — plan specified `angle={-2}` only, no color override.
- Channel chip uses `bg-[var(--ink)] text-[var(--paper)]` (inverse of paper/ink) per plan spec "ink bg paper text" interpreted as ink-background + paper-text for visual contrast.
- `RecentMessagesPanel` avatar already used `font-mono font-bold` inline; refactored to `style={}` form matching the plan's reference code to avoid Tailwind purge issues with dynamic font-family.

## Commit SHA

(set after commit)
