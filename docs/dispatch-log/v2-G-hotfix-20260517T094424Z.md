# Dispatch Log — v2-G-hotfix

**Task:** v2-G-hotfix — Fix V025 migration statement order (DROP before UPDATE)
**UTC:** 20260517T094424Z
**Milestone:** v2-fixes

## Files Modified

| File | Change |
|------|--------|
| `backend/app/src/main/resources/db/migration/V025__order_item_kind_custom_merge.sql` | Reordered: DROP CONSTRAINT first, then UPDATE, then ADD CONSTRAINT; added explanatory comment |

## Root Cause

Original V025 ran `UPDATE order_item SET kind='CUSTOM' WHERE kind IN ('CUSTOM_BUTY','CUSTOM_KURTKA')` **before** dropping the check constraint. The constraint `order_item_kind_check` only allowed `(NAPRAWA, CUSTOM_BUTY, CUSTOM_KURTKA)`, so inserting the new `CUSTOM` value would violate it before the DROP ran. Fixed by reordering: DROP → UPDATE → ADD.

## Decisions

- Fixed before V025 ever ran in production — no data migration needed, pure SQL reorder.
- Explanatory comment added inline so the fix rationale is preserved in the migration itself.

## Test Results

| Suite | Result |
|-------|--------|
| Backend health (`curl /actuator/health`) | `{"status":"UP"}` |
| Flyway schema history (psql query) | V025 not yet applied (fix is preemptive) |

## Commit SHA

35aed7c
