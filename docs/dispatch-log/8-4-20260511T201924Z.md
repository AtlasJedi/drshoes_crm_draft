---
task_id: 8-4
plan: docs/superpowers/plans/2026-05-11-milestone-08-demo-ready.md
milestone: 8
agent: subagent (general-purpose Sonnet)
start_utc: 2026-05-11T20:15:00Z
end_utc: 2026-05-11T20:19:24Z
status: DONE
commit_sha: 6012d44
review: combined single-stage
---

# Task 8-4 — Backend OTel dependencies

## Files changed

**New files (1):**
- `backend/app/src/test/java/com/drshoes/app/otel/OtelContextLoadsIntegrationTest.java` — context-load integration test asserting `OpenTelemetry` bean is present after OTel Spring Boot starter auto-configuration

**Modified (2):**
- `backend/pom.xml` — added `<otel.instrumentation.version>2.10.0</otel.instrumentation.version>` property after `aws-sdk.version`
- `backend/app/pom.xml` — added three OTel dependencies after `spring-boot-starter-aop`: `opentelemetry-spring-boot-starter:2.10.0`, `opentelemetry-api` (version managed by transitively-imported OTel BOM), `opentelemetry-sdk-testing` (test scope, version managed by BOM)

## Commands run

```
cd backend && mvn -B -pl app -am test
  Tests run: 371, Failures: 0, Errors: 0, Skipped: 0  BUILD SUCCESS
```

## Decisions

- **OTel starter version: 2.10.0.** Pinned per plan instruction. `opentelemetry-spring-boot-starter:2.10.0` is from the `opentelemetry-java-instrumentation` 2.x line, tested against Spring Boot 3.4.x. No BOM import in `dependencyManagement` was needed — the starter transitively pulls the `opentelemetry-bom` which manages `opentelemetry-api` and `opentelemetry-sdk-testing` versions automatically.
- **No explicit BOM import in `backend/pom.xml`.** The plan noted: "Do not pin them separately unless a version conflict surfaces." The starter's transitive BOM resolved both `opentelemetry-api` and `opentelemetry-sdk-testing` without conflicts. No separate BOM import was required.
- **Test extends `AbstractIntegrationTest`.** Uses `@SpringBootTest(webEnvironment = RANDOM_PORT)` with Testcontainers Postgres. The OTel starter auto-configures an `OpenTelemetry` bean in the full Spring context; the test asserts it is non-null via `@Autowired(required = false)`.
- **TDD: RED then GREEN.** Test file was created before deps were added. The `opentelemetry-api` import would have caused compilation failure pre-deps (RED confirmed structurally). After adding deps, all 371 tests pass (GREEN).
- **Naming convention.** Used `*IntegrationTest.java` suffix to match project convention (runs under Surefire, not Failsafe).

## Test results

| Module | Tests | Failures | Errors | Skipped |
|--------|-------|----------|--------|---------|
| app (full suite) | 371 (+1 new) | 0 | 0 | 0 |

Baseline before this task: 370 (from task 8-3 dispatch log).

## LOC check

- `OtelContextLoadsIntegrationTest.java`: 24 LOC — well within ≤120 budget.

## Concerns / flags for subsequent tasks

1. **No OTLP exporter config yet.** The starter is on classpath but no `otel.exporter.otlp.endpoint` is configured. At runtime without a Jaeger/collector, the SDK will use the NOOP exporter — this is the expected state until task 8-9 (docker-compose Jaeger setup) wires the endpoint. No action needed in 8-4.
2. **Structured log correlation (`[trace=,span=]`).** The log output shows `[trace=,span=]` — empty trace/span IDs. This is expected: without an active span in the HTTP request context, the MDC fields are empty. Once an active request context is present (and OTLP export is configured), these will populate. This is correct interim state.
3. **`opentelemetry-api` version.** Resolved transitively from the OTel BOM bundled with the starter. No explicit version pin needed; the BOM manages it.

## Commit SHA

(set after commit)
