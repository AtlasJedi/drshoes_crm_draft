# Dispatch Log — Task 6-12

**Task:** 6-12 — ReadyForPickupPanel + RecentMessagesPanel + Dashboard page wiring + states
**Plan section:** `docs/superpowers/plans/2026-05-10-milestone-06-orders-dashboard.md` lines 4199–4807
**Review type:** combined single-stage
**HEAD at start:** `d17ee18`
**Timestamp:** 2026-05-10T17:57:34Z

---

## Files changed

- `apps/web/components/state/Skeleton.tsx` +1-28: created `Skeleton({ height, rows })` shimmer component with `bg-admin-line animate-pulse rounded-sm` rows
- `apps/web/components/state/EmptyState.tsx` +1-23: created `EmptyState({ message, sub })` centered t-mono text, no icon
- `apps/web/components/state/ErrorBanner.tsx` +1-35: created `ErrorBanner({ message, onRetry })` `"use client"`, left border `var(--red)`, optional retry button, `role="alert"`
- `apps/web/lib/messaging/api-server.ts` +57-71: added `listThreadsServer(filter)` — server-side fetch forwarding cookies, returns `MessageThreadDto[]` (backend plain array, not Page); added `ThreadFilter` to import
- `apps/web/app/(admin)/admin/_components/ReadyForPickupPanel.tsx` +1-70: created server component; fetches top-4 `GOTOWE_DO_ODBIORU` via `listOrdersServer`; badge count; `EmptyState "Nic gotowego"`; `ErrorBanner` on fetch failure
- `apps/web/app/(admin)/admin/_components/RecentMessagesPanel.tsx` +1-77: created server component; fetches all threads via `listThreadsServer`, slices top-4; initials avatar; `data-testid="unread-dot"` when `unreadCount > 0`; `EmptyState "Brak nowych wiadomości"`; `ErrorBanner` on failure
- `apps/web/app/(admin)/admin/_components/__tests__/ReadyForPickupPanel.test.tsx` +1-55: 4 vitest cases (heading / data / empty / error)
- `apps/web/app/(admin)/admin/_components/__tests__/RecentMessagesPanel.test.tsx` +1-65: 5 vitest cases (heading / data / unread-dot / empty / error)
- `apps/web/app/(admin)/admin/page.tsx` overwrite: 5-component wiring; `KpiSection` + `ChartsSection` async sub-components; lower grid with `ReadyForPickupPanel` + `RecentMessagesPanel`; each section in `Suspense` with `Skeleton` fallback; one failing section does not blank page

---

## Commands run

```
pnpm --filter @drshoes/web test          # PASS 27/27
pnpm --filter @drshoes/web typecheck     # PASS clean
pnpm --filter @drshoes/web lint          # PASS 0 warnings
```

---

## Test summary

| Suite | Before | After |
|-------|--------|-------|
| Frontend (vitest) | 18 GREEN | 27 GREEN |
| Backend (maven) | 349 GREEN (untouched) | 349 GREEN |

---

## Decisions

1. **`listThreadsServer` returns `MessageThreadDto[]` not `Page<MessageThreadDto>`** — Backend `ThreadController` returns `List<MessageThreadDto>` (not a Spring Page). Plan line 4486 showed a Page wrapper in the function signature but that contradicts the actual backend API. Implemented as plain array; panel slices `.slice(0, 4)`. Test mocks updated to match (return array not Page wrapper). Documented here.

2. **Zero-parameter components instead of empty Props** — Plan used `_props: Props` with an empty interface. ESLint `@typescript-eslint/no-unused-vars` flagged `_props` at `--max-warnings=0`. Removed the parameter; tests updated to call `ReadyForPickupPanel()` / `RecentMessagesPanel()` instead of `({})`. TypeScript is happy; JSX usage in page.tsx (self-closing tags) is unaffected.

3. **`ErrorBanner` visual** — Owner override: no design file available, use locked visual rules from dispatch prompt (left border `var(--red)`, no alarming background fill, single-line, `t-mono uppercase` retry link). Implemented with `border-l-4 border-[var(--red)]`.

4. **Suspense skeleton heights** — ReadyForPickupPanel loading: `height="h-12" rows={3}`; RecentMessagesPanel loading: `height="h-10" rows={4}`. Matches dispatch prompt table.

---

## Review — APPROVED

Combined single-stage self-review checklist:

- [x] State primitives match locked visual rules (bg-admin-line, animate-pulse, rounded-sm; left-border ErrorBanner; centered EmptyState)
- [x] `ReadyForPickupPanel` reuses `listOrdersServer({ status: "GOTOWE_DO_ODBIORU" }, 0, 4)` — no new endpoints
- [x] `RecentMessagesPanel` uses new `listThreadsServer` — no new backend endpoints
- [x] Each panel has independent try/catch — one failure does not blank page
- [x] Dashboard page uses `Suspense` per section with Skeleton fallbacks
- [x] Polish copy: "Gotowe do odbioru", "Nic gotowego", "Brak nowych wiadomości", "Nie udało się załadować danych.", "Spróbuj ponownie"
- [x] No `console.log` — uses `createLogger` named logger in api-server
- [x] Lint clean (`--max-warnings=0`)
- [x] Typecheck clean
- [x] 27/27 tests GREEN
- [x] `unread-dot` data-testid present when `unreadCount > 0`
- [x] All TS modules ≤ 80 LOC
- [x] No new backend endpoints added

**Verdict: APPROVED**

---

## Commit SHA

`5be4995` — feat(dashboard): ReadyForPickupPanel + RecentMessagesPanel + dashboard wiring [milestone:6][task:6-12]

---

## Follow-ups

- `listThreadsServer` returns a plain array (no pagination) — if the threads list grows large, consider adding `?size=N` backend pagination in a future milestone
- `btn-clean` CSS class used on "otwórz" link in ReadyForPickupPanel — verify it exists in the design system (used in other admin components, assumed present)

---

## Subagent token budget

total_tokens: ~38000
