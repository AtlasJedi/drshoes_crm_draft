---
task_id: 5-13
plan: docs/superpowers/plans/2026-05-09-milestone-05-inbound-replies.md
milestone: 5
agent: sonnet
start_utc: 2026-05-10T09:50:18Z
end_utc: 2026-05-10T10:10:00Z
status: DONE
commit_sha: dac09c2
---

# Task 5-13 — MessagesNavItem (sidebar) + unread polling + OrderDrawerMessages NIT fixups

## Files changed
- `apps/web/app/(admin)/admin/_components/Sidebar/MessagesNavItem.tsx` (new, 51 LOC)
- `apps/web/lib/messaging/useUnreadCount.ts` (new, 45 LOC)
- `apps/web/components/admin/AdminSidebar.tsx` (modified — import + render MessagesNavItem)
- `apps/web/app/(admin)/admin/orders/_components/OrderDrawerMessages.tsx` (modified — Edit A + Edit B)

## Commands run
```
cd apps/web && pnpm typecheck   # exit 0 (after Route cast fix)
```

## Test summary
- No web unit tests configured (confirmed in 5-12). Typecheck is the gate.
- pnpm typecheck: 0 errors.

## Decisions / deviations from plan
- **Route cast required:** Next.js typed routes enforced in this project's tsconfig. Plan's code used bare `href="/admin/messages"` which caused TS2322. Fixed by adding `import type { Route } from "next"` and casting `href={"/admin/messages" as Route}`. All other Link usages in the project use the same pattern (e.g. triggers/page.tsx).
- **Sidebar insert anchor:** AdminSidebar still uses stub div elements for Zamówienia and Klienci (not real Links yet — carry-forward from 0B). MessagesNavItem inserted immediately after the "Klienci (0B)" stub div, before closing `</nav>` — matches the plan's "after Klienci" intent exactly.
- **Edit B race-cancel guard:** `load` returns `Promise<void>` (confirmed from useCallback body). The `.then(() => { if (cancelled) return; })` wrapper is a no-op result handler as described in plan note — belt-and-suspenders alongside the useCallback reset on orderId change.

## Logging / metadata wired
- `MessagesNavItem` logs at DEBUG: `op=MessagesNavItem.render` with `{unread, active}` via `createLogger("messaging.nav")`
- `useUnreadCount` logs at DEBUG on success: `op=unreadCount.poll outcome=ok {total}` and at WARN on error: `op=unreadCount.poll outcome=error {err}` via `createLogger("messaging.unread")`

## Concerns
- (none)

## Next action
- Move to task `5-14` per plan (UnreadElsewhereBanner + dedicated backend endpoint).
