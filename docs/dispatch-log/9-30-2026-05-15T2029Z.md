# Dispatch Log: 9-30 — Messages 3-col layout + ClientMiniProfile NEW

**Task:** 9-30
**UTC:** 2026-05-15T20:29Z
**Commit SHA:** 2c256ce (bundled into 9-31 bookkeeping commit)
**Status:** DONE

## Files modified
- `apps/web/app/(admin)/admin/messages/_components/MessagesShell.tsx` — replaced flex layout with `grid-cols-admin-msg-3`, removed MessagesHeader import, wired ClientMiniProfile in right rail
- `apps/web/app/(admin)/admin/messages/_components/ThreadList.tsx` — design parity: border-r-2 border-ink, graffiti search box, Chip filter chips (nieprzeczytane/wymaga odp./wszystkie) + channel chips (WhatsApp/Email/SMS/IG)
- `apps/web/app/(admin)/admin/messages/_components/ThreadListRow.tsx` — graffiti row: 36px paper-2 avatar, acid-tinted active state, mono ink channel chip, 8px pink unread dot

## Files created
- `apps/web/app/(admin)/admin/messages/_components/ClientMiniProfile.tsx` — main component (~80 LOC), fetches client + active orders, loading skeleton, empty panel
- `apps/web/app/(admin)/admin/messages/_components/ClientMiniProfileIdentity.tsx` — 64px acid avatar, t-display-22 name, klient-od date, Sticker "stały klient"
- `apps/web/app/(admin)/admin/messages/_components/ClientMiniProfileContact.tsx` — phone/email/preferredChannel key-value rows
- `apps/web/app/(admin)/admin/messages/_components/ClientMiniProfileOrders.tsx` — active orders mini-list with PhImg + DR code + Pill
- `apps/web/app/(admin)/admin/messages/__tests__/ClientMiniProfile.test.tsx` — 6 tests covering all states
- `apps/web/lib/orders/types.ts` — added `clientId?: string` to OrderListFilters
- `apps/web/lib/orders/api.ts` — wired `clientId` param in buildOrdersQuery

## Test summary
- ClientMiniProfile: 6/6 GREEN
- Full suite: 472/472 GREEN (no regressions)
- typecheck: pre-existing errors in packages/ui only (StatTile, Sticker, Tape, Toggle) — not introduced by this task

## Decisions
- Sub-components extracted to sibling files to stay ≤80 LOC each per granulate directive
- `listOrders` called with `clientId` filter + 4 active status values (PRZYJETE, W_REALIZACJI, CZEKA_NA_KLIENTA, GOTOWE_DO_ODBIORU), limit 5
- MessagesHeader component kept on disk (not deleted) — still referenced by nothing now but safe to remove in M10 cleanup
- MessagesPageHeaderSetter unchanged — continues to set topbar title via usePageHeader
- `I.search` icon used in graffiti search box from `@repo/ui`
- "wymaga odp." chip wired to `filter="ALL"` fallback with TODO comment (backend doesn't support NEEDS_REPLY yet)
- IG channel chip renders but passes empty onClick (no IG integration yet)
