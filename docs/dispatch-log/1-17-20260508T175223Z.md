---
task_id: 1-17
plan: docs/superpowers/plans/2026-05-08-milestone-01-orders.md
milestone: 1
agent: sonnet
start_utc: 2026-05-08T17:52:23Z
end_utc: 2026-05-08T18:30:00Z
status: DONE
commit_sha: 8f9df16
---

# Task 1-17 — OrderDrawerStatusChanger + trigger preview placeholder

## Files changed
- `apps/web/app/(admin)/admin/orders/_components/OrderDrawerStatusChanger.tsx` (new, 80 LOC)
- `apps/web/app/(admin)/admin/orders/_components/StatusChangeConfirm.tsx` (new, 49 LOC)
- `apps/web/app/(admin)/admin/orders/_components/OrderDrawer.tsx` (modified — import + replace status stub)

## Commands run
```
cd apps/web && pnpm typecheck   # exit 0 — zero errors
cd apps/web && pnpm build       # exit 0 — clean production build, all admin routes ƒ dynamic
```

## Test summary
- Tests run: N/A (web test command is a no-op placeholder until milestone 1-20 playwright)
- typecheck: PASS (zero errors)
- build: PASS (clean)

## Decisions / deviations from plan
- `StatusChangeConfirm.tsx` was extracted (49 LOC) because the combined file hit 115 LOC before split. Plan says "only if it actually pushes the file past 80 LOC" — it did, so the split is sanctioned.
- `OrderDrawerStatusChanger.tsx` is exactly 80 LOC (including trailing newline = 79 content lines) — within cap.
- `changeStatus` helper already existed in `lib/orders/api.ts` (shipped in 1-12). No new client helper needed.
- `StatusChangeConfirm` uses `@radix-ui/react-dialog` (already installed as of 1-13). No new deps.
- `OrderDrawer.tsx` already managed `order` state via `useState<OrderDto>(initialOrder)` (shipped in 1-16). No state lifting needed — `setOrder` passed directly as `onOrderUpdated`.
- Conflict error detection uses `(err as { status?: number })?.status === 409`. The `api` helper from `lib/api.ts` throws objects with a `status` field on non-2xx responses — confirmed consistent with existing error handling in `OrderDrawerCoreFields`.
- All 7 statuses rendered (including `WSTEPNIE_PRIJETE` and `ANULOWANE`) per `STATUS_ORDER` constant from `lib/orders/status.ts`. Current status button is disabled + ring-highlighted; non-current buttons have reduced opacity with hover restore.

## Logging / metadata wired
- `createLogger("status-changer")` wired in `OrderDrawerStatusChanger.tsx`.
- `op=open-confirm` logged at INFO when confirm dialog opens (includes orderId, from, to).
- `op=changeStatus outcome=ok` logged at INFO on successful POST.
- `op=changeStatus outcome=conflict` logged at INFO on 409.
- `op=changeStatus outcome=error` logged at ERROR on unexpected errors.

## Concerns
- Conflict error surfaced as inline `aria-live="assertive"` region — no toast primitive available (same pattern as 1-16). Extract to a shared toast in M2.
- `WSTEPNIE_PRZYJETE` is selectable as a target status via the UI (it appears in `STATUS_ORDER`). Plan does not explicitly exclude it from manual selection in the status changer (only in the create-order form). Acceptable for M1 — Misza can manually set it if needed; restrict in M2 if needed.
- Unexpected errors (non-409) log at ERROR but are not surfaced to the user — silent failure. Acceptable for M1; add user-visible error state in M2.

## Next action
- Move to task `1-18`: `OrderDrawerItems` CRUD.
