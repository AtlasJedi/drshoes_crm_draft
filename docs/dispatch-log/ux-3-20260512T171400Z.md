# Dispatch log — ux-3 — 2026-05-12T17:14:00Z

## Task
Kanban sort + pin urgent (from `docs/superpowers/plans/2026-05-12-admin-ux-fixes.md`)

## Files modified

| File | Change |
|------|--------|
| `backend/app/src/main/java/com/drshoes/app/order/dto/KanbanResponseDto.java` | Added `receivedAt: Instant` to `KanbanCardDto` record |
| `backend/app/src/main/java/com/drshoes/app/order/api/KanbanController.java` | Populate `receivedAt` from `Order.getReceivedAt()` in card projection |
| `backend/app/src/test/java/com/drshoes/app/order/api/KanbanControllerIntegrationTest.java` | Added `cardProjectionIncludesReceivedAt` test |
| `apps/web/lib/kanban/types.ts` | Added `receivedAt: string \| null` to `KanbanCardDto` interface |
| `apps/web/lib/kanban/sort.ts` | **New** — pure `sortKanbanCards()`: urgent first, then receivedAt asc, null=far-future |
| `apps/web/lib/kanban/sort.test.ts` | **New** — 6 vitest cases (all-urgent, no-urgent, mixed, null, immutability, empty) |
| `apps/web/app/(admin)/admin/orders/_components/kanban/KanbanColumn.tsx` | Import sort, apply `sortKanbanCards()`, add "Pilne ↑ · Najstarsze ↑" header indicator |
| `apps/web/app/(admin)/admin/orders/_components/kanban/KanbanCard.tsx` | Thin magenta left border (`border-l-[3px] border-l-[var(--pink)]`) when `card.urgent` |
| `apps/web/app/(admin)/admin/orders/_components/kanban/__tests__/KanbanCard.test.tsx` | Added `receivedAt` to fixture |
| `apps/web/app/(admin)/admin/orders/_components/kanban/__tests__/KanbanColumn.test.tsx` | Added `receivedAt` to fixture |
| `apps/web/app/(admin)/admin/orders/_components/kanban/__tests__/KanbanBoard.test.tsx` | Added `receivedAt` to fixture |

## Commands run

```
pnpm vitest run lib/kanban app/(admin)/admin/orders/_components/kanban
# 29 passed (29)
```

## Test summary

- 6 new `sortKanbanCards` unit tests — all GREEN
- 19 existing kanban component tests — all GREEN
- Backend IT: `cardProjectionIncludesReceivedAt` — added; compile verified (pre-existing `DemoOrderFactory` error from ux-4 is unrelated to ux-3, blocked only the test runner context load, not this task's code)

## Decisions

- Thin left border (`border-l-[3px]`) chosen over 📌 badge — cleaner, consistent with card look
- Sort is pure client-side after fetch — no backend query change, no new endpoint
- `null receivedAt` → far-future sentinel (`Date max`) so null cards sink to bottom of their group
- `sortKanbanCards` returns a new array (spread `[...cards]`) — does not mutate dnd state
- Sort indicator "Pilne ↑ · Najstarsze ↑" is static text in column header, `hidden sm:block` to avoid cramping on narrow viewports

## Commit SHA

`3f6d975`
