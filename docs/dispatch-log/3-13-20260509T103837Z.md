---
task_id: 3-13
plan: docs/superpowers/plans/2026-05-09-milestone-03-photos.md
milestone: 3
agent: opus-inline
start_utc: 2026-05-09T10:36:00Z
end_utc: 2026-05-09T10:38:37Z
status: DONE
commit_sha: 98f9769
---

# Task 3-13 — Smoke + milestone-3 tag + CLAUDE.md flip

## Files changed
- `CLAUDE.md` (status row 127: `[ ] Milestone 3: Real providers + photos` → `[x] Milestone 3: Photos + actor resolution (real providers deferred to a later milestone)`)
- Local git tag `milestone-3` created (NOT pushed)

## Commands run
```
cd backend && mvn -B verify          # exit 0 — Tests run: 167, Failures: 0, Errors: 0, Skipped: 0
cd apps/web && pnpm typecheck        # exit 0
cd apps/web && pnpm build            # exit 0 — Next.js 16.2.6, all routes compiled
cd apps/web && pnpm lint             # exit 1 — pre-existing breakage (NOT introduced by M3)
```

## Test summary
- Backend: **167 / 0 / 0 / 0** (M2 close was 150 → +17 from M3 photo + AdminPrincipal coverage).
- Frontend typecheck: clean.
- Frontend production build: clean. All admin routes compile and render dynamically (`ƒ`) as expected for cookie-bearing routes.

## Decisions / deviations from plan
- **Smoke approach: automated only**, per owner direction (matches M2 precedent of skipping docker-compose UI smoke, but here we ran the full backend + frontend automated checks).
- **`pnpm lint` skipped**: `apps/web/package.json` has `"lint": "next lint --max-warnings=0"`, but Next.js 16 removed the `next lint` subcommand entirely. There is no `eslint.config.*` in `apps/web/` either, so eslint 9 (flat config required) cannot run directly. This is **pre-existing tech debt** from the M0A skeleton (commit `cb15024`) — not introduced by M3. Documented as deferred debt below.
- Plan tag annotation predicted "Suite: 150 → 162/0/0/0"; actual is 150 → 167. Tag annotation reflects the real number.

## Logging / metadata wired
- None — closure task; no new code paths.

## Concerns
- **Pre-existing lint breakage** in `apps/web`: `next lint` removed in Next 16, and no eslint flat config exists. To restore lint coverage: either add a minimal `eslint.config.mjs` using `@next/eslint-plugin-next` flat preset and rewrite the script to `eslint .`, or drop the `lint` script entirely. Tracked for a future hygiene pass — does not block M3 closure.
- **UI smoke not run**: docker compose photo-upload-relabel-delete-Audyt round trip not verified end-to-end. Backend integration tests cover the API surface (Testcontainers MinIO in `PhotoControllerIT`), and frontend typecheck + build cover compile-time correctness. Manual UI smoke deferred per owner direction.

## Next action
- Milestone 3 closed locally. Possible next moves:
  - Plan Milestone 4 (real providers — Postmark / SMSAPI / inbound parsing).
  - Address the deferred lint hygiene pass.
  - Address other M3 deferred debt (PhotoCard outside-click menu close, Lightbox remount flash on prev/next, drag-drop UI on PhotoUploader, MIME magic-byte verification).
