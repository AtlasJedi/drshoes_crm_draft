# Dispatch Log — Task 10-4

**Task:** V019 migration + Order.location field + OrderDto update
**Milestone:** 10
**Timestamp:** 2026-05-16T09:45:00Z
**Agent:** Claude Sonnet 4.6 (inline, no subagent)

## Files Created / Modified

| File | Change |
|------|--------|
| `backend/app/src/main/resources/db/migration/V019__orders_location.sql` | Created — ALTER TABLE order_ ADD COLUMN location VARCHAR(64) NULL |
| `backend/app/src/main/java/com/drshoes/app/order/domain/Order.java` | Added `location` field + getLocation/setLocation accessors |
| `backend/app/src/main/java/com/drshoes/app/order/dto/OrderDto.java` | Added `String location` record component + wired in `OrderDto.of()` |
| `backend/app/src/test/java/com/drshoes/app/order/domain/OrderLocationFieldIntegrationTest.java` | Created — 2 IT cases extending AbstractIntegrationTest |

## Key Decisions

- Table is `order_` (confirmed via V001__init.sql) — plan text said `orders` which was wrong; fixed.
- `location` placed before `description` in OrderDto record for logical grouping (storage metadata near storage fields).
- `OrderDto.of()` is the only mapper — no external `OrderMapper` class exists.
- IT uses same `newMinimalOrder()` pattern as `OrderRepositoryIntegrationTest` (code + clientId + status).

## Test Results

- `OrderLocationFieldIntegrationTest`: **2/2 PASS**
- Full backend suite: **433/0/0/0** (baseline 431 + 2 new = 433, zero regressions)

## Commits

- feat: `d680a66` — feat(order): add location field to order_ table + OrderDto (V019) [milestone:10][task:10-4]

## Mapper / Fixture Files Touched

- `OrderDto.java` — only mapper (`OrderDto.of()` inline static factory) updated
- No external fixture files required updating (no JSON snapshot tests asserting full OrderDto shape)
