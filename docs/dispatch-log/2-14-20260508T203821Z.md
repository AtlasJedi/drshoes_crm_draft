# Dispatch Log: 2-14 — lib/messaging API client + types + server wrappers

**Task:** 2-14
**UTC:** 20260508T203821Z
**Milestone:** 2 (Messaging)
**Commit SHA:** 46dead5

---

## Files Created

- `apps/web/lib/messaging/types.ts` — Channel, TemplateDto, CreateTemplateRequest, UpdateTemplateRequest, TriggerDto, MessageDto, SendMessageRequest
- `apps/web/lib/messaging/api.ts` — client-side API functions using `api` from `@/lib/api` (ApiClient)
- `apps/web/lib/messaging/api-server.ts` — server-side fetch wrappers using inline fetch + `cookies()` from `next/headers`

---

## Commands Run + Outcomes

```
pnpm typecheck   → PASS (zero TS errors)
pnpm build       → PASS (clean build, Compiled successfully in 1347ms)
```

Pre-existing WARN on `/admin/orders/new` about `cookies` in static context — not introduced by this task, not a build failure.

---

## Decisions Made (Plan Reconciliation)

### 1. `apiFetch` import → `api` from `@/lib/api`
Plan body showed `import { apiFetch } from "@/lib/orders/api"` — this helper does not exist. Actual pattern (from M1 task 1-12) exports named functions using the `api` singleton (ApiClient class in `@/lib/api`) with `api.get/post/patch/delete`. All calls in `api.ts` adapted accordingly. Endpoint paths adjusted: plan used full `/api/admin/...` prefixes but `ApiClient.request` prepends `NEXT_PUBLIC_API_BASE` (which already includes `/api`). Canonical pattern from `lib/orders/api.ts` omits the `/api` prefix — followed that: `/admin/templates`, `/admin/orders/{id}/messages`, etc.

### 2. `serverFetch` import → inline fetch pattern
Plan body showed `import { serverFetch } from "@/lib/orders/api-server"` — no such export exists. The `api-server.ts` file uses inline `fetch()` with `cookies()` from `next/headers`, forwarding the session cookie, and `INTERNAL_API_BASE` env var. Followed that pattern verbatim. Extracted two small private helpers (`base()` and `cookieHeader()`) to avoid repetition across 5 exported functions. Server-side paths use full `/api/admin/...` prefix (backend receives raw HTTP, not Next.js proxy).

### 3. Log style
Plan showed `log.info({ op: "create", name: req.name })` (object-first). Actual `createLogger` pattern is `log.info("op=listOrders", { page, size })` (string-first, key=value fields). All log calls adapted to match.

### 4. LOC counts
- `types.ts`: 63 LOC — within limits (pure types)
- `api.ts`: 65 LOC — within 80 LOC ceiling
- `api-server.ts`: 62 LOC — within 80 LOC ceiling

---

## Suite Delta

Backend: 149/0/0/0 (unchanged — frontend-only task, no mvn run)
Frontend: typecheck + build PASS

---

## Notes

No scope creep. No unrelated drift fixed. Three files match plan intent exactly; reconciliation was mechanical import-path correction only.
