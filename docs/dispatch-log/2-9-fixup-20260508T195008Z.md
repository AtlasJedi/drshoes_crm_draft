---
task_id: 2-9-fixup
plan: docs/superpowers/plans/2026-05-08-milestone-02-messaging.md
milestone: 2
agent: sonnet
start_utc: 2026-05-08T19:50:08Z
end_utc: 2026-05-08T19:55:00Z
status: DONE
commit_sha: ec86b96
---

# Task 2-9-fixup — Drop redundant delivered_at, use existing picked_up_at

## Files changed
- `backend/app/src/main/resources/db/migration/V008__drop_order_delivered_at.sql` (new, 6 LOC) — drops order_delivered_at_idx and order_.delivered_at column added by V007
- `backend/app/src/main/java/com/drshoes/app/order/domain/OrderRepository.java` (modified, -6/+7) — renamed findAllByDeliveredDate → findAllByPickedUpDate; query now targets picked_up_at
- `backend/app/src/main/java/com/drshoes/app/messaging/service/ScheduledTriggerJob.java` (modified, -1/+1) — runAfterHandover calls findAllByPickedUpDate
- `backend/app/src/main/java/com/drshoes/app/order/domain/Order.java` (modified, -5/+0) — removed deliveredAt field + getDeliveredAt/setDeliveredAt accessors
- `backend/app/src/test/java/com/drshoes/app/messaging/service/ScheduledTriggerJobIntegrationTest.java` (modified, -3/+3) — createOrderHandedOverOn seeds picked_up_at instead of delivered_at

## Commands run
```
# Targeted test
cd backend && mvn -B -pl app -am test -Dtest=ScheduledTriggerJobIntegrationTest -Dsurefire.failIfNoSpecifiedTests=false
# exit 0 — Tests run: 5, Failures: 0, Errors: 0, Skipped: 0

# Full suite
cd backend && mvn -B -pl app -am test
# exit 0 — Tests run: 135, Failures: 0, Errors: 0, Skipped: 0

# Orphan grep
grep -RIn "deliveredAt\|delivered_at\|findAllByDeliveredDate" backend/app/src/
# Remaining hits: V007 (migration file — immutable), V008 (migration file — the fix itself),
# V001 line 270 (message table — different table), MessageEntity.java (delivery receipts — different table).
# Zero hits in main Java code on the order_ domain. Clean.
```

## Test summary
- Targeted (ScheduledTriggerJobIntegrationTest): Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
- Full suite: Tests run: 135, Failures: 0, Errors: 0, Skipped: 0

## Decisions / deviations from plan
- Reverted V007 via V008 (Flyway forbids amending applied migrations); renamed query method.
- Root cause: 2-9 dispatch prompt incorrectly claimed `order_.delivered_at` already existed at V001 line 270 — that line is inside the `message` table. The 2-9 subagent added V007 to satisfy the claim rather than flagging the contradiction. Fix: drop via V008, use the correct pre-existing `picked_up_at` column (V001 line 157, written by OrderService.changeStatus on WYDANE transition).
- Order.deliveredAt field + accessors removed — the 2-9 subagent had added the JPA field to match V007. Post-V008 there is no DB column, so the field must be gone.

## Logging / metadata wired
- No logging changes. ScheduledTriggerJob.runAfterHandover already logs `op=scheduled_trigger.run kind=AFTER_HANDOVER date={}` at INFO.

## Concerns
- No orphan `deliveredAt` / `delivered_at` / `findAllByDeliveredDate` references remain in order domain main code.
- V001 line 270 `delivered_at` in `message` table is unrelated and intentionally untouched.
- MessageEntity.java `deliveredAt` is the messaging delivery receipt field — unrelated and intentionally untouched.

## Next action
- Move to task `2-10` (MessageSentTimelineHandler) per plan.
