# 9-1 — Tokens + fonts + Tailwind preset + globals.css utility classes

**Status:** completed
**HEAD before:** 289e999
**HEAD after:** 53874a6
**Started:** 2026-05-15T191814Z
**Completed:** 2026-05-15T192400Z

## Files touched
- Create: `packages/ui/src/components/__tests__/tokens.test.ts`
- Modify: `packages/ui/src/tokens.ts` — full rewrite (new colour values + extended orderStatusColor)
- Modify: `packages/ui/src/fonts.ts` — add stencil descriptor + stencil CSS var
- Modify: `packages/ui/tailwind-preset.ts` — new boxShadow + gridTemplateColumns + fontFamily.stencil
- Modify: `packages/ui/src/index.ts` — verify barrel exports
- Modify: `apps/web/app/layout.tsx` — 5 next/font instances with CSS variable emission
- Modify: `apps/web/app/globals.css` — full graffiti utility class set from handoff/design/styles.css
- Modify: `apps/web/vitest.config.ts` — add include pattern for packages/ui tests

## Commands run
- pnpm --filter @drshoes/web test -- tokens.test → RED: 18 failed / 2 passed (expected)
- pnpm --filter @drshoes/web test -- tokens.test → GREEN: 20 passed
- pnpm --filter @drshoes/web typecheck → 0 errors (after fixing Big_Shoulders_Stencil name)
- pnpm --filter @drshoes/web test → 304 passed / 0 failed (was 284 before; +20 new)
- pnpm --filter @drshoes/web build → exit 0 (compiled successfully)

## Decisions
- vitest.config.ts needed explicit `include` globs covering `../../packages/ui/src/**/__tests__/**` — without it vitest only scans apps/web root
- next/font/google exports `Big_Shoulders_Stencil` not `Big_Shoulders_Stencil_Display` — plan had wrong name; corrected in both layout.tsx and fonts.ts
- plan step 5 puts font vars in `<html>` className (not body) — followed plan exactly
- "Failed to find font override values for font `Big Shoulders Stencil`" is non-fatal next/font warning (metrics DB miss) — build succeeds

## Commit SHAs
- 53874a6: feat(ui): rewrite design tokens, fonts, tailwind preset; wire 5 next/font instances; append graffiti utility classes [milestone:9][task:9-1]

## Followups for orchestrator
- Cross-cutting note A in the plan header — grep audit for orderStatusColor direct readers must run before fanning out wave 1.
