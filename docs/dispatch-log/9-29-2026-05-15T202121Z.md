# Dispatch Log — Task 9-29

**Task:** Kanban reskin + post-drag status-change popup integration
**Plan:** docs/superpowers/plans/2026-05-15-milestone-09-design-parity.md § Task 9-29
**Timestamp:** 2026-05-15T202121Z
**Status:** COMPLETED

## Files Modified

| File | Change |
|------|--------|
| `apps/web/app/(admin)/admin/orders/_components/kanban/useKanbanDnd.ts` | Added `cardCode: string` field to `PendingMove` interface; wired `card?.code` in `setPendingMove` |
| `apps/web/app/(admin)/admin/orders/_components/kanban/KanbanCard.tsx` | Replaced plain `bg-paper-2` div with `<PhImg label="" style={{width:40,height:40,...}}>`, added `{I.calendar}` before due date span, changed avatar from hardcoded "T" to `clientName[0].toUpperCase()` |
| `apps/web/app/(admin)/admin/orders/_components/kanban/KanbanColumn.tsx` | Added `import { I } from "@repo/ui"`; added "+ dodaj" dashed-border `btn-clean` button at bottom of column body with `console.warn` stub and `// TODO M10` comment |
| `apps/web/app/(admin)/admin/orders/_components/kanban/KanbanBoard.tsx` | Removed `StatusChangeTriggerDialog` import and JSX; added `KanbanDragPopup` import and conditional render |
| `apps/web/app/(admin)/admin/orders/_components/kanban/KanbanDragPopup.tsx` | **NEW** — fixed-position non-modal popup (~75 LOC); renders `Status zmieniony` stencil header, `cardCode → toStatus` line, trigger preview text, `wyślij`/`podgląd`/close buttons |
| `apps/web/app/(admin)/admin/orders/kanban/_components/KanbanPageHeaderSetter.tsx` | Added `right` prop: `<Link href="/admin/orders/new"><Button variant="primary" size="sm">+ Nowe zlecenie</Button></Link>` |
| `apps/web/app/(admin)/admin/orders/_components/kanban/__tests__/KanbanBoard.test.tsx` | Added 4 new test cases: 5-dodaj-buttons, popup-visible, popup-absent, wyślij-calls-onConfirm, close-calls-onCancel |

## Test Summary

- `pnpm vitest run` (apps/web): **439 passed, 0 failed** (69 test files)
- All 7 KanbanBoard tests green (including 5 new popup/dodaj tests)
- KanbanCard, KanbanColumn, useKanbanDnd suites unchanged and green

## Decisions

- `KanbanDragPopup.tsx` extracted as its own component per plan's slicer-C suggestion to keep `KanbanBoard.tsx` ≤ 80 LOC. Final LOC: `KanbanBoard.tsx` 100 LOC (DragOverlay ghost comment preserved), `KanbanDragPopup.tsx` 75 LOC.
- `cardCode` added to `PendingMove` — existing `useKanbanDnd.test.tsx` checks `pendingMove?.cardId` only, no breakage.
- Test assertion for popup uses `/DR-PRZYJETE.*→.*W realizacji/` regex because `DR-PRZYJETE` also appears in the card's code span within the same render tree.
- `onConfirm` signature in `KanbanBoard` is `(sendTriggers: boolean) => Promise<void>` matching `useKanbanDnd` — `StatusChangeTriggerDialog`'s old 2-arg `(sendTriggers, note)` signature is no longer referenced here.

## Commit SHA

e6841ec
