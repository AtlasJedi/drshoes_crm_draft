---
task_id: 5-3
plan: docs/superpowers/plans/2026-05-09-milestone-05-inbound-replies.md
milestone: 5
agent: sonnet
start_utc: 2026-05-10T00:30:00Z
end_utc: 2026-05-10T00:50:00Z
status: DONE_WITH_CONCERNS
commit_sha: 96ede4f
---

# Task 5-3 — MessageGatewayDispatcher hygiene extraction

## Files changed
- `backend/app/src/main/java/com/drshoes/app/messaging/service/MessageGatewayDispatcher.java` (new, 101 total / 89 non-blank LOC)
- `backend/app/src/main/java/com/drshoes/app/messaging/service/MessageRouter.java` (modified, 222 total / 193 non-blank LOC)
- `backend/app/src/test/java/com/drshoes/app/messaging/service/MessageGatewayDispatcherTest.java` (new, 5 unit tests)

## Commands run
```
mvn -B -pl app -am test -q  # baseline: 213/0/0/0 — exit 0
mvn -B -pl app -am test -Dsurefire.failIfNoSpecifiedTests=false -Dtest=MessageGatewayDispatcherTest  # RED: COMPILATION ERROR
# (created MessageGatewayDispatcher.java)
mvn -B -pl app -am test -Dsurefire.failIfNoSpecifiedTests=false -Dtest=MessageGatewayDispatcherTest  # first run: 5/0/2/0 UnnecessaryStubbing
# (fixed: lenient() on messages.save stub in buildMessage helper)
mvn -B -pl app -am test -Dsurefire.failIfNoSpecifiedTests=false -Dtest=MessageGatewayDispatcherTest  # GREEN: 5/0/0/0
# (refactored MessageRouter)
mvn -B -pl app -am test -q  # final: 218/0/0/0 — exit 0
```

## Test summary
- Tests run: 218, Failures: 0, Errors: 0, Skipped: 0
- New tests: 5 (MessageGatewayDispatcherTest: EMAIL happy path, SMS happy path, gateway throws→FAILED, unknown channel, blank recipient)
- Baseline was 213; gain is exactly 5.

## Decisions / deviations from plan
- Plan test code had `when(messages.save(any()))` inside `buildMessage()` helper. Mockito strict mode flagged it as UnnecessaryStubbing for the two exception-path tests (blank recipient, unknown channel) that never reach `messages.save()`. Fixed by switching to `lenient().when(...)` — minimal, semantics-preserving.
- Plan expected `MessageRouter` to land at ≤ 130 LOC after refactor. Actual: 222 total / 193 non-blank. The gateway-dispatch duplication (the specific carry-forward debt) is fully eliminated. Remaining bulk is javadoc, `buildContext()`, and `polishKindLabel()` — none of which were in scope for this extraction. No further split point exists without breaking cohesion.
- `MessageThreadRepository threads` field removed from `MessageRouter` (now owned exclusively by `MessageGatewayDispatcher`). `threadService` reference retained (needed by `findOrCreateForClient` calls in both `send()` and `sendRetry()`).
- `EmailGateway email` and `SmsGateway sms` fields removed from `MessageRouter`. `truncate()` static helper moved to `MessageGatewayDispatcher`.

## Logging / metadata wired
- `MessageGatewayDispatcher` logs at INFO: `op=gateway.dispatch outcome=ok messageId={} channel={} providerId={}`
- `MessageGatewayDispatcher` logs at WARN: `op=gateway.dispatch outcome=failed messageId={} channel={} err={}`
- `MessageRouter.send()` retains: `op=message.send outcome={} ...`
- `MessageRouter.sendRetry()` retains: `op=message.sendRetry outcome={} ...`
- All pre-existing `op=` key values preserved — log queries work across the refactor.

## Concerns
- `MessageRouter` LOC is 222 total / 193 non-blank — still above the 120-LOC cap by a wide margin. The gateway-dispatch duplication is gone, but `buildContext()` (25 lines) + `polishKindLabel()` (7 lines) + javadoc push the count up. A future split (e.g., extract `OrderContextBuilder`) could get `MessageRouter` under cap, but it would be a purely cosmetic split with no cohesion benefit. Flagged in commit message per plan instructions.

## Next action
- Move to task 5-4 (InboundMessageService + PhoneNormalizer) per plan.
- 5-10 and 5-11 subagents should call `MessageGatewayDispatcher.dispatch(saved, recipient, subject, body)` — the single public method on the new bean.
