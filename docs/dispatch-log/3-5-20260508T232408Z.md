---
task_id: 3-5
plan: docs/superpowers/plans/2026-05-09-milestone-03-photos.md
milestone: 3
agent: sonnet
start_utc: 2026-05-09T01:24:00Z
end_utc: 2026-05-09T01:31:00Z
status: DONE
commit_sha: 72ca604
---

# Task 3-5 — Photo JPA entity + PhotoLabel enum + PhotoRepository

## Files changed
- `backend/app/src/main/java/com/drshoes/app/photo/domain/Photo.java` (pre-existing from 3-1, no change — already complete)
- `backend/app/src/main/java/com/drshoes/app/photo/domain/PhotoLabel.java` (pre-existing from 3-1, no change — already final)
- `backend/app/src/main/java/com/drshoes/app/photo/domain/PhotoRepository.java` (pre-existing from 3-1, no change — already has required finder)
- `backend/app/src/test/java/com/drshoes/app/photo/domain/PhotoRepositoryTest.java` (modified — removed @Disabled, added @BeforeEach/@AfterEach isolation, fixed Order.code NOT NULL, switched to testUserId)

## Commands run
```
mvn -pl app -am test -Dtest=PhotoRepositoryTest -Dsurefire.failIfNoSpecifiedTests=false  # exit 0 (1 test, 0 failures)
mvn verify  # exit 0 — 154/0/0/0
```

## Test summary
- Tests run: 154, Failures: 0, Errors: 0, Skipped: 0
- PhotoRepositoryTest: 1 test, 1 passed (was @Disabled — now enabled and green)
- Net change: +1 test (153 → 154)

## Decisions / deviations from plan
- Photo.java stub from task 3-1 was already the full entity (not a stub): all 10 V009 columns mapped, `@JdbcType(PostgreSQLEnumJdbcType.class)` on the label field for correct Postgres enum binding, getters/setters complete. No changes needed to Photo.java, PhotoLabel.java, or PhotoRepository.java.
- PhotoRepositoryTest required two fixes vs the 3-1 authored version:
  1. `Order.code` is NOT NULL (V001 constraint) — added `o.setCode("PHOTO-TEST-" + UUID.randomUUID().toString().substring(0, 8))` to `newOrder()`.
  2. `users.findAll().iterator().next()` throws NoSuchElement when run after AdminWebTestBase tests (which call `users.deleteAll()` in @AfterEach). Fixed with @BeforeEach that creates a dedicated test user + @AfterEach cleanup (photos → orders → clients → user, respecting FK order).

## Logging / metadata wired
- No service logging in this task (entity + repo only). PhotoService (task 3-6) will add structured logging.

## Concerns
- none

## Next action
- Move to task 3-6 (PhotoService + @Audited + TimelineEventCurator extension — TWO-STAGE REVIEW) per plan.
