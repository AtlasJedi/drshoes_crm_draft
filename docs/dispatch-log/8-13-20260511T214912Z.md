# Dispatch Log — Task 8-13

**Task:** 8-13 — README "Run the demo" section + `.env.example` update
**Plan section:** `docs/superpowers/plans/2026-05-11-milestone-08-demo-ready.md` lines 2367–2452
**Review type:** Combined single-stage (pure docs, no code)

---

## Files changed

- `README.md` — added "## Run the demo" H2 section (lines 24–57) with `make demo` one-liner, banner output block, idempotency note, and three troubleshooting subsections (Postgres port conflict, M2/Apple Silicon multi-arch, slow backend start)
- `.env.example` — appended Jaeger (`JAEGER_OTLP_HTTP_PORT=4318`, `JAEGER_UI_PORT=16686`), OTel (`OTEL_EXPORTER_OTLP_ENDPOINT`), and demo seed (`DRSHOES_DEMO_SEED_ENABLED=false`) variables

## Commands run

- `git diff README.md .env.example` — verified changes before commit (PASS)
- `git add README.md .env.example && git commit ...` — PASS

## Test summary

- Backend: 388/0/0/0 (no change — pure docs task)
- Frontend: no change
- No test commands run (not applicable)

## Decisions

- Plan specified the "Run the demo" section to be inserted "after Quick start and before Layout" — implemented exactly; README structure is now: Quick start → Run the demo → Layout.
- Plan acceptance criteria required three troubleshooting items; plan body showed only two in the list (Postgres port conflict, M2 images) but the acceptance note also called out `make logs` for slow start — included all three as listed in plan step 2.
- Inner code fences inside the outer markdown block are rendered as bare ``` fences (no language tag on the banner block, `sh` tag on shell examples) to avoid nesting rendering issues per errata note in plan.
- `DRSHOES_DEMO_SEED_ENABLED` set to `false` in `.env.example` (the compose default); `application-local.yaml` sets it to `true` at runtime (wired in 8-5). This matches the plan's intent: the example file documents the var without enabling it by default for non-demo runs.

## Review

**Verdict: APPROVED**

Checklist:
- [x] `.env.example` documents all new compose vars: `JAEGER_OTLP_HTTP_PORT`, `JAEGER_UI_PORT`, `OTEL_EXPORTER_OTLP_ENDPOINT`, `DRSHOES_DEMO_SEED_ENABLED`
- [x] README "Run the demo" H2 section present with `make demo` command
- [x] Banner output matches what 8-12 `make demo` target prints
- [x] Seed credentials documented (misza@drshoes.pl / change-me-on-first-login)
- [x] Jaeger UI URL (http://localhost:16686) and MinIO console URL (http://localhost:9001) listed
- [x] Idempotency note present ("second time is safe")
- [x] Postgres port conflict troubleshooting present
- [x] M2/arm64 multi-arch troubleshooting present
- [x] Slow backend start troubleshooting present
- [x] All UI copy in Polish where applicable (banner uses Polish: "gotowy", "Hasło")
- [x] No layout/visual inventions — pure text documentation

## Commit SHA

- Implementation: `2da81d0`

## Follow-ups

None. Pure docs task, clean close.

## Subagent token budget

- Estimated `total_tokens`: ~18,000
