# Task 5-15 — Page shell + ThreadList + filter chips + search input (Stage 1: build)

**Dispatched:** 2026-05-10 (UTC 10:18:35)
**Plan:** docs/superpowers/plans/2026-05-09-milestone-05-inbound-replies.md (Task 5-15, lines 5736-6254)
**Review mode:** TWO-STAGE — this is Stage 1 (build); Stage 2 to be dispatched separately.

## Files created (with LOC)
- `apps/web/app/(admin)/admin/messages/page.tsx` — 19 LOC
- `apps/web/app/(admin)/admin/messages/_components/MessagesShell.tsx` — 65 LOC
- `apps/web/app/(admin)/admin/messages/_components/MessagesHeader.tsx` — 54 LOC
- `apps/web/app/(admin)/admin/messages/_components/ThreadList.tsx` — 76 LOC
- `apps/web/app/(admin)/admin/messages/_components/ThreadListSkeleton.tsx` — 19 LOC (extracted from ThreadList to stay under 80 LOC cap)
- `apps/web/app/(admin)/admin/messages/_components/ThreadListRow.tsx` — 56 LOC
- `apps/web/app/(admin)/admin/messages/_components/FilterChip.tsx` — 35 LOC
- `apps/web/app/(admin)/admin/messages/_components/useThreadSelection.ts` — 36 LOC

## Commands run
- `cd apps/web && pnpm typecheck` — zero errors
- `cd apps/web && pnpm lint` — zero warnings (eslint, not `next lint`)

## Test summary
- Frontend: `pnpm typecheck` — zero errors
- Lint: `pnpm lint` (eslint directly) — zero warnings. `next lint` was removed in Next 16; project already stubs this with eslint directly.

## Decisions / deviations
1. **`ThreadDto` → `MessageThreadDto`**: Plan literal uses `ThreadDto` throughout, but the actual types file exports `MessageThreadDto`. All files use `MessageThreadDto` consistently. No stub needed; type already shipped in task 5-12.
2. **`ThreadFilter` re-use**: Plan defines `ThreadFilter = "ALL" | "UNREAD" | "UNMATCHED"` inline in `useThreadSelection.ts`. The identical type is already exported from `@/lib/messaging/types`. Used the existing export and re-exported it from `useThreadSelection.ts` to preserve the plan's intended public API.
3. **`channel` parameter type**: Plan typed `channel` as `string | null` in `useThreadSelection`. Corrected to `Channel | null` to match `listThreads(filter, channel?: Channel, q?: string)` signature — avoids a type error and `channel ?? undefined` cast.
4. **`MessagesShell` stripped of future-task imports**: Plan's `MessagesShell` imports `SelectedThread`, `EmptyState`, `UnmatchedThreadPanel`, `ThreadClientPanel`, `NewMessageDialog` — none exist yet (tasks 5-16 through 5-20). Imported only existing components (Header + ThreadList + useThreadSelection). Added `// TODO task 5-16+` comments as scaffolding markers. The main area shows inline Polish placeholder text. This is intentional scaffolding — Stage 2 reviewer should note this is correct for a two-wave plan.
5. **`ThreadListSkeleton.tsx` added (8th file)**: `ThreadList.tsx` reached 93 LOC with the inline `SkeletonRows` helper. Extracted into a separate `ThreadListSkeleton.tsx` (19 LOC) to bring `ThreadList.tsx` to 76 LOC — within the 80 LOC cap. This follows the granularity rule.
6. **No `as Route` casts needed**: No `<Link>` tags in any of the 7 components — all navigation is via `onSelect` callbacks (prop drilling). The typed-routes issue from 5-14 does not apply here.

## Acceptance checklist (from plan — Stage 1 portion)
- [x] All 7 files created at the listed paths (plus 1 bonus extract: ThreadListSkeleton.tsx)
- [x] page.tsx awaits searchParams Promise (Next 16)
- [x] MessagesShell composes Header + List + placeholder main area (future-task slots marked with TODO comments)
- [x] FilterChip renders ALL/UNREAD/UNMATCHED choice
- [x] ThreadList polls /admin/threads list endpoint via existing api.ts `listThreads`
- [x] useThreadSelection hook exports {selectedId, filter, channel, q, setters}
- [x] LOC budget respected — all modules ≤ 76 LOC; ThreadListSkeleton extracted to achieve this
- [x] pnpm typecheck zero errors

## Commit
- SHA: 5da7c48 (feat) + 330fac2 (bookkeeping)

---

## Stage 2 Review — VERDICT: APPROVED-WITH-FOLLOWUP

**Reviewed:** 2026-05-10 (UTC ~10:25)
**Reviewer:** Sonnet subagent (cold)
**Blockers:** none

### Dimension scores
- Plan fidelity: PASS — 8 files (7 plan + 1 ThreadListSkeleton extract); deviations all upgrades.
- Polling correctness: PASS — `cancelled` flag + interval teardown + `[filter,channel,q]` deps wire correctly.
- Next 16: PASS — `searchParams` awaited, `"use client"` boundaries clean, no SSR misuse.
- Polish UI copy: PASS — all strings translated; minor NIT on UNMATCHED empty-state copy.
- Logging: PASS — `lib/log.ts` named-logger pattern used.
- Type safety: PASS — no `any`/`@ts-ignore`; `MessageThreadDto` consistent.
- LOC discipline: PASS — wc -l confirms all ≤ 76; ThreadListSkeleton extract is natural.
- A11y: FLAG — three NITs (aria-current missing, search input no aria-label, buttons no type="button").
- Defects: FLAG — cross-filter chip counts off by design (active chip correct; inactive counts reflect current filter's result set).

### NITs / follow-ups
- A11y NITs (aria-current, aria-label, type="button") — **fixed inline in commit `de8b585`.**
- MessagesShell: redundant `as Channel | null` cast + render-time `log.debug` noise — **fixed inline in commit `de8b585`.**
- ThreadListSkeleton width arrays simplified vs plan (`62%`/`48%`/`72%` vs single shared array) — cosmetic, defer.
- UNMATCHED empty-state copy collapsed with ALL — defer.
- Cross-filter chip count accuracy — architectural; defer (would need separate global-count fetch or server total field).
- Search debounce: plan acceptance says "debounces" but plan code sample has no debounce. Stage 1 matched the code. **Owner decision deferred.** Today: 2-char min server-side cutoff in `api.ts` mitigates traffic.

### Inline fixup commit
- SHA: `de8b585` — `fix(messages): a11y + type-safety NIT fixups from 5-15 review`
