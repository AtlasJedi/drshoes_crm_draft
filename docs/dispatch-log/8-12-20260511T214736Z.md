# Dispatch Log — Task 8-12

## Task

- **ID:** 8-12
- **Title:** `make demo` Makefile target
- **Plan file:** `docs/superpowers/plans/2026-05-11-milestone-08-demo-ready.md`
- **Plan section:** lines 2299–2366
- **Review type:** combined single-stage

## Files Changed

- `Makefile` +17 lines: added `.PHONY: demo demo-banner` declaration; `demo` target (lines 34–40) runs `docker compose up -d --build` with `DRSHOES_DEMO_SEED_ENABLED=true`, waits for backend health on `:8080`, waits for web on `:3000`, then calls `$(MAKE) demo-banner`; `demo-banner` target (lines 42–49) prints ANSI-colored access banner with admin URL, credentials, Jaeger UI, and MinIO.

## Commands Run

```
make -n demo       # dry-run — PASS, all 12 commands expanded correctly, no syntax errors
make -n demo-banner # standalone dry-run — PASS
git add Makefile && git commit ...  # PASS — 1 file changed, 17 insertions(+), 1 deletion(-)
```

## Test Summary

- Backend: no tests touched — pure Makefile change; suite remains 388/0/0/0 (unchanged)
- Frontend: no tests touched
- Sanity: `make -n demo` and `make -n demo-banner` both exit 0 with correct command expansion

## Decisions

1. **Tab indentation verified:** All recipe lines use literal tabs (required by make). The edit tool preserved them correctly.
2. **`$(MAKE)` for demo-banner:** Plan specifies `$(MAKE) demo-banner` rather than inlining the printf commands directly — allows `demo-banner` to be called standalone and is the correct make idiom. Followed exactly.
3. **No timeout added to health-wait loops:** Plan does not specify a timeout; the `until ... do sleep 2; done` loops are unbounded, consistent with the plan spec. Owner can Ctrl-C if services fail to start.
4. **`curl -fs` behavior confirmed:** `-f` fails on HTTP errors (non-2xx), `-s` suppresses progress output. During startup the backend may transiently return 503 — the loop retries correctly. curl is available on macOS and standard Linux dev environments.
5. **Idempotency:** `docker compose up -d --build` is inherently idempotent. Seed runner's `>=6 clients` guard (8-5/8-8/8-9/8-10) prevents double-seeding on re-run.

## Review

**Verdict: APPROVED**

Checklist:
- [x] `demo` target injects `DRSHOES_DEMO_SEED_ENABLED=true` ✓
- [x] Waits for backend health on `:8080` via `curl -fs` loop ✓
- [x] Waits for web on `:3000` via `curl -fs` loop ✓
- [x] Calls `$(MAKE) demo-banner` ✓
- [x] `demo-banner` prints admin URL, login, password, Jaeger UI, MinIO with ANSI colors ✓
- [x] Both added to `.PHONY` declaration ✓
- [x] `make -n demo` dry-run exits 0 ✓
- [x] `make -n demo-banner` dry-run exits 0 ✓
- [x] No tests broken (pure tooling change) ✓
- [x] Commit message format correct with `[milestone:8][task:8-12]` and `Refs:` ✓

## Commit SHA

- Implementation: `8c3ec38`

## Follow-ups

- None. Pure Makefile addition, no carry-forward items.

## Subagent Token Budget

- Estimated total_tokens: ~8,000 (light read-heavy task, no code generation beyond Makefile snippet)
