---
task_id: 3-12
plan: docs/superpowers/plans/2026-05-09-milestone-03-photos.md
milestone: 3
agent: opus-inline
start_utc: 2026-05-09T10:33:00Z
end_utc: 2026-05-09T10:33:38Z
status: DONE
commit_sha: e646356
---

# Task 3-12 — KIND_LABELS_PL extension for PHOTO_* timeline kinds

## Files changed
- `apps/web/lib/timeline/types.ts` (modified, +3 union members; refreshed comment that claimed "9 values" / "10 curated kinds")
- `apps/web/app/(admin)/admin/orders/_components/OrderDrawerTimeline.tsx` (modified, +3 entries each in `KIND_ICONS` and `KIND_LABELS_PL`)

## Commands run
```
cd apps/web && pnpm typecheck   # exit 0, zero errors
```

## Test summary
- Typecheck only — no runtime tests for this task per plan.

## Decisions / deviations from plan
- Plan body's union literal showed only 8 kinds (`STATUS_CHANGED`, `ITEM_ADDED`, `ITEM_UPDATED`, `ITEM_REMOVED`, `MESSAGE_SENT` + 3 photo kinds). Real union has 10 pre-M3 kinds including `ORDER_CREATED`, `ORDER_UPDATED`, `ASSIGNEE_CHANGED`, `PICKUP_DATE_CHANGED`, `ITEM_EDITED` (not `ITEM_UPDATED`), `ORDER_SOFT_DELETED`. Appended the three PHOTO_* kinds to the existing union without removing prior members.
- Plan body's `KIND_LABELS_PL` snippet only listed the 3 new PHOTO entries to add — appended them to the existing 10-entry map.
- Added matching icon entries to `KIND_ICONS` (📷 / 🗑️ / 🏷️). Plan didn't specify icons but the `Record<TimelineEventKind, string>` type forces icon coverage.
- Deleted stale comment `"TimelineEventKind has 9 values..."` in `types.ts` since the union now has 13 kinds.
- Inlined per dispatch protocol rule 5 (trivial fixup, ~6 lines of edits across 2 files).

## Logging / metadata wired
- None — pure label/type extension.

## Concerns
- (none)

## Next action
- Move to task 3-13 (Wave 5 closure: smoke + milestone-3 tag + CLAUDE.md flip).
