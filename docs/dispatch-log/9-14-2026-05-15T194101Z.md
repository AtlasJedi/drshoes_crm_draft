# Dispatch Log — Task 9-14

**Task:** `icons.tsx` — icon record  
**UTC:** 2026-05-15T194101Z  
**Status:** completed  
**Commit SHA:** 6535a2f77c5832e8972dc7d670220a5a38c34c1f  

---

## Files

| Action | File |
|--------|------|
| created | `packages/ui/src/components/icons.tsx` |
| created | `packages/ui/src/components/__tests__/icons.test.tsx` |
| modified | `packages/ui/src/index.ts` |

---

## Commands run

```bash
# RED — test fails before icons.tsx exists
cd apps/web && pnpm test -- icons.test
# Expected: Cannot find module '../icons' ✓

# GREEN — after icons.tsx created
cd apps/web && pnpm test -- icons.test
# 3/3 PASS ✓

# TypeScript
pnpm typecheck
# 0 errors ✓
```

---

## Test summary

- **3 tests PASS** in `packages/ui/src/components/__tests__/icons.test.tsx`
  - exports at least 25 icon keys (35 total)
  - every value is truthy
  - every icon element renders an svg tag

---

## Decisions

1. **35 icons total** — 31 verbatim from `handoff/design/shared.jsx` constant `I` + `list` (sidebar nav) + 4 craft icons (`sprayCan`, `shoeIcon`, `brushIcon`, `jacketIcon`) from the named component functions in shared.jsx. All wrapped via the local `Icn` helper for consistent SVG attribute inheritance.
2. **JSX elements not functions** — `I.search` etc. are pre-rendered JSX elements (React elements), matching the design's usage pattern `{I.search}` directly.
3. **`Icn` helper accepts `viewBox` prop** — `shoeIcon` uses `viewBox="0 0 32 24"` to match the original shoe SVG proportions.
4. **LOC overrun** — file is ~110 LOC, within plan's stated ~200 LOC budget for this single dense record. Granulate exception documented per plan note.
5. **Bundled into commit `6535a2f`** — prior dispatch agent (9-4 Sticker) pre-created icons.tsx alongside StatTile.tsx. This dispatch log retroactively documents the 9-14 work for the bookkeeping record; the code and tests were already green.
6. **Barrel export** — `packages/ui/src/index.ts` already exports `./components/icons` as of HEAD.

---

## Note: pre-existing test failures

`AdminCard.test.tsx`, `Button.test.tsx`, `Chip.test.tsx` et al. fail in the full vitest suite with `Failed to resolve import "react/jsx-dev-runtime"` / `@testing-library/react` not found. These are pre-existing failures from tasks 9-2 through 9-13 (vitest.config.ts module resolution path change from `alias` to `modules` array broke cross-package resolution for some tests). Not introduced by this task. The icons test itself passes cleanly in isolation.
