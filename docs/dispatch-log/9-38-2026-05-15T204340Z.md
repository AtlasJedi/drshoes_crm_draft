# Dispatch Log — Task 9-38: `<NewsTeaser>` public landing

**Milestone:** 9 — Design Parity  
**Task:** 9-38  
**UTC:** 2026-05-15T204340Z  
**Status:** APPROVED

## Files Created

- `apps/web/app/(public)/_components/NewsTeaserCard.tsx` — Hero + sm card variant (69 LOC)
- `apps/web/app/(public)/_components/NewsTeaser.tsx` — Section component, static NEWS array (58 LOC)
- `apps/web/app/(public)/_components/__tests__/NewsTeaser.test.tsx` — 7 vitest/RTL tests

## Commands Run

```
cd apps/web && npx vitest run "NewsTeaser"
# 7 passed (7)
```

## Test Summary

- 7/7 passed
- Covers: tape label render, heading + "dzieje" span, 4 article tiles total, Stamp "świeże" on hero, hero title, CTA link, 3 small tile dates

## Decisions

- `NewsTeaserCard` extracted to its own file (hero/sm variant prop) to keep both files ≤ 80 LOC
- `Splatter` uses `"var(--pink)"` string (not a TapeColor) — Splatter.color is `string`, not union type, so this is correct
- Static `NEWS` array in `NewsTeaser.tsx`; real API deferred until Aktualności stub unlocks (out-of-scope per spec)
- Not wired into `page.tsx` — that is task 9-40's job per spec
- 2 pre-existing test failures (Services.test.tsx, SklepTeaser.test.tsx) from parallel wave tasks 9-36/9-39 not yet implemented — unrelated to this task

## Commit SHA

TBD (committed after dispatch log written)
