# Dispatch Log — email-template-v031-20260517T225936Z

## Task
V031: email footer typography v2 (heavier, acid-yellow button) across all 4 EMAIL templates
+ full structural rewrite of followup template to match Gotowe do odbioru visual richness.

## Files Changed

### New
- `backend/app/src/main/resources/db/migration/V031__email_template_footer_v2_and_followup_rewrite.sql`

### Modified
- `backend/app/src/test/java/com/drshoes/app/messaging/service/MessageRouterFollowupIntegrationTest.java`

### Screenshots (replaced)
- `docs/screenshots/email-footer-fix/zlecenie-przyjete.png`
- `docs/screenshots/email-footer-fix/gotowe-do-odbioru.png`
- `docs/screenshots/email-footer-fix/prosba-o-opinie.png`
- `docs/screenshots/email-footer-fix/Dr-Shoes-followup-EMAIL.png`

## Commands Run
```bash
mvn -f backend/pom.xml -pl app -am -DskipTests clean package
docker compose build backend && docker compose up -d backend
mvn -f backend/pom.xml -pl app test
```

## Migration Summary

### Change A — Footer v2 (3 non-followup EMAIL templates)
REPLACE of V030 footer block in Zlecenie przyjete, Gotowe do odbioru, Prosba o opinie:
- Workshop name: 16px → 20px Impact
- Address: weight 400 → 600
- Hours color: `#6b6960` → `#3a3833` (darker, more readable on cream)
- Map button: **inverted from black-bg/yellow-text to acid-yellow-bg/black-text** (`#d8ff3a` background)
- Top border: 1px solid #d8d2c0 → 2px solid #0a0a0a
- Idempotency guard: `NOT LIKE '%background:#d8ff3a;padding:0;%'`

### Change B — Followup full rewrite
Full `body_html` overwrite for `Dr Shoes - followup (EMAIL)`:
- Body now inside cream card (`#f4efe6`) with 16px/25px line-height, pre-wrap
- New in-body "Zobacz na mapie →" acid-yellow CTA (matches Gotowe do odbioru energy)
- New sign-off: "Pozdrawiamy, ekipa {nazwa_warsztatu}"
- Footer inside card structure (2px solid separator, not orphaned)
- Footer uses new v2 block (same as Change A)

## Test Summary
- Backend: **505/0/0/0** (run, fail, error, skip) — all GREEN
- New tests added to `MessageRouterFollowupIntegrationTest`:
  - Updated Test 1: pinned V031 footer styles (acid-yellow button, 20px Impact, addr weight 600, in-body CTA, sign-off, 2px border)
  - Added Test 1b (`sendNewToClient_email_followup_v031_body_composition`): verifies multi-line message with pre-wrap, in-body CTA, sign-off, footer structure

## Visual Check Results (Playwright screenshots)
| Template | Button | Workshop name | Footer position | Result |
|---|---|---|---|---|
| Zlecenie przyjete | Acid yellow ✓ | 20px Impact ✓ | Inside card ✓ | PASS |
| Gotowe do odbioru | Acid yellow ✓ | 20px Impact ✓ | Inside card ✓ | PASS |
| Prosba o opinie | Acid yellow ✓ | 20px Impact ✓ | Inside card ✓ | PASS |
| Followup | Acid yellow ✓ | 20px Impact ✓ | Inside card ✓ | PASS |
| Followup in-body CTA | "ZOBACZ NA MAPIE →" acid yellow ✓ | — | — | PASS |
| Followup sign-off | "Pozdrawiamy, ekipa Dr Shoes Poznań" ✓ | — | — | PASS |

## Decisions
- Used targeted REPLACE for Change A (keeps diffs minimal, leaves non-footer content untouched)
- Used full body_html overwrite for Change B (structural rewrite makes REPLACE impractical)
- `containsPattern` used for in-body CTA assertion (multi-line HTML makes exact contains unreliable for ordering)

## Commit SHAs
1. `94c18c7` — feat(db): V031 migration + dispatch log
2. `6547e7c` — test(messaging): V031 assertions + Test 1b
3. `3e54e66` — docs(email): refreshed screenshots
