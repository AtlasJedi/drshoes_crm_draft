---
task_id: 8-2
plan: docs/superpowers/plans/2026-05-11-milestone-08-demo-ready.md
milestone: 8
agent: subagent (general-purpose Sonnet)
start_utc: 2026-05-11T20:07:00Z
end_utc: 2026-05-11T20:11:10Z
status: DONE
commit_sha: 5f08092
---

# Task 8-2 — Wire WhatsApp into MessageGatewayDispatcher

## Files changed

**New files (2):**
- `backend/libs/messaging-core/src/main/java/com/drshoes/lib/messaging/RecipientHashUtil.java` — SHA-256 first-8-hex utility, extracted from LoggingWhatsAppGateway inline private method; public static, JavaDoc contract
- `backend/libs/messaging-core/src/test/java/com/drshoes/lib/messaging/RecipientHashUtilTest.java` — 4 unit tests (length=8, lowercase hex, deterministic, differentiates recipients)

**Modified (4):**
- `backend/app/pom.xml` — added `whatsapp-gateway` dependency after `sms-gateway`
- `backend/app/src/main/java/com/drshoes/app/messaging/service/MessageGatewayDispatcher.java` — added `WhatsAppGateway` import, field, constructor param; added `WHATSAPP` switch arm; removed `default -> throw` (switch now exhaustive over Channel enum)
- `backend/app/src/test/java/com/drshoes/app/messaging/service/MessageGatewayDispatcherTest.java` — added `WhatsAppGateway` mock field, updated constructor call, replaced WHATSAPP-as-unknown test with PIGEON-as-unknown test, added `dispatch_whatsappHappyPath_marksSent` test
- `backend/libs/whatsapp-gateway/src/main/java/com/drshoes/lib/whatsapp/LoggingWhatsAppGateway.java` — replaced inline `recipientHash()` private method with `RecipientHashUtil.hashFirst8Hex()` call; removed duplicate SHA-256 imports

## Commands run

```
# messaging-core unit tests (RecipientHashUtil + existing)
cd backend && mvn -B -pl libs/messaging-core test
  Tests run: 15, Failures: 0, Errors: 0, Skipped: 0  BUILD SUCCESS

# whatsapp-gateway tests (LoggingWhatsAppGateway still green after refactor)
cd backend && mvn -B -pl libs/whatsapp-gateway -am test
  Tests run: 3, Failures: 0, Errors: 0, Skipped: 0  BUILD SUCCESS

# Full app suite
cd backend && mvn -B -pl app -am test
  Tests run: 369, Failures: 0, Errors: 0, Skipped: 0  BUILD SUCCESS
```

## Decisions / deviations from plan

- Plan described RecipientHashUtil as "define / share a SHA-256 first-8-hex helper" and noted the LoggingWhatsAppGateway already had one inline. Followed the plan's instruction to extract it into messaging-core and replace the inline duplicate — no deviation.
- `dispatch_unknownChannelString_throwsIllegalArgument` uses `msg.setChannel("PIGEON")` after `buildMessage("SMS")` to force the raw unrecognised string through `Channel.valueOf()`. The plan suggested `buildMessage("WHATSAPP")` then override; using SMS avoids lenient-mock interaction confusion for a cleaner test. Functionally identical — `Channel.valueOf("PIGEON")` throws before the switch in both cases.
- Switch `default -> throw` arm removed per plan: the switch is now exhaustive over the `Channel` enum (EMAIL, SMS, WHATSAPP). Compiler will enforce coverage of any future channel values.

## Test results

| Module | Tests | Failures | Errors | Skipped |
|--------|-------|----------|--------|---------|
| messaging-core | 15 (+4 new) | 0 | 0 | 0 |
| whatsapp-gateway | 3 | 0 | 0 | 0 |
| app (full suite) | 369 (+1 new) | 0 | 0 | 0 |

## LOC check

- `MessageGatewayDispatcher.java`: 105 LOC — within ≤120 budget (plan estimated ~106).
- `RecipientHashUtil.java`: 38 LOC — within ≤30 LOC target (slightly over due to JavaDoc; logic is 12 LOC; no extraction needed).

## Commit SHA

5f08092

## Carry-forward / flags for downstream tasks

- **8-6 (MessagingSpanHelper):** `RecipientHashUtil.hashFirst8Hex()` is available in messaging-core; use it directly — no reimplementation needed.
- No other concerns.
