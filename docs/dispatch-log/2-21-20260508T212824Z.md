# Dispatch log — 2-21: milestone-2 closure

**UTC:** 20260508T212824Z
**Task:** 2-21 — E2E compose smoke + milestone-2 tag + CLAUDE.md flip
**Status:** completed (smoke skipped per owner directive)

## Files modified

- `CLAUDE.md` — flipped Milestone 2 to `[x]`, added M3 placeholder line.
- Local annotated tag `milestone-2` created (NOT pushed).
- `docs/dispatch-log/2-21-20260508T212824Z.md` — this file.
- `docs/dispatch-log/tasks.json` — 2-21 marked completed.

## Commands

- `git add CLAUDE.md && git commit -m "docs: mark Milestone 2 complete [milestone:2][task:2-21] ..."` → SHA `2ad7b9a`.
- `git tag -a milestone-2 -m "..."` → tag created locally.

## Decisions

- **Smoke skipped (owner directive).** Relying on the 150/0/0/0 unit suite (controllers + services + integration tests covering the full message pipeline + scheduled job + timeline + idempotency + audit). Plan step 2 (boot stack + curl assertions) bypassed; plan steps 4–7 (tag, doc flip, bookkeeping) executed.
- **Tag message annotated** to record that smoke was skipped, so the audit trail shows the deviation.

## Suite

149 → **150/0/0/0** (delta from 2-20's HealthControllerTest). No additional backend changes in 2-21.

## Commits

- `2ad7b9a` — `docs: mark Milestone 2 complete [milestone:2][task:2-21]`
- (bookkeeping commit pending — tasks.json + this log)

## Milestone 2 — final summary

**21 tasks shipped across 5 waves.**

| Wave | Tasks | Notes |
|---|---|---|
| 0 Bookkeeping | 2-1 | tasks.json activation |
| 1 Backend domain | 2-2..2-6 | V006 migration, entities, idempotency, renderer, thread service |
| 2 Send pipeline | 2-7..2-10 | MessageRouter (two-stage), TriggerEngine, ScheduledTriggerJob (V007/V008 schema correction), MessageSentTimelineHandler |
| 3 Controllers | 2-11..2-13 | Templates CRUD, Triggers list+toggle, Messages thread+send |
| 4 Frontend | 2-14..2-19 | lib/messaging, /admin/templates, /admin/triggers, OrderDrawerMessages, MessageComposerModal, real trigger preview |
| 5 Closure | 2-20..2-21 | /api/health, errata bullet 13, milestone-2 tag, CLAUDE.md flip |

**Suite at close:** 150/0/0/0 (zero failures, zero errors, zero skipped).

**M3 debt captured (consolidated):**
1. Actor resolution — `MessagesController` passes `actorId=null`; needs `CurrentUserResolver` via `@AuthenticationPrincipal`.
2. Real providers — Postmark + SMSAPI behind existing gateway interfaces; webhook receivers; WireMock contract tests.
3. Inbound messages — `direction=INBOUND` parsing, top-nav Wiadomości view.
4. Trigger create/edit form — dynamic `event_params` editor.
5. Photos (M3 priority) — R2/MinIO backed, with `{link_do_zdjec}` placeholder for templates.
6. Manual-confirmation queue — surfaces triggers where `requires_manual_confirmation=true`.
7. Calendar/kanban view — deferred from M2 scope.
