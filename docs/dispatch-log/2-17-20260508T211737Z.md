# Dispatch Log: 2-17 — OrderDrawerMessages + MESSAGE_SENT timeline label

**Task:** 2-17
**UTC:** 20260508T211737Z
**Commit SHA:** 69b7d24

## Files

| Action | Path |
|--------|------|
| CREATE | `apps/web/app/(admin)/admin/orders/_components/MessageRow.tsx` |
| CREATE | `apps/web/app/(admin)/admin/orders/_components/OrderDrawerMessages.tsx` |
| MODIFY | `apps/web/app/(admin)/admin/orders/_components/OrderDrawer.tsx` |
| MODIFY | `apps/web/app/(admin)/admin/orders/_components/OrderDrawerTimeline.tsx` |
| MODIFY | `apps/web/lib/timeline/types.ts` |

## Commands

```
pnpm typecheck  → clean (0 errors)
pnpm build      → green (all 11 routes compiled)
```

## Test summary

Suite: 149/0/0/0 (backend unchanged; frontend typecheck + build green).

## Decisions

- `TimelineEventKind` in `lib/timeline/types.ts` extended with `MESSAGE_SENT`
  (backend Java enum already has this value from 2-13; TS mirror was behind).
- `KIND_LABELS_PL` in `OrderDrawerTimeline.tsx` uses a plain string
  `"Wysłano wiadomość"` — the plan's function-form `(labels) => ...` was
  incompatible with the existing `Record<TimelineEventKind, string>` shape;
  matched reality.
- `composeOpen` state added to `OrderDrawer` as a stub for 2-18's modal wire-up.
  TypeScript compiles clean (state setter passed to child, no unused-var error).
- Log calls follow message-first pattern: `log.info("op=load outcome=ok", { ... })`.

## Notes

No scope creep. Composer modal is task 2-18.
