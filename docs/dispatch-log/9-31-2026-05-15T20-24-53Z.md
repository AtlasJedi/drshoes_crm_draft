---
task_id: 9-31
plan: docs/superpowers/plans/2026-05-15-milestone-09-design-parity.md
milestone: 9
agent: sonnet
start_utc: 2026-05-15T20:24:53Z
end_utc: 2026-05-15T20:30:00Z
status: DONE
commit_sha: baed648
---

# Task 9-31 — Triggers `<TriggerEditPanel>` NEW + sticky edit-in-place

## Files changed
- `apps/web/app/(admin)/admin/triggers/page.tsx` (modified, 4 LOC): converted from server component to client shell wrapper
- `apps/web/app/(admin)/admin/triggers/_components/TriggerCard.tsx` (new, 73 LOC): AdminCard with left-border pink/blue, opacity-55 if inactive, 38px zap icon, name + manual Chip, mono meta line, stats row, Toggle + edytuj button
- `apps/web/app/(admin)/admin/triggers/_components/TriggerEditPanel.tsx` (new, 120 LOC): sticky-top-20 panel, Tape header, t-display-26, form with name/event/delay/channels/textarea, clickable placeholder chips with cursor restore, manual-confirm toggle in dashed paper-2 box, save + test-to-self stub
- `apps/web/app/(admin)/admin/triggers/_components/useTriggerEditForm.ts` (new, 79 LOC): state hook; insertPlaceholder uses textareaRef.selectionStart + requestAnimationFrame for jsdom-friendly cursor restore
- `apps/web/app/(admin)/admin/triggers/_components/TriggerListShell.tsx` (new, 79 LOC): 1.4fr/1fr grid, filter chips (aktywne/wyłączone/do potwierdzenia), biblioteka szablonów link, usePageHeader wired with "+ Nowy trigger" right button, editing state drives right panel
- `apps/web/app/(admin)/admin/triggers/__tests__/TriggerCard.test.tsx` (new, 47 LOC): 4 tests
- `apps/web/app/(admin)/admin/triggers/__tests__/TriggerEditPanel.test.tsx` (new, 63 LOC): 6 tests

## Commands run
```
pnpm --filter web test -- --run triggers   # exit 0, 10/10 passed
```

## Test summary
- Tests run: 10, Failures: 0, Errors: 0, Skipped: 0
- TriggerCard: 4 tests (name, manual chip, onEdit callback, disabled opacity)
- TriggerEditPanel: 6 tests (name heading, tape header, placeholder chips render, chip insert, api.patch on save, close callback)

## Decisions / deviations from plan
- TriggerEditPanel is 120 LOC (plan target ≤80). Split not done inline; panel is highly cohesive JSX — a form split would reduce readability. Flagged as LOC overrun per dispatch protocol but no functional concern.
- page.tsx old server component (getTriggersServer) replaced entirely with thin client shell import — data fetching moved to TriggerListShell useEffect via client-side getTriggers().
- activeCt filter excludes requiresManualConfirmation triggers from "active" count for cleaner UX separation.
- "biblioteka szablonów →" rendered as Next.js Link (not Button href) since Button variant="paper" with href doesn't match visual spec exactly.

## Logging / metadata wired
- `TriggerCard.tsx` logs `op=toggleTrigger` at INFO with id + enabled outcome
- `TriggerEditPanel.tsx` logs `op=TriggerEditPanel.render` at DEBUG with triggerId
- `useTriggerEditForm.ts` logs `op=saveTrigger outcome=ok/error` at INFO/ERROR with triggerId
- `TriggerListShell.tsx` logs `op=getTriggers outcome=ok/error` at INFO/ERROR with count

## Concerns
- TriggerEditPanel.tsx is 120 LOC (limit is 80). Acceptable for a form panel per the "granulate exception" precedent from icons.tsx.
- updateTrigger() API doesn't exist on backend yet — PATCH /admin/triggers/:id is wired but backend may return 404/405. This is a frontend-only task per plan scope.

## Next action
- Move to task 9-32 (Templates editor parity) per plan.
