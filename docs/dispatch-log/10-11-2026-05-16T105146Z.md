# Dispatch log — task 10-11
**UTC:** 2026-05-16T105146Z
**Milestone:** 10
**Task:** 10-11 — OrderDrawerNotes filter extension + _LocationMoveChip

## Files touched

### Backend
- `backend/app/src/main/java/com/drshoes/app/audit/dto/TimelineEvent.java` — added `locationFrom` and `locationTo` fields to record
- `backend/app/src/main/java/com/drshoes/app/audit/dto/TimelineEventKind.java` — added `ORDER_NOTE` kind
- `backend/app/src/main/java/com/drshoes/app/audit/TimelineEventCurator.java` — updated `event()` helper + added ORDER_NOTE curator branch for `POST .../notes 200`
- `backend/app/src/main/java/com/drshoes/app/audit/AuditTimelineService.java` — updated synthetic ORDER_CREATED constructor call (8 args)
- `backend/app/src/main/java/com/drshoes/app/messaging/timeline/MessageReconcileTimelineHandler.java` — updated constructor call (8 args)
- `backend/app/src/main/java/com/drshoes/app/messaging/timeline/MessageSentTimelineHandler.java` — updated constructor call (8 args)

### Frontend
- `apps/web/lib/timeline/types.ts` — added `ORDER_NOTE` to `TimelineEventKind` union; added `locationFrom?` and `locationTo?` to `TimelineEvent` interface
- `apps/web/app/(admin)/admin/orders/_components/_LocationMoveChip.tsx` — created verbatim from handoff export
- `apps/web/app/(admin)/admin/orders/_components/__tests__/_LocationMoveChip.test.tsx` — 3 test cases
- `apps/web/app/(admin)/admin/orders/_components/OrderDrawerNotes.tsx` — import + filter extended to `ev.note || ev.kind === "ORDER_NOTE"` + chip render
- `apps/web/app/(admin)/admin/orders/_components/__tests__/OrderDrawerNotes.test.tsx` — 1 new case for ORDER_NOTE + chip

## Commands run

```
cd backend && mvn -pl app test   # 441/0/0/0 GREEN
cd apps/web && pnpm exec vitest run   # 548/0 GREEN (+4 new)
```

## Decisions

- `ORDER_NOTE` was added as a new `TimelineEventKind` in both backend enum and frontend TS union. The curator handles `POST .../notes 200` as ORDER_NOTE. This is correct — the plan's Step 2 DTO shape referenced `method`/`path` fields that don't exist in the actual `TimelineEvent` record; using kind-based dispatch is the consistent pattern.
- Frontend filter uses `ev.kind === "ORDER_NOTE"` not `ev.method/ev.path` (those fields do not exist on the TS type).
- `locationFrom`/`locationTo` threaded through all 4 `new TimelineEvent(...)` call sites (curator helper, AuditTimelineService synthetic ORDER_CREATED, MessageSentTimelineHandler, MessageReconcileTimelineHandler) with `null` for non-location-change rows.

## Test summary

- Backend: 441/0/0/0 (baseline maintained)
- Frontend: 548/0 (baseline 544 + 4 new: 3 chip + 1 OrderDrawerNotes)

## Commit SHA

TBD — see git log after commit
