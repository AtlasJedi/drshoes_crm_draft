# Dispatch Log: Task 5-8 — MessageThreadService read/mutation split

**Task:** 5-8
**Milestone:** 5
**UTC timestamp:** 2026-05-10T01:12:36Z
**Dispatcher:** main-session subagent (general-purpose Sonnet)

---

## Files Changed

| File | Change |
|------|--------|
| `backend/app/src/main/java/com/drshoes/app/messaging/service/MessageThreadService.java` | Rewritten as read-side only (~55 LOC): one-arg back-compat overload + channel-aware findOrCreateForClient + findOrCreateForRawSender. Now uses new `findFirstByClientIdAndChannelOrderByCreatedAtAsc` and `findFirstByRawSenderAndChannelOrderByCreatedAtAsc` repo methods. |
| `backend/app/src/main/java/com/drshoes/app/messaging/service/MessageThreadMutationService.java` | Created — write-side (~77 LOC): markRead, assignUnmatched, discardUnmatched — all @Audited(parent="#threadId") with structured INFO logging on each. |
| `backend/app/src/main/java/com/drshoes/app/messaging/repository/MessageThreadRepository.java` | Added `findFirstByClientIdAndChannelOrderByCreatedAtAsc` + `findFirstByRawSenderAndChannelOrderByCreatedAtAsc` (plan Step 2). |
| `backend/app/src/main/java/com/drshoes/app/messaging/repository/MessageRepository.java` | Added `bulkUpdateClientIdByThreadId` @Modifying @Query (plan Step 3). |
| `backend/app/src/test/java/com/drshoes/app/messaging/service/MessageThreadServiceTest.java` | Created — 10 unit tests (RED → GREEN): 2 findOrCreateForClient, 2 findOrCreateForRawSender, 2 markRead, 2 assignUnmatched, 2 discardUnmatched. |
| `backend/app/src/main/java/com/drshoes/app/audit/TimelineEventCurator.java` | Updated 3 Pattern constants: `MessageThreadService#...` → `MessageThreadMutationService#...`. Removed TODO(5-8) comment and replaced with single clarifying comment. |
| `backend/app/src/main/java/com/drshoes/app/audit/dto/TimelineEventKind.java` | Updated comments on THREAD_MARKED_READ/THREAD_ASSIGNED/THREAD_DISCARDED to reference MessageThreadMutationService. |
| `backend/app/src/test/java/com/drshoes/app/audit/TimelineEventCuratorM5Test.java` | Updated class-level NOTE docstring + 3 path strings (lines 73, 83, 93): `MessageThreadService#...` → `MessageThreadMutationService#...`. |
| `docs/dispatch-log/tasks.json` | 5-8 entry set to status=done, commit_sha filled, dispatch_log path filled. |

---

## Commands Run

```bash
# RED phase — compilation errors confirming missing class + methods
mvn -B -f backend/pom.xml -pl app -am test -Dtest=MessageThreadServiceTest -Dsurefire.failIfNoSpecifiedTests=false
# Result: COMPILATION ERROR — MessageThreadMutationService not found, repo methods not found

# GREEN phase — after all implementation
mvn -B -f backend/pom.xml -pl app -am test -Dtest=MessageThreadServiceTest -Dsurefire.failIfNoSpecifiedTests=false
# Result: Tests run: 10, Failures: 0, Errors: 0, Skipped: 0 — BUILD SUCCESS

# Full backend suite
mvn -B -f backend/pom.xml verify
# Result: Tests run: 271, Failures: 0, Errors: 0, Skipped: 0 — BUILD SUCCESS
```

---

## Test Summary

| Suite | Before | After | Delta |
|-------|--------|-------|-------|
| Backend (mvn verify) | 261/0/0/0 | 271/0/0/0 | +10 |

New tests: `MessageThreadServiceTest` — 10 cases:
- `findOrCreateForClient_returnsExisting`
- `findOrCreateForClient_createsNew`
- `findOrCreateForRawSender_returnsExisting`
- `findOrCreateForRawSender_createsNew`
- `markRead_setsZero`
- `markRead_notFound`
- `assignUnmatched_happy`
- `assignUnmatched_alreadyAssigned`
- `discardUnmatched_happy`
- `discardUnmatched_alreadyDiscarded`

---

## Decisions

### Service split at LOC cap
`MessageThreadService` was ~35 LOC. Adding ~120 LOC of mutation methods would exceed the 120-LOC cap. Split per plan: read-side stays in `MessageThreadService`, write-side goes into new `MessageThreadMutationService`. Both stay in `com.drshoes.app.messaging.service`.

### Back-compat overload retained
`MessageThreadService.findOrCreateForClient(UUID)` retained as a one-arg back-compat overload delegating to `findOrCreateForClient(UUID, "EMAIL")`. Verified callers: `MessageRouter` calls the one-arg form at lines 104 and 146 — covered. `InboundMessageService` calls the two-arg form directly — covered. No call-site changes required.

### Repository finder method choice
The plan specifies `findFirstByClientIdAndChannelOrderByCreatedAtAsc` and `findFirstByRawSenderAndChannelOrderByCreatedAtAsc` — simpler variants without the discard filter. These coexist with the existing filtered variants used by InboundMessageService. The mutation-service tests mock these simpler methods (matching the plan spec). The existing `findFirstByClientIdAndChannelAndDiscardedAtIsNullOrderByLastMessageAtDesc` is still used by InboundMessageService (unchanged).

### Cross-task integration — curator patterns updated atomically
Per the task instructions, the curator `Pattern` constants, `TimelineEventKind` comments, and `TimelineEventCuratorM5Test` path strings were all updated in the same commit as the service split. This ensures the audit row path `MessageThreadMutationService#markRead` (etc.) matches the curator patterns from the moment the split ships — no window of silent timeline kind loss.

### AuditLogAspect uses `SimpleClassName#methodName` format
Verified at `AuditLogAspect.java:112`: `method.getDeclaringClass().getSimpleName() + "#" + method.getName()`. New paths will be `MessageThreadMutationService#markRead`, `MessageThreadMutationService#assignUnmatched`, `MessageThreadMutationService#discardUnmatched` — matching the updated curator patterns.

---

## Concerns / Follow-ups

1. **`@Audited(parent="#threadId")` parameter-name SpEL not exercised by unit tests.** Unit tests call service methods directly (no Spring context, no AOP proxy). The `#threadId` SpEL binding is verified at the code level: `AuditedParentResolver.buildContext()` uses `Parameter#getName()` to bind args by name, and the project's root `pom.xml` maven-compiler-plugin sets `<parameters>true</parameters>` — so JVM retains parameter names. However, the binding is not proven end-to-end until task 5-9 (`ThreadController` IT) exercises these methods through the Spring container with the aspect in the chain.

2. **`TimelineEventCurator` LOC debt (carried from 5-7).** At ~240 non-blank LOC — still over the 120-line cap. The TODO(5-8) comment about `CuratorInternalDispatcher` extraction has been removed from the curator (it was tied to the class-name rename, now done). The LOC debt itself is deferred to 5-21 closure hygiene or a dedicated hygiene task, as it does not affect correctness.

3. **NIT-A from 5-7 review** — `TimelineEventCuratorM5Test.auditLog()` helper omits `setId()`. Benign (TimelineEvent.id is null-safe). Carried into 5-21 hygiene.

---

## Self-Review Against Plan Checklist

- [x] Pre-step: AuditLogAspect aspect parameter resolution verified — `<parameters>true</parameters>` in root pom. `AuditedParentResolver` uses `Parameter#getName()`. `#threadId` will resolve.
- [x] Step 1 (RED): `MessageThreadServiceTest.java` written, confirmed COMPILATION ERROR before implementation.
- [x] Step 2: `MessageThreadRepository` extended with `findFirstByClientIdAndChannelOrderByCreatedAtAsc` + `findFirstByRawSenderAndChannelOrderByCreatedAtAsc`.
- [x] Step 3: `MessageRepository` extended with `bulkUpdateClientIdByThreadId` @Modifying @Query.
- [x] Step 4: `MessageThreadService` rewritten as read-side (~55 LOC, under cap). Back-compat one-arg overload retained.
- [x] Step 5: `MessageThreadMutationService` created (~77 LOC, under cap). Three @Audited methods with structured INFO logging.
- [x] Step 6 (GREEN): 10 unit tests passing. Full suite 271/0/0/0.
- [x] Cross-task integration (CRITICAL): curator patterns updated, enum comments updated, M5 test path strings updated — all in same commit.
- [x] Audit class-name format verified: `SimpleClassName#methodName` → `MessageThreadMutationService#...`.
- [x] All callers of `MessageThreadService` confirmed compile-safe (back-compat overload + unchanged two-arg callers).
- [x] Java LOC: MessageThreadService ~55, MessageThreadMutationService ~77 — both under 120-line cap.
- [x] No frontend changes needed for 5-8.
- [x] Conventional Commits title with [milestone:5][task:5-8] tag and Refs trailer.
- [x] No Co-Authored-By trailer.
- [x] No push to origin.

## Review verdict (combined single-stage, 2026-05-10)

**APPROVED** — spec-compliant, no NITs.

Reviewer verified:
- Cross-task integration: curator Patterns + enum comments + M5 test path strings all updated to `MessageThreadMutationService` in same commit. TODO(5-8) markers gone.
- AOP parameter resolution backed by actual code: `AuditedParentResolver.java:82` uses `Parameter#getName()`; `backend/pom.xml:72` sets `<parameters>true</parameters>` in `maven-compiler-plugin`. SpEL `#threadId` will resolve at runtime; end-to-end proof arrives in 5-9 IT.
- All call sites compile: `MessageRouter` uses one-arg back-compat overload; `InboundMessageService` uses two-arg signatures. No regression.
- `assignUnmatched` ordering correct (bulk update before save, both flushed at commit).
- V012 backfill of `channel='EMAIL'` makes the new channel-filtered finder non-breaking for pre-M5 threads.
- 10 unit tests, plan-mandated coverage, no scope creep.

Carry-forwards (out of scope for 5-8): 5-6 NITs (allowlist trim, IT asymmetry), 5-7 NITs (test helper setId, pnpm build skip).
