---
task_id: 5-13
plan: docs/superpowers/plans/2026-05-09-milestone-05-inbound-replies.md
milestone: 5
agent: opus-main-session-inline
start_utc: 2026-05-10T09:55:00Z
end_utc: 2026-05-10T09:56:00Z
status: APPROVED
reviewed_commit_sha: dac09c2
---

# 5-13 Review — APPROVED

Inline review by main session. Surface: ~150 LOC of TSX/TS UI translation from design pack `handoff/design/m5-messages/SidebarItem.jsx`. No business logic, no auth surface.

## Spec compliance
- `MessagesNavItem.tsx` matches plan §5068–5119 (icon, label "Wiadomości", red badge, 99+ cap, active-state styling).
- `useUnreadCount.ts` matches plan §5126+: `POLL_MS = 30_000`, `listThreads("UNREAD")`, sum of `unreadCount`, `messaging.unread` logger.
- AdminSidebar integration: `<MessagesNavItem />` rendered after Klienci stub.
- OrderDrawerMessages NIT fixups applied (404/409 distinct Polish error strings; orderId race-cancel guard).

## Race-cancel + lifecycle (critical for polling hook)
Verified `useUnreadCount` correctness:
- `let cancelled = false;` set on each effect run ✓
- `if (!cancelled)` gate before every `setCount` / log call ✓
- Cleanup function sets `cancelled = true;` AND `clearInterval(id);` ✓
- `void fetch()` immediate kick-off + interval — no double-fire on mount ✓
- Errors logged at `warn`, success at `debug` — appropriate levels ✓

## Deviations evaluated
1. **`Route` cast for typed routes** — required by Next.js 16 `experimental.typedRoutes`-style enforcement. Same pattern as elsewhere in the project. Accept.
2. **Sidebar anchor on Klienci stub** — pragmatic; real Klienci link doesn't exist yet. When a real link replaces the stub, the anchor still holds (insert position is logically "after Klienci, before Ustawienia"). Accept.

## Code quality findings
- All modules well under 80 LOC (47 LOC for `useUnreadCount`, 38 LOC for `MessagesNavItem`).
- Structured logging present with `key=value` named fields.
- No comments / no rot.
- **Minor (non-blocking):** `useUnreadCount` defines a local `async function fetch()` which shadows the global `fetch`. Cosmetic; no functional impact. Could be renamed `poll` in a future cleanup.

## Action items
None.

## Concerns
None new.
