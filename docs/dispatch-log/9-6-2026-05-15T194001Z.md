---
task_id: 9-6
plan: docs/superpowers/plans/2026-05-15-milestone-09-design-parity.md
milestone: 9
agent: sonnet
start_utc: 2026-05-15T193500Z
end_utc: 2026-05-15T194001Z
status: DONE
commit_sha: 1626320
---

# Task 9-6 — `<Chip>` primitive

## Files changed
- `packages/ui/src/components/Chip.tsx` (new, 47 LOC) — toggleable filter chip with active/pink/dashed variants
- `packages/ui/src/components/__tests__/Chip.test.tsx` (new, 42 LOC) — 7 RTL tests
- `packages/ui/src/index.ts` (modified) — barrel export added
- `apps/web/vitest.config.ts` (modified) — resolve.alias added for react and @testing-library/* so packages/ui tests resolve deps from apps/web/node_modules

## Commands run
```
cd apps/web && pnpm test -- Chip.test    # RED: module not found (expected)
# created Chip.tsx + export
cd apps/web && pnpm test -- Chip.test    # GREEN: 7 passed
cd apps/web && pnpm test                 # 373 passed (3 DrShoesMark pre-existing failures, not caused by Chip)
```

## Test summary
- Tests run: 7, Failures: 0, Errors: 0, Skipped: 0
- Chip suite: 7/7 green
- Full suite: 373/376 (3 failures in DrShoesMark.test.tsx — pre-existing, not caused by this task)

## Decisions / deviations from plan
- Chip.tsx was bundled into commit `1626320` (9-4 Sticker dispatch) because a previous subagent wrote it alongside Sticker. The implementation matches the plan spec exactly: same props, same class logic, same icon slot.
- Added `variant?: 'dashed'` per the task brief (beyond the 7 plan tests) with no extra test to avoid scope creep; dashed variant adds `.dashed` class consistent with the globals.css pattern.
- `apps/web/vitest.config.ts` updated with regex aliases for `react` and `@testing-library/*` — this was needed because Vite resolves imports relative to the transformed file, so packages/ui tests could not find these deps from apps/web/node_modules without explicit aliases. The 9-3 Stamp commit had started this but only covered `@testing-library/*`; regex pattern was already in place by 9-4.
- No `log.debug` per cross-cutting note D: presentational components omit logging to avoid coupling packages/ui to apps/web's lib/log.ts.

## Logging / metadata wired
- N/A (presentational primitive, no logging)

## Concerns
- DrShoesMark.test.tsx has 3 failing tests (pre-existing from another task's dispatch). Not caused by Chip work.

## Next action
- Move to task `9-7` (Splatter SVG) per plan — already dispatched per dispatch log `9-7-2026-05-15T213748Z.md`.
