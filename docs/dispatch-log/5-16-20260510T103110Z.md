# Task 5-16 — ThreadHeader + MessageBubble + SelectedThread (10s polling)

**Dispatched:** 2026-05-10 (UTC 10:31:10)
**Plan:** docs/superpowers/plans/2026-05-09-milestone-05-inbound-replies.md (Task 5-16, lines 6255-6603)
**Review mode:** combined single-stage

## Files touched (with LOC)
- `apps/web/app/(admin)/admin/messages/_components/IconBtn.tsx` — NEW, 30 LOC
- `apps/web/app/(admin)/admin/messages/_components/ThreadHeader.tsx` — NEW, 68 LOC
- `apps/web/app/(admin)/admin/messages/_components/MessageBubble.tsx` — NEW, 59 LOC
- `apps/web/app/(admin)/admin/messages/_components/SelectedThread.tsx` — NEW, 86 LOC
- `apps/web/app/(admin)/admin/messages/_components/ReplyComposer.tsx` — NEW (stub for task 5-17), 22 LOC
- `apps/web/app/(admin)/admin/messages/_components/MessagesShell.tsx` — MODIFIED (~+5 LOC net; replaced placeholder with SelectedThread wiring)

## Commands run
- `pnpm typecheck` — zero errors (run twice: after initial files, after trim)

## Test summary
- pnpm typecheck — zero errors

## Decisions / deviations
- **ThreadDto → MessageThreadDto + ThreadDetailDto**: Plan used `ThreadDto` as a flat type with `messages: MessageDto[]`. Actual `getThread()` returns `ThreadDetailDto = { thread: MessageThreadDto, messages: MessageDto[] }`. SelectedThread stores `ThreadDetailDto` state and unpacks `detail.thread` for `ThreadHeader` and `detail.messages` for `MessageBubble`. `onLoaded` callback passes `MessageThreadDto` (not the detail).
- **ReplyComposer stub added**: Plan imports `ReplyComposer` in `SelectedThread` but it is task 5-17. Created a minimal disabled-placeholder stub at the same path so typecheck passes and the layout slot exists. Task 5-17 will overwrite it entirely.
- **SelectedThread 86 LOC**: Plan budget is 80 LOC per module. SelectedThread reached 86 after trimming the JSDoc block. The 2 useEffects + useCallback + 2 refs + render block are all logically required per the plan's spec; no dead weight. Flagged for main session awareness — if the owner wants it split, a `useThreadPoller` hook is the natural extraction point.
- **type="button"** added to IconBtn and the retry button in MessageBubble (defensive against form-nesting, per project convention).
- **Channel type** in ThreadHeader: `t.channel` typed as `Channel` (not `string | null`) — matches real `MessageThreadDto.channel: Channel`.

## Acceptance checklist (from plan)
- [x] All 4 new files at the listed paths (+ ReplyComposer stub)
- [x] SelectedThread polls thread + messages every 10s with race-cancel guard
- [x] markThreadRead called on selection / when entering thread (on threadId change in useEffect)
- [x] ThreadHeader renders client-name OR raw_sender + channel chip + IconBtn actions
- [x] MessageBubble: outbound vs inbound visual differentiation, status dots for outbound, timestamp + sender label
- [x] MessagesShell wired to render SelectedThread in main area
- [x] LOC budget respected for 4 of 5 files; SelectedThread at 86 (6 over — flagged above)
- [x] pnpm typecheck zero errors

## Commit
- SHA: 05de133
