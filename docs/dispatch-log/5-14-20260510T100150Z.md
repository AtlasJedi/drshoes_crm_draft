# Task 5-14 — UnreadElsewhereBanner + GET /orders/{id}/unread-elsewhere

**Dispatched:** 2026-05-10 (UTC 10:01:50)
**Plan:** docs/superpowers/plans/2026-05-09-milestone-05-inbound-replies.md (Task 5-14, lines 5324-5735)
**Review mode:** combined single-stage

## Files touched
- `backend/app/src/main/java/com/drshoes/app/messaging/dto/UnreadElsewhereDto.java` — new record DTO (count, threadId)
- `backend/app/src/main/java/com/drshoes/app/messaging/api/OrderUnreadElsewhereController.java` — new GET /api/admin/orders/{orderId}/unread-elsewhere controller (~65 LOC)
- `backend/app/src/main/java/com/drshoes/app/messaging/repository/MessageThreadRepository.java` — added `findAllByClientIdAndUnreadCountGreaterThan` finder
- `backend/app/src/test/java/com/drshoes/app/messaging/api/OrderUnreadElsewhereControllerIntegrationTest.java` — new 3-case IT (RED then GREEN)
- `apps/web/app/(admin)/admin/orders/_components/OrderDrawer/UnreadElsewhereBanner.tsx` — new amber banner component (~34 LOC, Polish pluralization, Link to /admin/messages?thread=)
- `apps/web/app/(admin)/admin/orders/_components/OrderDrawerMessages.tsx` — added unreadElsewhere state + useEffect + banner render
- `apps/web/lib/messaging/api.ts` — added UnreadElsewhereDto interface + getUnreadElsewhere function

## Commands run
- `mvn -B -pl app -am test -Dtest=OrderUnreadElsewhereControllerIntegrationTest -Dsurefire.failIfNoSpecifiedTests=false` → RED (3 failures, all 404 — no controller yet)
- `mvn -B -pl app -am test -Dtest=OrderUnreadElsewhereControllerIntegrationTest -Dsurefire.failIfNoSpecifiedTests=false` → GREEN (3/3 passing)
- `mvn -B -pl app -am test` → 302 tests, 0 failures, 1 pre-existing error in MessagesControllerIntegrationTest (FK ordering issue in AdminWebTestBase.seedUsers — confirmed pre-existing by stash check)
- `pnpm typecheck` → zero errors (fixed Next.js 16 typed-routes issue: added `as Route` cast + `import type { Route } from "next"`)

## Test summary
- Backend: 3 new tests, all passing. Full app suite: 302 tests, 0 failures (1 pre-existing error unrelated to this task — was 299+3=302)
- Frontend: pnpm typecheck — zero errors

## Decisions / deviations
- Plan's IT used `OrderType.NAPRAWA` which doesn't exist in the codebase (Order entity has no orderType field). Removed that line — deviation justified by domain reality.
- Plan's `owner()` helper used `SecurityMockMvcRequestPostProcessors.user(...).roles(...)` which would inject a plain `UserDetails` not `AdminPrincipal`, causing NPE on `actor.email()`. Replaced with `authentication(UsernamePasswordAuthenticationToken.authenticated(new AdminPrincipal(...), ...))` pattern — same as ThreadControllerIntegrationTest.
- `UnreadElsewhereBanner.tsx` required `import type { Route } from "next"` and `as Route` cast on the href template literal — Next.js 16 typed routes requirement not reflected in plan literal.

## Acceptance checklist (from plan)
- [x] GET /api/admin/orders/{orderId}/unread-elsewhere returns {count, threadId} for unread-elsewhere; {count:0, threadId:null} when none — 3 IT cases GREEN
- [x] UnreadElsewhereBanner amber border, Polish plural (1/2-4/5+), Link to /admin/messages?thread=<id>
- [x] Banner renders when count>0, hidden at 0
- [x] Race-cancel guard in useEffect
- [x] pnpm typecheck green
- [x] Suite delta +3 (299 → 302)

## Commit
- SHA: 8d10fea823d8af2737a1b53cd58b0cec9a02ec7a
