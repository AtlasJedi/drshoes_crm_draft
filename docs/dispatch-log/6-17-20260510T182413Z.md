# Dispatch Log — Task 6-17

## Task
- **ID:** 6-17
- **Title:** `lib/kanban/types.ts` + `lib/kanban/api-server.ts`
- **Plan section:** `docs/superpowers/plans/2026-05-10-milestone-06-orders-dashboard.md`
- **Line range:** 6376–6667
- **Review type:** combined single-stage

## Files Changed

- `apps/web/lib/kanban/types.ts` +52 LOC
  - `KanbanStatus` type (Extract from OrderStatus, +6: line 16-20)
  - `KanbanCardDto` interface (line 23-32): id, code, clientName, itemSummary, plannedPickupAt, urgent
  - `KanbanColumnDto` interface (line 35-41): status, total, cards, hasMore
  - `KanbanResponseDto` interface (line 44-46): columns
  - `KanbanFetchParams` interface (line 49-52): limitPerColumn optional
- `apps/web/lib/kanban/api-server.ts` +52 LOC
  - `cookieHeader()` async fn (line 13-17): forwards cookies from next/headers
  - `base()` fn (line 19-21): reads INTERNAL_API_BASE env, fallback localhost:8080
  - `serverGet<T>()` async fn (line 23-32): generic fetch wrapper with cache:no-store
  - `getKanbanBoardServer()` async fn (line 37-47): main exported fetcher with optional limitPerColumn QS
- `apps/web/lib/kanban/api-server.test.ts` +100 LOC (4 tests)
  - Test: calls correct URL with no QS when limitPerColumn not provided (line 55-67)
  - Test: appends limitPerColumn query param when provided (line 69-79)
  - Test: forwards session cookie in request headers (line 81-91)
  - Test: throws when backend returns non-OK status (line 93-100)

## Commands Run

```
pnpm --filter @drshoes/web typecheck   → PASS (clean, no output)
pnpm --filter @drshoes/web lint        → PASS (--max-warnings=0, clean)
pnpm --filter @drshoes/web test        → PASS 69/0/0/0 (13 test files)
```

## Test Summary

| Suite | Before | After |
|-------|--------|-------|
| Frontend (vitest) | 65 GREEN | 69 GREEN (+4) |
| Backend | 349 GREEN | 349 GREEN (untouched) |

## Decisions

1. **DTO shape verified against KanbanResponseDto.java** — exact match with plan. Fields: `columns`, `status`, `total`, `cards`, `hasMore`, `id`, `code`, `clientName`, `itemSummary`, `plannedPickupAt`, `urgent`. No deltas.
2. **Test pattern: vi.stubGlobal** — used `vi.stubGlobal("fetch", ...)` + `vi.restoreAllMocks()` pattern from `lib/calendar/api-server.test.ts` instead of the plan's `globalThis.fetch = mockFetch` pattern. This is the established project convention and is cleaner/safer (auto-restores between tests).
3. **Test isolation: module re-import** — each test calls `await import("./api-server")` dynamically. This works because vi.mock is hoisted and the module is not pre-imported at the top level.
4. **WSTEPNIE_PRZYJETE excluded from KanbanStatus** — plan correctly excludes it; backend COLUMN_ORDER also does not include it. Confirmed consistent.
5. **api-server.ts LOC: 52** — within 80 LOC cap. types.ts LOC: 52 — within cap.

## Review

**Verdict: APPROVED (combined single-stage)**

Checklist:
- [x] DTO interfaces match KanbanResponseDto.java exactly (record fields, types, nullability)
- [x] `KanbanStatus` uses `Extract<OrderStatus, ...>` — no free-floating string literal
- [x] Cookie forwarding pattern matches calendar/api-server.ts precedent
- [x] `INTERNAL_API_BASE` env var used with same fallback
- [x] `cache: "no-store"` set (no stale board data)
- [x] `createLogger("kanban.api-server")` used — no ad-hoc console.log
- [x] All 4 tests green (URL, QS param, cookie, error path)
- [x] typecheck clean, lint clean (--max-warnings=0)
- [x] No layout invention (types-only lib, no UI)
- [x] LOC caps respected (both files ≤ 80 LOC)

## Commit SHA

`3bcc341` — feat(kanban): lib/kanban types + api-server [milestone:6][task:6-17]

## Follow-ups

- None. The types and fetcher are ready for 6-18 (KanbanBoard components).

## Subagent Token Budget

- Estimated total_tokens: ~18,000
