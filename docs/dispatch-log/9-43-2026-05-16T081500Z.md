---
task_id: "9-43"
plan_file: "docs/superpowers/plans/2026-05-15-milestone-09-design-parity.md"
start_time: "2026-05-16T08:10:00Z"
end_time: "2026-05-16T08:15:00Z"
---

# Task 9-43 — Milestone-9 README update + git tag local

## Files changed
- `docs/superpowers/ROADMAP.md`: added M9 Done row (closed 2026-05-16, HEAD SHA, tag, suite counts); replaced stale M6/M7/M8+ "Next" sections with M10 candidate backlog (10 deferred items from 9-41 parity audit)
- `CLAUDE.md`: added M8 + M9 checkboxes under `## Status`
- `docs/dispatch-log/9-43-2026-05-16T081500Z.md`: this file

## Commands run
| Command | Exit code | Result |
|---|---|---|
| `pnpm --filter web exec vitest run` | 0 | 521/521 GREEN (84 test files) |
| `mvn -pl app verify -B` (from backend/) | 0 | 409/0/0/0 GREEN |
| `git tag -a milestone-9 -m "..."` | 0 | Tag object: `0d6ddadd6b6392fac530f14651cefab3a2f96043` |
| `git commit` (closure commit) | 0 | HEAD: `8aafc7337b3ad2ba04b287abbfab949cf6f7e44c` |

## Decisions
- README.md has no milestone table (quick-start + layout doc only) — skipped per task instructions ("if sparse, skip").
- M8 entry added to CLAUDE.md Status alongside M9 — it was missing from the status list even though M8 was completed.
- Tag created on the pre-commit HEAD (`011945b1da331249411bcd6a24933601fd76a1c2`) — this is the correct tagging point: the milestone-9 tag should point to the last implementation commit (9-42 clients reskin), not the bookkeeping/closure commit. If owner prefers the tag to point at the closure commit (`8aafc73`), retag with `git tag -d milestone-9 && git tag -a milestone-9 -m "..." HEAD`.
- DO NOT push tag or commits — owner sign-off pattern (same as milestone-7).

## M10 carry-forward (from 9-41 parity audit)
1. Drag-drop wiring — UnscheduledOrdersPanel drop handler, kanban "+ dodaj" column button
2. Order drawer add-tag flow — "+ dodaj tag" chip click handler
3. AdminTopbar global search handler — Szukaj zlecenia, klienta… input
4. Notifications popover — bell icon in AdminTopbar with real data feed
5. FreshReservationsPanel live data — GET /api/admin/sklep/reservations?limit=3 wiring
6. MixDonut chart rendering — Recharts/Chart.js integration for donut segment visual
7. Light / dark mode toggle — theme switcher UI (tokens exist, switcher not implemented)
8. Mobile-responsive layouts — admin shell and views not mobile-tuned through M9
9. Real /sklep + /aktualnosci implementations — catalog + reservations management; markdown editor + scheduling
10. Map iframe precise coords — Contact section on public landing uses placeholder coords

## Suite counts at close
- Backend: 409 tests / 0 failures / 0 errors / 0 skipped (GREEN)
- Frontend vitest: 521 tests / 0 failures (GREEN)
- Playwright m9-parity-audit: 12/12 GREEN

## commit_sha
8aafc7337b3ad2ba04b287abbfab949cf6f7e44c

## tag_sha
0d6ddadd6b6392fac530f14651cefab3a2f96043 (milestone-9, annotated, local)

## next_action
Owner sign-off + `git push && git push --tags` to publish. Then M10 brainstorming.
