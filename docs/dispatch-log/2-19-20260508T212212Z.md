# Dispatch Log: 2-19 — OrderDrawerStatusChanger real trigger preview

**Task:** 2-19  
**UTC:** 20260508T212212Z  
**Status:** completed  

## Files Modified

- `apps/web/app/(admin)/admin/orders/_components/StatusChangeConfirm.tsx`
  - Added `TriggerPreview` exported type (`kind: "match" | "disabled" | "none"`)
  - Added `triggerPreview` prop to component interface
  - Replaced placeholder italic text with real preview rendering (Polish UI copy)
  - Added `channelLabel()` helper for Polish channel names
  - Shows: template name + channels (for `match`), disabled trigger name, or "no trigger" message

- `apps/web/app/(admin)/admin/orders/_components/OrderDrawerStatusChanger.tsx`
  - Added `useEffect` to fetch all triggers once on mount via `getTriggers()`
  - Added `previewFor(targetStatus, triggers)` pure function: finds STATUS_CHANGE trigger matching `eventParams.toStatus`, returns typed `TriggerPreview`
  - Computes `triggerPreview` from current `target` state before rendering
  - Passes `triggerPreview` down to `<StatusChangeConfirm>`

## Commands Run

```bash
cd apps/web && pnpm typecheck   # exit 0
cd apps/web && pnpm build       # exit 0, all 11 routes ƒ/○ as expected
```

## Decisions

- Used `toStatus` (camelCase) in eventParams parse — confirmed from `TriggerEngine.java` line 95: `params.path("toStatus").asText("")`
- Triggers fetched once on mount (not per-open) since the list rarely changes; avoids latency on each confirm dialog open
- Extracted `TriggerPreview` type from `StatusChangeConfirm.tsx` (exported) so `OrderDrawerStatusChanger` can import it without circular deps
- No backend changes — pure frontend, informational only

## Test Summary

Suite: 149/0/0/0 (backend unchanged)  
Typecheck: green  
Build: green  

## Commit

SHA: d57789a  
Message: `feat(web): real trigger preview in StatusChangeConfirm [milestone:2][task:2-19]`
