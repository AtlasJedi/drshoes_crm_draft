# 9-2 — `<Tape>` primitive

**Status:** completed
**HEAD before:** 3119cd4 (9-1 dispatch log)
**HEAD after (Tape landed):** 3bdc5d1 (batch commit by 9-11 agent)
**Started:** 2026-05-15T193231Z
**Completed:** 2026-05-15T214200Z

## Files touched

- Create: `packages/ui/src/components/Tape.tsx` — Tape component 38 LOC
- Create: `packages/ui/src/components/__tests__/Tape.test.tsx` — 6 vitest/RTL tests
- Modify: `packages/ui/src/index.ts` — barrel export added (committed by parallel agents)
- Modify: `apps/web/vitest.config.ts` — resolve.alias for `react/jsx-dev-runtime`,
  `@testing-library/react`, etc. (committed by parallel agents; enables packages/ui RTL tests)

## Commands run

- `pnpm --filter @drshoes/web test -- Tape.test` → RED: `Cannot find module '../Tape'` (expected)
- Created `packages/ui/src/components/Tape.tsx`
- `pnpm --filter @drshoes/web test -- Tape.test` → GREEN: 6 passed ✓
- `pnpm --filter @drshoes/web test` → 379 passed / 0 failed ✓

## Decisions / deviations

1. **vitest resolve.alias required** — tests in `packages/ui/__tests__/` that import
   `@testing-library/react` and `react/jsx-dev-runtime` fail because those packages only
   live in `apps/web/node_modules`. The fix is an explicit `alias` array in
   `vitest.config.ts` mapping each sub-path specifier to the `apps/web/node_modules`
   copy. String alias `react` alone does NOT match `react/jsx-dev-runtime` — sub-path
   aliases must be listed explicitly. This config change is shared across all
   Wave 1 RTL-using primitive tests (9-2 through 9-14).

2. **Tape files batch-committed by 9-11 parallel agent** — concurrent wave-1 dispatches
   ran while 9-2 was resolving the vitest alias issue. The 9-11 agent committed
   `Tape.tsx` and `Tape.test.tsx` as part of its dispatch log batch commit (`3bdc5d1`).
   The implementation is identical to the plan spec — both agents wrote from the same
   plan source. No functional deviation.

3. **Note D (no log.debug)** honoured — `Tape.tsx` contains no `log.debug` calls per
   cross-cutting note D (presentational primitives in packages/ui must not couple to
   apps/web's `lib/log.ts`).

## Test summary

```
Test Files  61 passed (61)
     Tests  379 passed (379)
```

## Commit SHA

- `3bdc5d1` — chore(dispatch): 9-11 StatTile primitive complete (contains Tape.tsx + Tape.test.tsx)

## Followups for orchestrator

- vitest.config.ts resolve.alias pattern is now established for all Wave 1 primitive tests
- No followup needed for Tape itself
