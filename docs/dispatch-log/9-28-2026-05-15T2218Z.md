# Dispatch Log — Task 9-28

**Task:** Calendar reskin + `<UnscheduledOrdersPanel>` NEW  
**Milestone:** 9  
**UTC timestamp:** 2026-05-15T22:18Z  
**Commit SHA:** 8e12973

## Files modified

| File | Change |
|---|---|
| `apps/web/app/(admin)/admin/orders/_components/calendar/CalendarCell.tsx` | Import `Tape` from `@repo/ui`; replace plain stencil "dziś" badge with `<Tape angle={2}>` |
| `apps/web/app/(admin)/admin/orders/_components/calendar/BezTerminuPanel.tsx` | Wrap in `<AdminCard padding={16}>`; count badge → `<Chip>`; drag icon ⠿ → `<I.drag />`; `draggable` + `onDragStart` stub |
| `apps/web/app/(admin)/admin/orders/_components/calendar/utils.ts` | Split `WSTEPNIE_PRZYJETE` from `PRZYJETE` (now `adminMute`); accept `string` param; align all colours with M9 tokens |

## Commands run

```
pnpm vitest run -- calendar   # 434/434 GREEN, 69 files
```

## Test summary

- 434 tests passed, 0 failed, 0 skipped
- No new test files added (existing CalendarCell + BezTerminuPanel tests cover the changes)

## Decisions

1. **CalendarPageHeaderSetter already present** — `page.tsx` already imports `CalendarPageHeaderSetter` which calls `usePageHeader({ title: "Kalendarz", subtitle: "planowane odbiory" })`. No new component needed.
2. **CalendarMonthGrid + page.tsx toggle already match design** — segmented `bg-acid text-ink` active, `bg-paper-2` header row with `font-stencil`. No changes needed.
3. **`@repo/ui` TS2307 typecheck errors** — pre-existing pattern across all `_components/orders/` files. 2 new errors match the established pattern. Vitest alias resolves correctly; tests pass.
4. **Drag deferred per spec §7** — `draggable` + `onDragStart={() => console.warn("drag-drop wkrótce")}` + `// TODO M10` comment. No functional drag-drop shipped.
5. **colorOfStatus param widened to `string`** — allows callers with non-exhaustive status values without TS error.
