# Dispatch Log: b3 — messages plain text invariant

**Task:** B3  
**UTC:** 2026-05-16T192217Z  
**SHA:** 8666421  
**Branch:** main

## Files Changed

| File | Action |
|------|--------|
| `backend/app/src/main/java/com/drshoes/app/messaging/util/HtmlStripper.java` | Created — naive HTML→plain text stripper |
| `backend/app/src/main/java/com/drshoes/app/messaging/service/InboundMessageService.java` | Modified — apply HtmlStripper defensively on inbound email body |
| `backend/app/src/test/java/com/drshoes/app/messaging/util/HtmlStripperTest.java` | Created — 6 unit tests |
| `backend/app/src/test/java/com/drshoes/app/messaging/service/MessageRouterPlainBodyTest.java` | Created — integration test locking plain body invariant |
| `backend/app/src/test/java/com/drshoes/app/order/OrderServiceIntegrationTest.java` | Modified — fixed compile breakage from B1 (urgent param added to OrderService.list) |

## Investigation Findings

- V006 seeded plain-text body for all templates initially.
- V022 updated all 3 EMAIL template bodies to ASCII-art plain text AND set body_html to designer HTML.
- Live DB confirmed: `body` column is already plain text (no HTML markup).
- `PostmarkInboundPayload` has no `HtmlBody` field — inbound uses `textBody`/`strippedTextReply` (plain).
- Root cause of any reported "HTML in body": NOT from template seed — V022 correctly sets both columns. No V024 migration needed.

## Test Summary

- HtmlStripperTest: 6/6 GREEN
- MessageRouterPlainBodyTest: 1/1 GREEN (confirmed body is already plain, body_html has `<table`)
- MessageRouterEmailHtmlTest: 1/1 GREEN (baseline unchanged)
- Full messaging suite: 173/173 GREEN

## Decisions

- No V024 migration needed — V022 already sets plain-text body for all templates.
- HtmlStripper applied defensively to inbound email body for future-safety.
- Fixed pre-existing OrderServiceIntegrationTest compile error (lines 273, 287 missing `urgent` arg) as collateral fix — these were broken by B1's OrderService.list signature change.
