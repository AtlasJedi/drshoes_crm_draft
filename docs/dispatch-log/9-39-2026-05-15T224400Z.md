# Dispatch Log: 9-39 — `<SklepTeaser>`

**Task:** 9-39  
**Milestone:** 9 — Design Parity  
**UTC:** 2026-05-15T22:44:00Z  
**Status:** DONE

## Files Created

- `apps/web/app/(public)/_components/SklepTeaser.tsx` — section#sklep with Tape header, notice box, 5 filter pills (non-functional), 4-tile grid (84 LOC / 76 non-empty)
- `apps/web/app/(public)/_components/ProductTile.tsx` — sub-component with PhImg + Stamp overlay + index badge (66 LOC)
- `apps/web/app/(public)/_components/__tests__/SklepTeaser.test.tsx` — 8 vitest tests
- `apps/web/app/(public)/_components/__tests__/__snapshots__/SklepTeaser.test.tsx.snap` — snapshot

## Commands

```bash
cd apps/web && npm test -- "SklepTeaser"
# 8/8 GREEN
```

## Test Summary

8 tests passing: snapshot, 4 product tiles via data-testid, filter buttons (Nike, Vans), notice box text, 5 total buttons, filtruj: label, stamp per tile.

## Decisions

- Used exact PRODUCTS placeholder array from task spec (Nike AF1 Mid 'Bandana' zarezerwowane, Vans Old Skool 'Drip' dostępne, Jordan 1 'Tag' dostępne, Dr. Martens 1460 Vibram sprzedane)
- Filter onClick uses `console.warn('filter wkrótce')` per spec §7 deferral (no state needed for static demo)
- Wszystkie pill is visually active (acid bg + shadow) per design reference; real filter logic deferred
- `createLogger` import from `@/lib/log` (not `logger` — the module exports `createLogger`)
- Files entered git via commit `8adea79` (9-38 parallel agent staged them alongside NewsTeaser files)

## Commit SHA

`8adea79` (files included in NewsTeaser commit due to parallel staging)
