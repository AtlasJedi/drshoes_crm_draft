# Dispatch Log — ux-4 — 2026-05-12T17:13:00Z

**Task:** ux-4 — Quote (wycena) + advance payment (zaliczka)
**Plan:** docs/superpowers/plans/2026-05-12-admin-ux-fixes.md § Task ux-4
**Milestone:** m8-fb

---

## Backend commit

**SHA:** 702b800 (included in ux-3 dispatch commit — parallel session bundled both)

**Files modified:**
- `backend/app/src/main/resources/db/migration/V016__order_quote_and_advance.sql` — new migration
- `backend/app/src/main/java/com/drshoes/app/order/domain/Order.java` — added `quotedPriceCents`, `advancePaidCents` fields + getters/setters
- `backend/app/src/main/java/com/drshoes/app/order/dto/OrderDto.java` — added both fields + of() mapping
- `backend/app/src/main/java/com/drshoes/app/order/dto/OrderListRow.java` — added both fields + of() mapping
- `backend/app/src/main/java/com/drshoes/app/order/dto/CreateOrderRequest.java` — added `Integer quotedPriceCents`, `Integer advancePaidCents`
- `backend/app/src/main/java/com/drshoes/app/order/dto/UpdateOrderRequest.java` — added same
- `backend/app/src/main/java/com/drshoes/app/order/OrderService.java` — wire create() + update() patch
- `backend/app/src/main/java/com/drshoes/app/demo/DemoOrderFactory.java` — updated positional constructor call
- `backend/app/src/test/java/com/drshoes/app/order/OrderServiceIntegrationTest.java` — 3 new tests (18-20) + helper + updated constructors
- `backend/app/src/test/java/com/drshoes/app/audit/AuditLogAspectIntegrationTest.java` — updated constructor call

**Test result:** 20/20 OrderServiceIntegrationTest GREEN (including 3 new ux-4 tests)

**Jar rebuilt:** yes — `mvn -pl app -am -DskipTests clean package` run after commit

---

## Frontend commit

**SHA:** 1d083a8

**Files modified/created:**
- `apps/web/lib/orders/money.ts` (new) — `plnToCents` + `centsToPlnDisplay` shared helpers
- `apps/web/lib/orders/types.ts` — added `quotedPriceCents`, `advancePaidCents` to `OrderDto`, `OrderListRow`, `CreateOrderRequest`, `UpdateOrderRequest`
- `apps/web/app/(admin)/admin/orders/new/_components/NewOrderForm.tsx` — added Wycena + Zaliczka inputs + "Do zapłaty" live preview; import plnToCents from shared util
- `apps/web/app/(admin)/admin/orders/_components/OrderDrawerCoreFields.tsx` — display Wycena, Zaliczka, Do zapłaty fields; inline-edit via patch(); import plnToCents
- `apps/web/app/(admin)/admin/orders/new/_components/__tests__/NewOrderForm.test.tsx` (new) — 21 tests

**Test result:** 284/284 vitest GREEN (21 new)

---

## Decisions

- `plnToCents` extracted to `lib/orders/money.ts` (shared util) rather than duplicated — keeps both form and drawer in sync on conversion semantics.
- Drawer `patch()` signature widened to `string | number | null` so numeric fields are sent as JSON numbers to the backend.
- No CHECK constraint added in migration per plan spec (application-layer validation only).
- "Do zapłaty" display clamps to 0 (no negative balance shown) per plan spec.
- `plnToCents("")` returns 0 — so omitting the Zaliczka input naturally sends `advancePaidCents: 0`.

---

## Commands run

```
# Backend tests
mvn -pl app -am -B test -Dtest=OrderServiceIntegrationTest -Dsurefire.failIfNoSpecifiedTests=false

# Jar rebuild
mvn -pl app -am -DskipTests clean package

# Frontend tests
cd apps/web && pnpm vitest run --reporter=dot
```
