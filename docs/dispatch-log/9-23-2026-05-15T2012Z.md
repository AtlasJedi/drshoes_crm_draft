# Dispatch Log — Task 9-23

**Task:** Orders list page reskin  
**Milestone:** 9 (Design Parity)  
**Started:** 2026-05-15T20:00Z  
**Completed:** 2026-05-15T20:12Z  
**Status:** APPROVED

---

## Files Modified

| File | Change |
|------|--------|
| `packages/ui/src/components/Chip.tsx` | Added `aria-pressed`, `aria-label` props; button path now forwards them |
| `apps/web/app/(admin)/admin/orders/_components/SavedFilterPresets.tsx` | Full reskin — plain `<button>` chips → `<Chip>` from `@repo/ui`; pink for "Pilne", active for "Gotowe", dashed disabled for "+ zapisz widok" |
| `apps/web/app/(admin)/admin/orders/_components/OrdersFilters.tsx` | Full reskin — `<fieldset>`/`<select>`/`<input>` → `<Chip>` row; added `visible`/`total` props; counter "X z Y zleceń" right-aligned |
| `apps/web/app/(admin)/admin/orders/_components/OrdersTable.tsx` | `<table className="tbl">`, `<Pill status={row.status}>` replaces ad-hoc STATUS_PILL_CLASS span, `<PhImg>` photo column added between Wykonawca and actions; outer border wrapper removed |
| `apps/web/app/(admin)/admin/orders/page.tsx` | Removed old `<h1>` + `<Link>` header block; passes `visible={pageData?.content.length}` and `total={pageData?.totalElements}` to `<OrdersFilters>` |
| `apps/web/app/(admin)/admin/orders/_components/OrderDrawerStatusChanger.tsx` | FILLED_CLASS: `W_REALIZACJI` `bg-acid→bg-orange`, `GOTOWE_DO_ODBIORU` `bg-magenta→bg-green`, `WYDANE` `bg-green→bg-ink-2`, `ANULOWANE` `bg-orange→bg-red`; IDLE_CLASS updated to match |
| `apps/web/app/(admin)/admin/orders/_components/__tests__/SavedFilterPresets.test.tsx` | Added RED→GREEN test asserting `data-color="pink"` on first preset chip; updated `bg-ink\|text-paper` assertion to also match `.active` class |

---

## Commands Run

```
pnpm vitest run   # apps/web — 434 passed, 0 failed, 69 files
```

---

## Test Summary

- **434 tests passed, 0 failed** across 69 test files
- SavedFilterPresets: 13/13 tests green (including new pink-chip assertion)
- No regressions in OrdersTable, OrderDrawer, BulkActionBar, StatusChangeTriggerDialog suites

---

## Decisions

1. `Chip` renders as `<button>` when `onClick` or `disabled` is given — keeps `.closest("button")` test patterns working without changing test selectors.
2. `OrdersFilters` status chip is display-only (no dropdown toggle) — the full `StatusMultiSelect` dropdown behaviour was dropped in favour of the design-spec chip row; status filtering still works via URL params set by `SavedFilterPresets`.
3. `OrderDrawerStatusChanger` colour migration: fixed `FILLED_CLASS`/`IDLE_CLASS` maps to match M9 token colours (`orange` for W_REALIZACJI, `green` for GOTOWE_DO_ODBIORU) without replacing the segmented control with `<Pill>` — the segmented control is a distinct interactive pattern, not a display badge.
4. `visible` count uses `pageData.content.length` (current page items), `total` uses `pageData.totalElements` — matches `Page<T>` shape from `lib/clients/types.ts`.

---

## Commit SHA

`b60b6ee` — changes landed in the `feat(orders): reskin drawer photo grid + items + tags [milestone:9][task:9-26]` commit (9-23 work was bundled with 9-26 in the same session wave)_
