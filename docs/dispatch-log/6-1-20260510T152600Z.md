---
task_id: 6-1
plan: docs/superpowers/plans/2026-05-10-milestone-06-orders-dashboard.md
milestone: 6
agent: sonnet
start_utc: 2026-05-10T15:20:00Z
end_utc: 2026-05-10T15:26:00Z
status: DONE
commit_sha: 11bc14e
---

# Task 6-1 — AdminWebTestBase FK ordering fix

## Files changed
- `backend/app/src/test/java/com/drshoes/app/AdminWebTestBase.java` (modified — added `OrderRepository` + `MessageThreadRepository` injections; FK-correct delete order in both `seedUsers()` and `cleanupUsers()`)
- `backend/app/src/test/java/com/drshoes/app/AdminWebTestBaseFkOrderingIntegrationTest.java` (new, 72 LOC — regression test proving the FK-correct teardown)

## Commands run
```
# Step 2: regression test before fix (expected to fail)
cd backend && mvn -pl app -am -B test -Dsurefire.failIfNoSpecifiedTests=false \
  -Dtest=AdminWebTestBaseFkOrderingIntegrationTest
# → Tests run: 2, Failures: 0, Errors: 2 (DataIntegrityViolationException on order__client_id_fkey)

# Step 4: regression test after fix
cd backend && mvn -pl app -am -B test -Dsurefire.failIfNoSpecifiedTests=false \
  -Dtest=AdminWebTestBaseFkOrderingIntegrationTest
# → Tests run: 2, Failures: 0, Errors: 0, Skipped: 0  ✓

# Step 5: full suite
cd backend && mvn -pl app -am -B verify
# → Tests run: 304, Failures: 0, Errors: 0, Skipped: 0  BUILD SUCCESS
```

## Test summary
- **Before fix (M5 close):** 301 passed / 0 failures / 1 error / 0 skipped
- **After fix:** 304 passed / 0 failures / 0 errors / 0 skipped
- Net change: +3 tests (2 new regression tests + 1 extra that was previously erroring out), 0 errors

## Decisions / deviations from plan

- **Teardown order chosen:** `auditLogs → threads → orders → clients → users` — exactly as specified in the plan's FK graph. No deviations.
- **Regression test `@AfterEach` intentionally empty:** `cleanupOrderAndThread()` does not delete the extra order/thread, forcing `AdminWebTestBase.cleanupUsers()` to handle them. This is the exact scenario that was causing the M5 flake.
- **Suite count 304 vs plan's expected 303:** The plan expected 303/0/0/0 (301 + 2 new). The actual result is 304/0/0/0. The extra test was `MessagesControllerIntegrationTest.emptyThreadReturnsEmptyList` — previously it was erroring in `@BeforeEach` (counted as ERROR, not as a passing test). With the FK fix in place, `@BeforeEach` now succeeds and the test itself passes, adding 1 to the passing count on top of the 2 new regression tests. Net: +3 passing, -1 error. This is the correct outcome.

## Logging / metadata wired
- N/A — test-only change, no production logging paths affected.

## Concerns
- None.

## Next action
- Move to task 6-2 (MessageRouter split + sendRetry/send dedup) per plan.
