---
task_id: 3-2
plan: docs/superpowers/plans/2026-05-09-milestone-03-photos.md
milestone: 3
agent: sonnet
start_utc: 2026-05-09T22:54:01Z
end_utc: 2026-05-09T23:00:00Z
status: DONE
commit_sha: ed2f164
---

# Task 3-2 — Extend BlobStorage with get(BlobKey) → InputStream

## Files changed
- `backend/libs/storage/src/main/java/com/drshoes/lib/storage/BlobStorage.java` (modified — added `InputStream get(BlobKey key)` with javadoc)
- `backend/libs/storage/src/main/java/com/drshoes/lib/storage/S3BlobStorage.java` (modified — implemented `get()` via `S3Client.getObject`)
- `backend/libs/storage/src/test/java/com/drshoes/lib/storage/S3BlobStorageIntegrationTest.java` (modified — refactored to shared `@BeforeAll` + added 2 new tests)

## Commands run
```
mvn -pl libs/storage test   # exit 0 — 4/0/0/0
mvn -pl app -am test        # exit 0 — 151/0/0/1 (1 skipped = @Disabled PhotoRepositoryTest)
```

## Test summary
- Storage module: Tests run: 4, Failures: 0, Errors: 0, Skipped: 0
- App module (full reactor): Tests run: 151, Failures: 0, Errors: 0, Skipped: 1

## Decisions / deviations from plan

- **Test refactored to shared `@BeforeAll` setup**: The plan appended two new test methods that reference a `storage` field variable. The existing tests each built `S3BlobStorage` inline per method. Refactored to a single `@BeforeAll` that builds the shared `storage` instance and creates the bucket once. This avoids duplication and matches what the plan's new test bodies require. Existing two tests migrated to use the shared instance with no behavior change.

- **No structured logging added**: `S3BlobStorage` has no `Logger` field — the existing methods have no log statements. Adding one only for `get()` would be inconsistent. Plan ERRATA #7 specifies logging for service/controller layers; the storage microlib is infrastructure and was intentionally logging-free. Consistent with prevailing pattern.

## Logging / metadata wired
- N/A — storage microlib; no logger present or warranted.

## Concerns
- none

## Next action
- Move to task `3-3` (AdminPrincipal record + AuthService wrap — two-stage review) per plan.
