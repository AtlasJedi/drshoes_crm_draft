---
task_id: "9-42"
plan_file: "docs/superpowers/plans/2026-05-15-milestone-09-design-parity.md"
start_time: "2026-05-15T22:50:00Z"
end_time: "2026-05-15T23:00:00Z"
---

# Task 9-42 — Clients reskin

## Files changed
- `apps/web/app/(admin)/admin/clients/page.tsx` +1-12: removed inline h1/header div, imported ClientSearchResultsTable from extracted file, removed inline sub-component block (64 LOC)
- `apps/web/app/(admin)/admin/clients/_components/ClientListSearchBox.tsx` +37: input class → `t-mono border-ink` + `placeholder:text-admin-mute` (41 LOC)
- `apps/web/app/(admin)/admin/clients/_components/ClientListTable.tsx` +1-71: removed RodoInline inline function + fmtRodoDate, imported RodoBadge + ClientListPagination, table → `.tbl`, channel → `.chip`, extracted pagination (71 LOC)
- `apps/web/app/(admin)/admin/clients/_components/ClientListPagination.tsx` +1-36: NEW — extracted from ClientListTable (36 LOC)
- `apps/web/app/(admin)/admin/clients/_components/ClientSearchResultsTable.tsx` +1-53: NEW — extracted from page.tsx, uses `.tbl` (53 LOC)
- `apps/web/app/(admin)/admin/clients/_components/ClientsPageHeaderSetter.tsx` +16: subtitle → `N klientów` (was `N w bazie`)
- `apps/web/app/(admin)/admin/clients/[id]/page.tsx` +1-64: extracted preview cards to ClientPreviewCards, dropped fmtDate local helper, added ClientPreviewCards import (64 LOC)
- `apps/web/app/(admin)/admin/clients/[id]/_components/ClientPreviewCards.tsx` +1-99: NEW — two-column orders+threads preview cards with AdminCard wrappers (99 LOC)
- `apps/web/app/(admin)/admin/clients/[id]/_components/ClientHeader.tsx` +39: h1 → `t-display` class + clamp font-size style
- `apps/web/app/(admin)/admin/clients/[id]/_components/ClientDetailPageHeaderSetter.tsx` +1-27: added createdAt prop + fmtMonthYear, subtitle → `klient od MM.YYYY`
- `apps/web/app/(admin)/admin/clients/_components/__tests__/ClientListTable.test.tsx`: added 3 new assertions (tbl class, rodo-badge testid, chip class)
- `apps/web/app/(admin)/admin/clients/_components/__tests__/ClientListSearchBox.test.tsx`: added t-mono + border-ink class assertion
- `apps/web/app/(admin)/admin/clients/_components/__tests__/ClientsPage.test.tsx`: updated "renders Klienci heading" → "renders search input (topbar carries title)" since h1 moved to topbar; renamed "renders search input" → "renders client list table"

## Commands run
| Command | Exit code |
|---|---|
| `pnpm --filter=web exec vitest run` (baseline) | 0 (517 pass) |
| `pnpm --filter=web exec vitest run` (after RED) | 1 (4 new failures expected) |
| `pnpm --filter=web exec vitest run` (after GREEN) | 0 (521 pass) |

## Decisions
- `ClientSearchResultsTable` extracted from page.tsx (was inline, flagged in plan comment) to keep list page under 80 LOC
- `ClientListPagination` extracted from ClientListTable.tsx (plan directive) to keep table file at 71 LOC
- `ClientPreviewCards` extracted from [id]/page.tsx to keep detail page at 64 LOC
- `AdminCard` from `@drshoes/ui` used in detail page (Wave 1 already shipped it)
- `ClientHeader` kept as `.admin-card` div wrapper — `ClientHeader` already uses `className="admin-card ..."` div which is visually identical to `<AdminCard>` and avoids breaking the existing layout with `EditClientIsland` flex alignment
- `thCls` removed from ClientListTable — `.tbl th` CSS class handles all th styling
- Subtitle changed from "N w bazie" → "N klientów" per plan acceptance criteria

## commit_sha
<!-- filled after commit -->

## next_action
"Dispatch 9-43 (milestone-9 close-out / README + git tag)"
