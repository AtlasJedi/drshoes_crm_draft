# Dispatch Log — Task 9-27

**Task:** OrderDrawer footer actions + header reskin
**Milestone:** 9 (design-parity)
**UTC:** 2026-05-15T22:10Z
**Commit:** f46cc5d
**Status:** APPROVED

## Files Modified

- `apps/web/app/(admin)/admin/orders/_components/OrderDrawerHeader.tsx` — full reskin: close btn-clean + I.close, t-display 26 DR-code, t-mono 11 client·receivedAt sub-line, Pill status, more btn-clean + I.more; added clientName + receivedAt props
- `apps/web/app/(admin)/admin/orders/_components/OrderDrawer.tsx` — Dialog.Content className removed, replaced with inline style aside (position fixed right 0, width 540, ink border-left 3px, box-shadow, drawerIn animation); OrderDrawerHeader now receives clientName + receivedAt; footer action bar added after scroll container
- `apps/web/vitest.config.ts` — added `{ find: "@repo/ui", replacement: repoUi }` alias pointing at `packages/ui/src/index.ts`; this fixed the long-standing @repo/ui resolution failure in vitest, unlocking full suite (434 tests vs previous ~203)

## Files Created

- `apps/web/app/(admin)/admin/orders/_components/__tests__/OrderDrawer.test.tsx` — 6 smoke tests for drawer reskin (footer buttons, header code + clientName)
- `apps/web/app/(admin)/admin/orders/_components/__tests__/OrderDrawerHeader.test.tsx` — 5 unit tests (code render, sub-line, close/more buttons, absent sub-line)

## Test Summary

- Frontend vitest: **434 passed / 0 failed** (69 test files)
- New tests: 11 passing (OrderDrawer 6 + OrderDrawerHeader 5)

## Decisions

- `vi.mock("@repo/ui")` alone is insufficient — vite resolves specifiers at transform time before mocks apply. Fix: alias in vitest.config.ts.
- `OrderDrawerHeader` mock in `OrderDrawer.test.tsx` avoids double-mocking `@repo/ui` and keeps the drawer test focused on footer/structure.
- Footer "zmień status" and "oznacz jako wydane" buttons are stubs with comments per plan — full wiring deferred to future milestone per plan spec.
- `@repo/ui` alias also fixed `OrderDrawerTagsRow.test.tsx` which was pre-existing failing.
