---
task_id: 4-9
plan: docs/superpowers/plans/2026-05-09-milestone-04-real-providers.md
milestone: 4
agent: sonnet
start_utc: 2026-05-09T19:14:22Z
end_utc: 2026-05-09T19:18:15Z
status: DONE
commit_sha: TBD
---

## Files Changed

- `backend/app/src/main/java/com/drshoes/app/messaging/service/WebhookStatusReconciler.java`
  — Replaced `AUDIT_PATH` (single constant) with three public constants:
    `AUDIT_PATH_PREFIX`, `AUDIT_PATH_DELIVERED`, `AUDIT_PATH_FAILED`.
    Audit write now chooses `#applyDelivered` or `#applyFailed` based on `targetStatus`.

- `backend/app/src/main/java/com/drshoes/app/messaging/timeline/MessageReconcileTimelineHandler.java` (NEW)
  — Per-handler class (68 LOC). Matches `#applyDelivered` → `MESSAGE_DELIVERED`,
    `#applyFailed` → `MESSAGE_FAILED`. Returns null for all other paths.

- `backend/app/src/main/java/com/drshoes/app/audit/TimelineEventCurator.java`
  — Removed `INTERNAL_RECONCILER_APPLY` Pattern constant and inline reconciler
    dispatch block. Added `MessageReconcileTimelineHandler reconcileHandler` field
    and wired in constructor (Spring auto-wires). Delegation via
    `reconcileHandler.toEvent(log, actorFullName)` placed after messaging handler.
    LOC: 213 → 203.

- `backend/app/src/test/java/com/drshoes/app/webhooks/PostmarkWebhookControllerIntegrationTest.java`
  — Added `MESSAGE_FAILED` timeline assertion to `bounce_validAuth_returns200_appliedOutcomeFailed`.

- `backend/app/src/test/java/com/drshoes/app/audit/TimelineEventCuratorTest.java`
  — Updated constructor call to pass second mock `MessageReconcileTimelineHandler`.

- `backend/app/src/test/java/com/drshoes/app/messaging/timeline/MessageReconcileTimelineHandlerTest.java` (NEW)
  — 5 unit tests: DELIVERED path → MESSAGE_DELIVERED, FAILED path → MESSAGE_FAILED,
    unrelated path → null, non-zero status → null, null path → null.

## Commands Run

```
# RED
mvn -B -pl app -am test -Dtest=PostmarkWebhookControllerIntegrationTest -Dsurefire.failIfNoSpecifiedTests=false
# → 6 run, 1 failure (bounce case: MESSAGE_FAILED not in timeline)

# GREEN (targeted)
mvn -B -pl app -am test -Dtest="PostmarkWebhookControllerIntegrationTest,MessageReconcileTimelineHandlerTest,TimelineEventCuratorTest" -Dsurefire.failIfNoSpecifiedTests=false
# → 26 run, 0 failures

# REGRESSION
mvn -B -pl app -am test
# → 187 run, 0 failures, 0 errors, 0 skipped
```

## Test Summary

| Phase      | Run | Fail | Error | Skip |
|------------|-----|------|-------|------|
| RED        |   6 |    1 |     0 |    0 |
| GREEN      |  26 |    0 |     0 |    0 |
| REGRESSION | 187 |    0 |     0 |    0 |

Prior suite total: 182. New total: 187 (+5 = MessageReconcileTimelineHandlerTest).

## Decisions

1. `AuditLogRepository.findOrderTimelineRows` uses `parentEntityId = :orderId` for INTERNAL
   rows — no hard-coded path string at all. No change needed.

2. `TimelineEventCuratorTest` constructor updated to pass a second mocked handler; all
   existing test cases unaffected (both mocks return null for non-reconcile rows).

3. `WebhookStatusReconciler` constants declared `public static final` so
   `MessageReconcileTimelineHandler` references them directly — no string duplication.

4. The reconciler had 186 LOC before this task (already over the 120-LOC cap, flagged
   in 4-7). After this task it is 194 LOC (+8 for constants + path-selection). The cap
   debt predates 4-9; a split of `persistWebhookEvent` helpers into a separate
   `WebhookEventPersistenceHelper` class is the cleanest split path — deferred to 4-16
   or a dedicated cleanup task.

## Deliverable Verification

**A — Correctness fix (DELIVERED vs FAILED audit paths):**
`WebhookStatusReconciler` now writes `#applyDelivered` for DELIVERED outcomes and
`#applyFailed` for FAILED outcomes. The bounce IT now asserts `MESSAGE_FAILED` in the
timeline and passes.

**B — Architectural refactor (`MessageReconcileTimelineHandler`):**
New 68-LOC handler class created under `messaging/timeline/`. `TimelineEventCurator`
delegates to it via `reconcileHandler.toEvent()` — mirrors `MessageSentTimelineHandler`
pattern exactly. Inline block and dead `INTERNAL_RECONCILER_APPLY` constant removed.
Path-suffix string literals are defined once in `WebhookStatusReconciler` constants.

**C — Missing IT assertion for `MESSAGE_FAILED`:**
Added `jsonPath("$[?(@.kind == 'MESSAGE_FAILED')]").exists()` assertion to the bounce
test method. Also added 5-case unit test `MessageReconcileTimelineHandlerTest` covering
both outcome paths and three null/skip cases.

## LOC After Refactor

| Class                          | LOC | Cap | Status          |
|--------------------------------|-----|-----|-----------------|
| TimelineEventCurator           | 203 | 120 | over (pre-existing) |
| MessageReconcileTimelineHandler |  68 | 120 | OK              |
| WebhookStatusReconciler        | 194 | 120 | over (pre-existing, grew +8) |

## Carry-Forward for 4-10 / 4-16

- `WebhookStatusReconciler` is 194 LOC (cap = 120). Split into a
  `WebhookEventPersistenceHelper` is the recommended path — suitable for a cleanup
  pass in 4-16 or after milestone close.
- `TimelineEventCurator` is 203 LOC (cap = 120). A pattern-registry refactor was
  noted in M1 dispatch logs as future debt; still open.
- `AuditLogRepository.findOrderTimelineRows` required no change — it queries by
  `parentEntityId`, not by path string.
