# Dispatch Log — v2-B Calendar Overhaul

**Task:** v2-B [TWO-STAGE] — Calendar overhaul — two markers per order
**Milestone:** v2-fixes
**UTC:** 20260516T230916Z
**Commit SHA:** 1b70c7b

## Files Modified

### Backend
- `backend/app/src/main/java/com/drshoes/app/order/dto/CalendarResponseDto.java` — added `effectivePickupAt: Instant`, `pickupAtDefaulted: boolean` to `CalendarOrderDto`; updated `unscheduled[]` javadoc (always empty)
- `backend/app/src/main/java/com/drshoes/app/order/api/CalendarController.java` — compute `effectivePickupAt = plannedPickupAt ?? receivedAt+14d`; move all active orders to `scheduled[]`; extended log with `defaultedCount`
- `backend/app/src/main/java/com/drshoes/app/order/domain/OrderRepository.java` — added `findAllActiveInWindow()` native query matching on receivedAt, plannedPickupAt, or +14d fallback
- `backend/app/src/test/java/com/drshoes/app/order/api/CalendarControllerIntegrationTest.java` — updated `unscheduledOrderAppearsInUnscheduledArray` → `orderWithoutPlannedPickupAppearsInScheduledWithDefaultedFlag`; added `calendarReturnsEffectivePickupAt_withPlannedPickup`, `calendarReturnsEffectivePickupAt_withFallbackPlus14d`

### Frontend
- `apps/web/lib/calendar/types.ts` — added `effectivePickupAt: string`, `pickupAtDefaulted: boolean`; `receivedAt` non-nullable; `unscheduled[]` documented as always empty
- `apps/web/app/(admin)/admin/orders/_components/calendar/CalendarMonthGrid.tsx` — full rewrite: buckets each order twice (green at `receivedAt`, red/dashed-red at `effectivePickupAt`); removed `CalendarCell` dependency; inline chip rendering with `data-testid="chip-{id}-{markerType}"`
- `apps/web/app/(admin)/admin/orders/_components/calendar/CalendarWeekGrid.tsx` — switched palette acid→green, magenta→red; `effectivePickupAt` replaces `plannedPickupAt`; dashed dot border when `pickupAtDefaulted`; added `data-testid` on chips
- `apps/web/app/(admin)/admin/orders/_components/calendar/CalendarDayGrid.tsx` — switched palette acid→green, magenta→red; uses `effectivePickupAt`
- `apps/web/app/(admin)/admin/orders/_components/calendar/CalendarCell.tsx` — removed `colorOfStatus` import; simplified to green chip style
- `apps/web/app/(admin)/admin/orders/_components/calendar/BezTerminuPanel.tsx` — **DELETED**
- `apps/web/app/(admin)/admin/orders/calendar/page.tsx` — dropped `BezTerminuPanel` import; switched 2-column grid → full-width single column
- `apps/web/app/(admin)/admin/orders/_components/calendar/__tests__/CalendarMonthGrid.test.tsx` — updated factory (new DTO fields); 4 new v2-B tests (green chip, red chip, due-defaulted, two-cell spread)
- `apps/web/app/(admin)/admin/orders/_components/calendar/__tests__/CalendarWeekGrid.test.tsx` — updated factory; 3 new v2-B tests (green chip, red chip, two-cell spread)
- `apps/web/app/(admin)/admin/orders/_components/calendar/__tests__/CalendarCell.test.tsx` — updated factory (added `effectivePickupAt`, `pickupAtDefaulted`, non-null `receivedAt`)
- `apps/web/app/(admin)/admin/orders/_components/calendar/__tests__/BezTerminuPanel.test.tsx` — replaced with tombstone test
- `apps/web/app/(admin)/admin/orders/calendar/__tests__/page.test.tsx` — removed `BezTerminuPanel` mock; replaced deleted test with v2-B full-width assertion

## Test Summary

**Backend:** 497 tests — 0 failures, 0 errors, 0 skipped
- `CalendarControllerIntegrationTest`: 10/10 GREEN (including 2 new v2-B cases)

**Frontend vitest:** 589 tests — 1 failure (pre-existing `OrderDrawerHeader` from v2-F work-in-progress), 0 v2-B failures
- All calendar tests: 56 GREEN
- `CalendarMonthGrid`: 8/8, `CalendarWeekGrid`: 7/7, `CalendarCell`: 7/7, page: 8/8, BezTerminuPanel tombstone: 1/1

## TWO-STAGE Self-Review

- (a) Backend nullability tested: with-pickup (defaulted=false) and null-pickup fallback (defaulted=true) both covered
- (b) All three grids use green/red consistently: confirmed
- (c) Defaulted red chip visually distinct: `border-dashed border-red bg-red/10` vs solid `border-red bg-red/15`
- (d) `BezTerminuPanel.tsx` fully removed from all imports: confirmed (deleted file, removed from page.tsx)

## Deviations

- `CalendarCell` is kept but inlined out of `CalendarMonthGrid` — production month view now renders chips directly in `CalendarMonthGrid`, `CalendarCell` is legacy-only (still tested via its own test file)
- `orderWithoutPlannedPickupAppearsInScheduledWithDefaultedFlag` replaces the old `unscheduledOrderAppearsInUnscheduledArray` test — semantics inverted per v2-B contract
- `OrderDrawerHeader` vitest failure is pre-existing from v2-F work-in-progress changes (not v2-B scope)
