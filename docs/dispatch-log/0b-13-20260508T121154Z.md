---
task_id: 0b-13
plan: docs/superpowers/plans/2026-05-08-milestone-00b-auth-rbac.md
milestone: 0B
agent: sonnet
start_utc: 2026-05-08T12:10:00Z
end_utc: 2026-05-08T12:12:00Z
status: DONE
commit_sha: TBD
---

# Task 0b-13 — End-to-end auth verification + tag milestone-0b

## Files changed
- `CLAUDE.md` (modified — status checklist: 0B marked complete)
- `docs/dispatch-log/0b-13-20260508T121154Z.md` (new — this file)
- `docs/dispatch-log/tasks.json` (modified — 0b-13 marked completed)

## Commands run
```
docker compose down -v                          # wipe volumes
docker compose up -d --build                    # build + boot all services
docker compose ps                               # confirm health
curl ... POST /api/admin/auth/login             # smoke check 1
curl ... GET  /api/admin/auth/me  (with cookie) # smoke check 2
curl ... GET  /api/admin/auth/me  (no cookie)   # smoke check 3
curl ... -L http://localhost:3000/admin          # smoke check 4
docker compose down                             # tear down (no -v)
```

## Pre-flight
- `Docker Compose version v2.40.3-desktop.1` — available.
- Compose file: `docker-compose.yml` at project root.
- Services: `postgres`, `minio`, `minio-init`, `backend`, `web`.
- Ports from `.env`: `BACKEND_PORT=8081`, `WEB_PORT=3000`.

## Stack boot
- `docker compose down -v` succeeded cleanly (volumes wiped).
- `docker compose up -d --build` — all images built from cache; all services reached healthy.
- `docker compose ps` output:
  ```
  misza_madafaka-backend-1   Up (healthy)   0.0.0.0:8081->8080/tcp
  misza_madafaka-minio-1     Up (healthy)   0.0.0.0:9000-9001->9000-9001/tcp
  misza_madafaka-postgres-1  Up (healthy)   0.0.0.0:5432->5432/tcp
  misza_madafaka-web-1       Up             0.0.0.0:3000->3000/tcp
  ```

## Smoke checks

### 1 — Login (POST /api/admin/auth/login)
```
curl -sS -c /tmp/dr-cookies.txt -X POST http://localhost:8081/api/admin/auth/login \
  -H 'Content-Type: application/json' \
  -d '{"email":"misza@drshoes.pl","password":"change-me-on-first-login"}' -i | head -25
```
Response (status + headers):
```
HTTP/1.1 204
X-Content-Type-Options: nosniff
X-XSS-Protection: 0
Cache-Control: no-cache, no-store, max-age=0, must-revalidate
Pragma: no-cache
Expires: 0
X-Frame-Options: DENY
Set-Cookie: dr_session=<redacted>; Path=/; HttpOnly; SameSite=Lax
Date: Fri, 08 May 2026 12:11:38 GMT
```
Result: PASS — 204 No Content, `dr_session` cookie issued.
Note: Login did NOT require a CSRF pre-flight (login endpoint is on the public allow-list). Direct POST succeeded.

### 2 — /me with cookie (GET /api/admin/auth/me)
```
curl -sS -b /tmp/dr-cookies.txt http://localhost:8081/api/admin/auth/me
```
Response body:
```json
{"id":"dfe698a2-79a1-4b90-90ef-e209eba9f91a","email":"misza@drshoes.pl","fullName":"Misza Doctor","role":"OWNER","lastLoginAt":"2026-05-08T12:11:38.439306Z"}
```
Result: PASS — OWNER user returned; V002 seed applied correctly.

### 3 — /me without cookie (unauthenticated)
```
curl -sS -i http://localhost:8081/api/admin/auth/me | head -5
```
Response:
```
HTTP/1.1 401
X-Content-Type-Options: nosniff
X-XSS-Protection: 0
Cache-Control: no-cache, no-store, max-age=0, must-revalidate
Pragma: no-cache
```
Result: PASS — 401 Unauthorized as expected.

### 4 — Admin redirect (Next.js auth guard)
```
curl -sS -L -o /dev/null -w '%{url_effective}\n' http://localhost:3000/admin
```
Response:
```
http://localhost:3000/admin/login?next=%2Fadmin
```
Result: PASS — redirects to `/admin/login?next=%2Fadmin` as specified.

## Test summary
- 4 / 4 smoke checks PASSED.
- All checks match expected behavior from the milestone plan.

## Decisions / deviations from plan
- Backend exposed on port 8081 (not default 8080) — configured via `.env` `BACKEND_PORT=8081`. The compose file correctly maps 8081→8080.
- Login endpoint is on the public allow-list (no CSRF pre-flight needed), confirming the SecurityConfig allow-list works as intended.

## Logging / metadata wired
- N/A (verification task, no code written).

## Concerns
- `next lint` issue noted in 0b-10 (ESLint config broken in Next 16 / next lint removed). Captured in `milestone-0b` tag message under "Known deferred to 0C". Does not affect runtime or smoke checks.
- `server.servlet.session.cookie.secure=false` in `application.yaml` (pre-existing hardening debt, noted in tasks.json notes). Deferred to 0C per plan.

## Next action
- Milestone 0B complete. Proceed to Milestone 1: Order domain + drawer + audit timeline.
