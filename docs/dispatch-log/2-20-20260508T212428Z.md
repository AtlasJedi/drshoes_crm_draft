---
task_id: 2-20
plan: docs/superpowers/plans/2026-05-08-milestone-02-messaging.md
milestone: 2
agent: sonnet
start_utc: 2026-05-08T21:24:28Z
end_utc: 2026-05-08T21:30:00Z
status: DONE
commit_sha: 02d0978
---

# Task 2-20 — /api/health endpoint + timeline label ratification errata

## Files changed
- `backend/app/src/main/java/com/drshoes/app/messaging/api/HealthController.java` (new, 19 LOC)
- `backend/app/src/test/java/com/drshoes/app/messaging/api/HealthControllerTest.java` (new, 22 LOC)
- `backend/app/src/main/java/com/drshoes/app/config/SecurityConfig.java` (modified — added `/api/health` to PUBLIC_MATCHERS)
- `docs/superpowers/plans/2026-05-08-milestone-02-messaging.md` (modified — appended ERRATA bullet 13)

## Commands run
```
# TDD RED
cd backend && mvn -B -pl app -am test -Dtest=HealthControllerTest -Dsurefire.failIfNoSpecifiedTests=false
# → ApplicationContext failed (no DB) — confirmed RED

# TDD GREEN (after implementing controller + SecurityConfig + fixing test base)
cd backend && mvn -B -pl app -am test -Dtest=HealthControllerTest -Dsurefire.failIfNoSpecifiedTests=false
# → Tests run: 1, Failures: 0, Errors: 0, Skipped: 0 — GREEN

# Full suite
cd backend && mvn -B -pl app -am test
# → Tests run: 150, Failures: 0, Errors: 0, Skipped: 0 — BUILD SUCCESS
```

## Test summary
- Tests run: 150, Failures: 0, Errors: 0, Skipped: 0
- Suite delta: 149 → 150 (+1 HealthControllerTest)

## Decisions / deviations from plan
- Plan specified plain `@SpringBootTest` + `@AutoConfigureMockMvc`. This project requires Testcontainers Postgres to boot the full context. Updated test to extend `AbstractIntegrationTest` (the established pattern for all integration tests in this codebase, per ERRATA bullet 7).
- CSRF is not enforced on `/api/health` (GET, public) — no `.with(csrf())` needed.
- `/api/health` added only to PUBLIC_MATCHERS, not to CSRF_IGNORED (GET endpoint, no state changes, no session required).

## Logging / metadata wired
- `HealthController` has no explicit logging (trivial read-only endpoint; AuditLogAspect will pick it up via `.api.` package convention per plan note).

## Concerns
- None.

## Next action
- Move to task 2-21: E2E compose smoke + milestone-2 tag + CLAUDE.md flip.
