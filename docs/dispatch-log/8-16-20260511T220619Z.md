# Dispatch Log — Task 8-16

**Task:** 8-16 — End-to-end OTel propagation smoke test (combined single-stage)
**Plan section:** `docs/superpowers/plans/2026-05-11-milestone-08-demo-ready.md` lines 2898–3082
**Timestamp:** 2026-05-11T220619Z
**Review type:** Combined single-stage (no Stage 2)

---

## Files changed

- `apps/web/lib/otel/propagation.test.ts` (new, 39 lines)
  - `describe("W3C traceparent propagation")` — 2 tests
  - Test 1: `W3CTraceContextPropagator injects traceparent into carrier` — uses `@opentelemetry/core` `W3CTraceContextPropagator`, creates a `SpanContext` with known traceId/spanId, injects into carrier, asserts `traceparent` header matches W3C format
  - Test 2: `traceparent format is 00-<traceId>-<spanId>-01` — format validation regex test

- `backend/app/src/test/java/com/drshoes/app/otel/TracePropagationIntegrationTest.java` (new, 58 lines)
  - `class TracePropagationIntegrationTest extends AbstractIntegrationTest`
  - `healthEndpointAcceptsTraceparentHeader()` — sends GET `/actuator/health` with W3C `traceparent` header, asserts HTTP 200 or 503 (not rejected)
  - `serverDoesNotReflectTraceparentInResponseByDefault()` — same request, asserts response headers do NOT contain `traceparent` (no header reflection risk)

- `apps/web/package.json` (modified)
  - Added `"@opentelemetry/core": "^1.28.0"` to dependencies (was missing; package was in pnpm virtual store via transitive dep from sdk-trace-web, but not declared)

- `pnpm-lock.yaml` (modified)
  - Updated lockfile to reflect new explicit `@opentelemetry/core` dep in `@drshoes/web`

---

## Commands run

```
pnpm install                                                          # PASS — resolved from pnpm store, no download
pnpm --filter @drshoes/web test                                       # PASS — 180/0/0/0
pnpm --filter @drshoes/web typecheck                                  # PASS — no errors
pnpm --filter @drshoes/web lint                                       # PASS — 0 warnings
mvn -f backend/pom.xml -pl app -am test -Dtest=TracePropagationIntegrationTest
  -Dsurefire.failIfNoSpecifiedTests=false                             # PASS — 2/0/0/0
mvn -f backend/pom.xml -pl app -am test                              # PASS — 390/0/0/0
```

---

## Test summary

| Suite | Before | After |
|-------|--------|-------|
| Backend `app-am` | 388/0/0/0 | 390/0/0/0 |
| Frontend vitest | 178/0/0/0 | 180/0/0/0 |

---

## Decisions

1. **`@opentelemetry/core` not in `apps/web/package.json`:** The plan noted to add it if missing. Confirmed it was missing from explicit deps but was present in pnpm virtual store as a transitive dep. Added it explicitly to deps as the plan requires — `pnpm install` resolved it from store without network download.

2. **`-Dsurefire.failIfNoSpecifiedTests=false` flag:** Using `-pl app -am -Dtest=TracePropagationIntegrationTest` causes Surefire to fail on non-app modules where the test class doesn't exist. Added the flag to suppress this. Alternatively, target only `app` module — but that fails dependency resolution without `-am`. This is the clean solution.

3. **OTel exporter connection errors in full suite run:** Expected noise — no OTLP collector running in test environment (localhost:4318 not available). Does not affect test pass/fail counts. Carry-forward from earlier milestones.

4. **AbstractIntegrationTest uses `RANDOM_PORT`:** Confirmed. `@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)`. `TestRestTemplate` auto-configured and `@Autowired` on the concrete test class works correctly.

5. **Health endpoint returns 200 in test context:** Confirmed. Actuator health probes enabled in `application.yaml`. No DB health check failure in test profile with Testcontainers Postgres running.

---

## Review

**Verdict: APPROVED**

Checklist:
- [x] Frontend vitest: 2 new tests, all green (+2 total → 180)
- [x] Backend IT: 2 new tests, all green (+2 total → 390)
- [x] Typecheck clean
- [x] Lint clean (0 warnings)
- [x] Full backend suite regression: no regressions
- [x] Tests named `*IntegrationTest.java` (not `*IT.java`) — naming convention respected
- [x] `@Autowired TestRestTemplate` on concrete class, not abstract base — correct
- [x] `W3CTraceContextPropagator` from `@opentelemetry/core` — confirmed export verified in pnpm store
- [x] Plan acceptance criteria met: backend IT confirms traceparent not rejected; frontend vitest confirms propagator injects traceparent correctly

---

## Commit SHA

Implementation: `783a6a9`

---

## Follow-ups

- OTel exporter connection errors during full suite run are expected noise (no OTLP collector in test env). Consider suppressing in `application-test.yaml` via `management.otlp.metrics.export.enabled=false` / similar — carry to M8 closure or hygiene pass.
- Real browser→server→audit trace correlation tested at Playwright E2E layer (task 8-22 Wave 7).

---

## Subagent token budget

`total_tokens`: ~28,000 (estimated)
