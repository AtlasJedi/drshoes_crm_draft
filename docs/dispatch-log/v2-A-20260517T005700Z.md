# v2-A — Kanban drag reuses StatusChangeTriggerDialog

**Date:** 2026-05-17 00:57 UTC
**Plan:** docs/superpowers/plans/2026-05-16-v2-six-fixes.md → Task v2-A
**Spec:** docs/superpowers/specs/2026-05-16-v2-six-fixes-design.md § Fix A
**Commit:** 823fd5a

## Files

- DELETED `apps/web/app/(admin)/admin/orders/_components/kanban/KanbanDragPopup.tsx`
- MODIFIED `apps/web/app/(admin)/admin/orders/_components/kanban/KanbanBoard.tsx` — drop popup mounting, ~60 LOC.
- MODIFIED `apps/web/app/(admin)/admin/orders/_components/kanban/KanbanBoardWrapper.tsx` — mount `StatusChangeTriggerDialog`, load locations, test-only drag handle.
- MODIFIED `apps/web/app/(admin)/admin/orders/_components/kanban/useKanbanDnd.ts` — extend `onConfirm` signature to `(sendTriggers, note?, location?)`; thread location-move via `addOrderNote`.
- MODIFIED `apps/web/app/(admin)/admin/orders/_components/kanban/__tests__/KanbanBoard.test.tsx` — drop the 4 popup-specific tests; structural tests preserved.
- MODIFIED `apps/web/app/(admin)/admin/orders/_components/kanban/__tests__/useKanbanDnd.test.tsx` — assertions updated for new `changeStatus` 5-arg signature; added test for note + location path.
- ADDED `apps/web/app/(admin)/admin/orders/_components/kanban/__tests__/KanbanBoardWrapper.test.tsx` — 2 RTL tests: dialog mounts on drag end, cancel reverts move.

## Test results

- vitest `app/(admin)/admin/orders/_components/kanban/` → **5 files / 23 tests / 23 pass**.
- vitest full suite → **97 files / 607 tests / 607 pass**.
- `pnpm typecheck` → 8 pre-existing errors (e2e/m11-fix-batch-sweep.spec.ts, lib/__tests__/locations.test.ts). None introduced by v2-A (verified by stashing changes and re-running typecheck).

## Decisions / deviations

- **Test handle naming:** `kanban-test-drag-{cardId}-{fromStatus}` — clicks drag-end to the first column with a different status. Simpler than encoding (cardId, fromStatus, toStatus) and matches the existing test fixture authored before reconciliation.
- **Location currentLocation = null:** `KanbanCardDto` does not carry `location` today. The dialog opens the picker with "— bez zmiany miejsca —" pre-selected. Future iteration can thread the current location into the card DTO without changing this commit.
- **Reconciled inline.** The Wave-1 v2-A Sonnet dispatch ran out of context before committing; left only `KanbanBoardWrapper.test.tsx` on disk. Source-side refactor was redone in the main session against the existing test contract (red → green).

## Subsequent commit

`docs/dispatch-log/v2-A-20260517T005700Z.md` + tasks.json flip → second commit (chore).
