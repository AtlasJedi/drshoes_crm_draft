# Dispatch Log — Task F4: MessageBubble layout + plain-text display sweep

**Task:** f4  
**UTC:** 2026-05-16T193241Z  
**Milestone:** m11  

## Files touched

| File | Change |
|------|--------|
| `apps/web/app/(admin)/admin/messages/_components/MessageBubble.tsx` | Added `whitespace-pre-wrap break-words` to bubble div; tightened wrapper max-width from `max-w-[78%]` to `max-w-[min(78%,640px)]` |
| `apps/web/app/(admin)/admin/messages/_components/__tests__/MessageBubble.test.tsx` | **Created** — 7 tests covering layout classes, body text, direction alignment, retry button visibility |

## No-change findings (Step 3)

- **RecentMessagesPanel.tsx** — renders `t.lastMessagePreview` (a string field from `MessageThreadDto`, server-side computed) as plain `{t.lastMessagePreview ?? ""}` inside a `<div>`. No `dangerouslySetInnerHTML`. No change needed.
- **OrderDrawerMessages.tsx** — renders `msg.body` as plain `{msg.body}` inside a `<p>` with `className="whitespace-pre-wrap"` (already present from a prior fix). No `dangerouslySetInnerHTML`. No change needed.

## Commands run

```
cd apps/web && pnpm test
```

## Test summary

- Before: 590 tests / 93 files — all green
- After:  597 tests / 94 files — all green
- Delta:  +7 tests (MessageBubble.test.tsx created)

## Decisions

- Used `container.querySelector` with class selector to assert `whitespace-pre-wrap break-words` on the inner bubble div — avoids RTL text-normalisation issues with `\n` in body text.
- `max-w-[min(78%,640px)]` CSS selector escaped as `.max-w-\[min\(78\%\,640px\)\]` in querySelector.
- `bodyHtml` field is absent from `MessageDto` interface (it belongs to the backend DTO only, not surfaced to frontend) — removed from test mock accordingly.

## Commit SHA

8a43b3b
