# Dispatch Log — Task 9-40

**Task:** `<Contact>` + `<Footer>` + compose `page.tsx`
**Milestone:** 9 — Design Parity
**UTC timestamp:** 2026-05-15T22:47Z
**Commit SHA:** 3c8a479

---

## Files created / modified

| File | Action | LOC |
|------|--------|-----|
| `apps/web/app/(public)/_components/Contact.tsx` | created | 86 |
| `apps/web/app/(public)/_components/Footer.tsx` | created | 23 |
| `apps/web/app/(public)/_components/__tests__/Contact.test.tsx` | created | 31 |
| `apps/web/app/(public)/_components/__tests__/Footer.test.tsx` | created | 22 |
| `apps/web/app/(public)/_components/__tests__/__snapshots__/Contact.test.tsx.snap` | created | — |
| `apps/web/app/(public)/_components/__tests__/__snapshots__/Footer.test.tsx.snap` | created | — |
| `apps/web/app/(public)/page.tsx` | rewritten | 21 |
| `apps/web/e2e/public-landing.spec.ts` | created | 43 |

## Test summary

- **Vitest (Contact + Footer):** 8/8 PASS
- **Full vitest suite:** 517/517 PASS (84 test files, 0 regressions)
- **Playwright:** 5 scenarios in `public-landing.spec.ts` — not run (requires running stack; excluded from CI per vitest config `e2e/**` exclusion rule)

## Decisions

1. **Contact bg-ink**: Task spec (9-40) explicitly states `bg-ink text-paper` for Contact section. The design reference (`landing.jsx` `Kontakt` function) uses `bg-paper-2`. Task spec takes precedence per Wave 7 cross-cutting note #2.
2. **Map iframe**: Uses generic `https://maps.google.com/maps?q=Wroclaw+Rynek&z=14&output=embed` per spec §7 deferral. Real coordinates left to owner.
3. **Contact LOC (86 vs 78 target)**: The plan's Step 2 reference code block was itself ~87 lines. The full sticker board (address + hours + phone + email + IG + stickers) with grid layout is semantically irreducible below 85 LOC. One import line consolidated (`Tape, Stamp, Sticker, I` from `@repo/ui`) to reduce from 87 to 86. Deviation flagged.
4. **Footer simplified**: Plan's footer is a two-span minimal bar per task spec, not the full 4-column design reference footer (which is out of M9 scope and would balloon well past 30 LOC).
5. **SklepTeaser pre-existing**: `SklepTeaser.tsx` already existed on disk (tasks.json showed 9-39 as pending but the file was present). Tests confirmed 8/8 pass. No changes needed.
6. **No public layout.tsx needed**: Root `layout.tsx` provides fonts + `<html lang="pl">`. Public landing composes server components directly — no additional layout wrapper required.

## Contact rows used (placeholder values)
- Address: ul. Włodkowica 14/2, 50-072 Wrocław · piętro 2 (from design reference, not task spec placeholder)
- Hours: Pn–Pt 11:00 — 19:00 / Sob 12:00 — 16:00 · Nd zamknięte
- Phone: +48 794 220 118 (from design reference)
- Email: siema@drshoes.pl (from design reference)
- IG: @dr_shoes · 38.4k followers
