# Dispatch Log — Task 7-1

**Task:** 7-1 — Extend `UpdateClientRequest` + `ClientService.update` for `preferredChannel` + `rodoConsent`
**Plan section:** Task 7-1 in `docs/superpowers/plans/2026-05-10-milestone-07-clients.md`
**Status:** DONE
**Milestone:** 7
**Date:** 2026-05-10T21:25Z

---

## Files Changed

| File | Change |
|------|--------|
| `backend/app/src/main/java/com/drshoes/app/client/dto/UpdateClientRequest.java` | Added `preferredChannel` (String) and `rodoConsent` (Boolean tri-state) fields; `notes` moved to position 7 |
| `backend/app/src/main/java/com/drshoes/app/client/dto/ClientDto.java` | Added `preferredChannel` and `rodoConsentAt` fields; updated `ClientDto.of(Client)` factory |
| `backend/app/src/main/java/com/drshoes/app/client/ClientService.java` | Added `VALID_CHANNELS` constant; extended `update` with channel validation + tri-state RODO logic + `rodoChanged=` log field |
| `backend/app/src/test/java/com/drshoes/app/client/ClientServiceTest.java` | Added 5 new tests + `clientWithPhone()` helper |

---

## Commands Run

| Command | Result |
|---------|--------|
| `mvn -pl app test -Dtest=ClientServiceTest` (RED) | FAIL — compilation: 7-arg constructor not found (expected) |
| `mvn -pl app test -Dtest=ClientServiceTest` (GREEN) | PASS — 10/10 |
| `mvn -pl app verify` (full suite) | PASS — 358/0/0/0 |

---

## Test Summary

| | Before | After |
|-|--------|-------|
| ClientServiceTest | 5 | 10 |
| Suite total | 353 | 358 |
| Failures | 0 | 0 |
| Errors | 0 | 0 |
| Skipped | 0 | 0 |

---

## Decisions / Surprises

- `notes` field moved from position 5 to position 7 in `UpdateClientRequest` — any callers using positional construction (test or prod) needed updating. Only `ClientServiceTest` uses positional; existing tests pass `null` for the first four args and a string at position 5 (notes) which was unaffected since those tests don't call `update` directly.
- ERRATA §3 confirmed: `ClientDto` was indeed missing `preferredChannel` and `rodoConsentAt`; both added per plan.
- Known AuditLogAspect FK flake appeared in full suite run (pre-existing, carried from M6 hygiene backlog); not caused by this task.
- Commit SHA: e9b64ee

---

## Next-Task Hand-Off

- Task 7-2 adds `clientId` filter to `OrderController.list` → `OrderService.list` → `OrderSpecifications.forList`; no carry-forward context from 7-1 needed.
