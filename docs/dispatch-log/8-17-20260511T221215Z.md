# Dispatch Log — Task 8-17

**Task:** 8-17 — `docs/MODULE_MAP.md` — feature-to-file lookup table
**Plan section:** `docs/superpowers/plans/2026-05-11-milestone-08-demo-ready.md` lines 3083–3144
**Review type:** Combined single-stage (pure docs)
**UTC timestamp:** 20260511T221215Z

---

## Files changed

- `docs/MODULE_MAP.md` — created; 88-line feature-to-file lookup table with 13 feature rows + OTel, Audit, Demo seed, Messaging dispatch hot-path sections + locked conventions/anti-patterns table

## Commands run

- `find backend/app/src/main/java/com/drshoes/app -type d` — scanned backend package structure ✅
- `find apps/web/app -type d` — scanned frontend app directories ✅
- `find apps/web/lib -type f` — scanned frontend lib files ✅
- `find backend/app/src/test/java -name "*IntegrationTest.java"` — verified test naming ✅
- `find apps/web/components -name "*.tsx"` — verified components directory ✅
- `wc -l docs/MODULE_MAP.md` — 88 lines ✅

## Test summary

No tests impacted (pure docs task).

- Backend before: 390/0/0/0
- Backend after: 390/0/0/0
- Frontend vitest before: 180/0/0/0
- Frontend vitest after: 180/0/0/0

## Decisions

1. **Path corrections from plan verbatim:** The plan table contained three path errors caught during filesystem verification:
   - `auth/AdminUserService.java` does NOT exist. Actual auth service is `auth/service/AuthService.java`. Corrected in MODULE_MAP.
   - `photo/PhotoService.java` should be `photo/service/PhotoService.java`. Corrected.
   - `photo/PhotoRepository.java` should be `photo/domain/PhotoRepository.java`. Corrected.
   - `messaging/TriggerEvaluator.java` does NOT exist as a standalone class; `TriggerEngine.java` is the evaluator. Used `messaging/service/TriggerEngine.java` instead.
   - `messaging/MessageGatewayDispatcher.java` path corrected to `messaging/service/MessageGatewayDispatcher.java`.

2. **ReportIssueButton.tsx not yet implemented:** Task 8-19 creates this file. The MODULE_MAP references it with `(task 8-19)` annotation so the table is forward-accurate and the row is still useful for navigation once implemented.

3. **DashboardService.java does not exist:** The plan table included `dashboard/DashboardService.java` but no such file exists. Dashboard logic lives directly in `DashboardController` + `DashboardChartsController`. Removed from MODULE_MAP.

4. **Added extra hot-path sections:** Beyond the plan's verbatim 13-row table, added four supplemental sections (OTel/Telemetry, Audit/AuditLog, Demo seed, Messaging dispatch) and the locked conventions/anti-patterns table. These are in-scope per the dispatch instructions: "Add an 'Anti-patterns / locked conventions' section."

5. **`client/domain/ClientRepository.java` not `client/ClientRepository.java`:** Corrected sub-package path.

6. **`order/domain/OrderRepository.java` not `order/OrderRepository.java`:** Corrected sub-package path.

## Review

**Verdict: APPROVED**

Checklist:
- [x] `docs/MODULE_MAP.md` exists
- [x] Contains 13 feature rows (≥ 11 per acceptance criterion)
- [x] Each row lists 2–5 concrete file paths (no bare globs except multi-file families)
- [x] Header explains purpose as cheap lookup index keyed by feature name/URL
- [x] Backend paths are relative to `backend/app/src/main/java/com/drshoes/app/`
- [x] Frontend paths are relative to `apps/web/`
- [x] Anti-patterns / locked conventions section included
- [x] `*IntegrationTest.java` naming rule called out
- [x] `proxy.ts` vs `middleware.ts` gotcha called out
- [x] Polish NBSP locale gotcha called out
- [x] Jackson Boolean defaulting gotcha called out
- [x] Path corrections vs plan noted in Decisions (not silently snuck in)
- [x] No code changes — pure docs

## Commit SHA

_To be filled after commit._

## Follow-ups

- `ReportIssueButton.tsx` + its test (task 8-19) will complete the Bug report button row — no action needed here.
- If a `DashboardService.java` is ever extracted, update the Dashboard row.

## Subagent token budget

Estimated ~15K tokens for this run (path scanning + plan read + doc write). Well under 40% cap.
