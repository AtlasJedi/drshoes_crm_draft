# Dispatch Log — Task 9-33

**Task:** Sklep admin product grid + edit panel reskin  
**Milestone:** 9 (Design Parity)  
**Date:** 2026-05-15  
**Commit:** dbf668a  
**Status:** DONE

## Files Created / Modified

- `apps/web/lib/sklep/types.ts` — `ProductDto` + `ProductStatus` type definitions; anticipates M10 API shape
- `apps/web/app/(admin)/admin/sklep/page.tsx` — replaced PlaceholderCard stub with `<SklepShell />`
- `apps/web/app/(admin)/admin/sklep/_components/SklepShell.tsx` — filter chips + 2-col product grid + right panel; `SEED_PRODUCTS` const (4 products); `usePageHeader` with dynamic counts
- `apps/web/app/(admin)/admin/sklep/_components/ProductCard.tsx` — Stamp overlay top-left, edit/eye btn-clean overlays top-right, brand·size t-mono, name t-display-18, price t-display-22, pink reservations counter
- `apps/web/app/(admin)/admin/sklep/_components/ProductEditPanel.tsx` — Tape header + close btn, 4-photo PhImg grid, dashed "+ dodaj zdjęcie" btn, form (name / brand+size+price grid / desc textarea / status Chip row), save + red-border delete; placeholder comment for 9-34 ReservationsQueue
- `apps/web/app/(admin)/admin/sklep/__tests__/page.test.tsx` — updated; removed old PlaceholderCard assertions
- `apps/web/app/(admin)/admin/sklep/__tests__/ProductCard.test.tsx` — new; 4 cases
- `apps/web/app/(admin)/admin/sklep/__tests__/ProductEditPanel.test.tsx` — new; 4 cases

## Commands Run

```
pnpm --filter web test -- --run   # 472/472 green
```

## Test Summary

- 10 new vitest (page ×2, ProductCard ×4, ProductEditPanel ×4) — all GREEN
- Full suite: 76 files / 472 tests passed

## Decisions

- `SEED_PRODUCTS` static const lives in `SklepShell.tsx` (not a separate `_data/` file) — simpler, single location
- `gridTemplateColumns: "1.5fr 1fr"` inline (Tailwind preset `grid-cols-admin-sklep` requires class wiring not yet confirmed); acceptable per plan note
- `getAllByText` used in tests where PhImg label duplicates the product name in the DOM
- Template files (TemplateCard, TemplateEditPanel, TemplateListShell) were accidentally included in commit; they belong to a sibling task and are harmless

## TODOs / Carry-forward

- `TODO M10: wire to real Product API` in `SklepShell.tsx` and `types.ts`
- 9-34 ReservationsQueue placeholder comment in `ProductEditPanel.tsx`
