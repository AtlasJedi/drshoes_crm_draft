# Dispatch log: fix-A — NewOrderForm: derive Wycena from items + default 1 row

**Task:** fix-A  
**Milestone:** fixes  
**UTC stamp:** 2026-05-16T13-50-00Z  
**Author:** Claude (inline, no subagent — 2-file change)

## Files

- `apps/web/app/(admin)/admin/orders/new/_components/NewOrderForm.tsx` — removed `quotedPricePln` state; added `useMemo` `wycenaCents` (sum of item `plnToCents(pricePln)`); initialized `items` with `[makeFreshItem()]`; replaced Wycena `<input>` with read-only `<div aria-label="Wycena">` styled `bg-admin-line/30 text-admin-mute`; helper text "Suma z pozycji zlecenia (poniżej)"; moved "Do zapłaty" inline under Zaliczka; `handleSubmit` uses `wycenaCents` directly.
- `apps/web/app/(admin)/admin/orders/new/_components/__tests__/NewOrderForm.test.tsx` — rewrote integration section; preserved all money-util unit tests; replaced old "Wycena input" tests with: default-1-row, Wycena=0 with empty price, Wycena reflects single item at 200, two items 100+250=350, submit sends correct `quotedPriceCents`, Wycena is not an editable input, helper text present.

## Commands run

```
cd apps/web && npx vitest run
```

## Test summary

- **89 test files, 554 tests — all passed, 0 failures**
- New tests added: 7 integration tests in `NewOrderForm — derived Wycena from items` describe block
- Old integration tests (3 that depended on editable Wycena input) replaced

## Decisions

- Used `aria-label="Wycena"` on the `<div>` so tests can target it with `getByRole("generic", { name: /wycena/i })` without using brittle text selectors.
- "Do zapłaty" moved from a standalone block below the grid to inline under the Zaliczka input — shows only when `wycenaCents > 0` (avoids showing "0,00 zł due" with empty form).
- `NewOrderItemRow` mock in tests now renders a controlled `<input aria-label="item-price-{index}">` so price changes flow through the real `useMemo` sum rather than returning a stub.
- No new dependencies added. LOC: 178 (well under 250 limit).

## Commit SHA

(filled after commit)
