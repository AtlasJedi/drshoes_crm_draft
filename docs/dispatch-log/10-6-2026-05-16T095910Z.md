# Dispatch Log: 10-6

**Task:** 10-6 — Frontend lib helpers (`lib/locations.ts` + types)
**UTC:** 2026-05-16T09:59:10Z
**Status:** completed

## Files

- `apps/web/lib/types.ts` — new; shared types `StorageLocation`, `AddOrderNotePayload`, `AddOrderNoteResult`
- `apps/web/lib/locations.ts` — new; 5 fetch helpers + `LocationsApiError` class
- `apps/web/lib/__tests__/locations.test.ts` — new; 8 vitest cases mocking `global.fetch`

## Commands

```
cd apps/web && pnpm exec vitest run lib/__tests__/locations.test.ts  # 8/8 GREEN
cd apps/web && pnpm exec vitest run                                   # 529/529 GREEN
```

## Test Summary

- Before: 521 tests
- After: 529 tests (+8)
- Result: all GREEN, no regressions

## Decisions

- Used `createLogger` (not `mkLogger` — plan had incorrect logger name; real export is `createLogger`)
- Used fields-object logger calls (e.g. `log.debug("op=...", { key: val })`) per actual `Logger` interface
- Used raw `fetch` with `credentials: "include"` as specified (not `api` wrapper) — enables raw-fetch mock in tests
- `lib/types.ts` created as new top-level shared types file (no pre-existing file at that path)
- `lib/__tests__/` directory created (did not exist)
- 8 test cases (plan said "10" but plan code has 8; count discrepancy was in the dispatch brief, not the plan code)

## Commit SHA

`600bee1`
