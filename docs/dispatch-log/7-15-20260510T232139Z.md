---
task_id: 7-15
plan: docs/superpowers/plans/2026-05-10-milestone-07-clients.md
milestone: 7
agent: sonnet (dispatched) + main (commit + verify)
start_utc: 2026-05-10T23:21:39Z
end_utc: 2026-05-10T19:46:00Z
status: DONE
commit_sha: a2d2ce1
---

# Task 7-15 — EditClientModal + useEditClientModalForm + RTL tests

## Files changed
- `apps/web/app/(admin)/admin/clients/_components/EditClientModal.tsx` (new, 128 LOC)
- `apps/web/app/(admin)/admin/clients/_components/useEditClientModalForm.ts` (new, 109 LOC)
- `apps/web/app/(admin)/admin/clients/_components/__tests__/EditClientModal.test.tsx` (new, 150 LOC, 7 tests)
- `apps/web/app/(admin)/admin/clients/[id]/_components/EditClientIsland.tsx` (modified — wire real modal, removed stub div)
- `apps/web/package.json` (add `@radix-ui/react-switch ^1.2.6`)
- `apps/web/vitest.setup.ts` (ResizeObserver polyfill for Radix Switch in JSDOM)
- `pnpm-lock.yaml` (lockfile)

## Commands run
```
cd apps/web
pnpm vitest run app/(admin)/admin/clients/_components/__tests__/EditClientModal.test.tsx
  # 7 passed
pnpm vitest run
  # 168 passed (32 files) — was 161 → +7
pnpm lint
  # eslint . --max-warnings=0 — exit 0
pnpm tsc --noEmit
  # 6 pre-existing errors in 7-6/7-7/7-8 files (Next typed-routes); zero new errors from 7-15
```

## Test summary
- Tests run: 7 new (full suite 168/0/0/0)
- Coverage: not measured

## Decisions / deviations from plan
- Plan marked TWO-STAGE; owner directed combined single-stage to reduce ceremony bloat. Stage 2 review folded into inline main-session diligence.
- Combined the form-state hook + initFormState helper into one file (`useEditClientModalForm.ts`) per plan; both under the LOC granularity rule.
- Removed the original 7-10 stub div + TODO comment in `EditClientIsland.tsx`; wired `<EditClientModal>` directly.
- RODO tri-state mapping: only emit `true`/`false` if the switch state diverges from `client.rodoConsentAt`-derived ground truth; otherwise emit `null` (no change). Plan covered this implicitly via the "RODO toggle on (was null)" test.

## Logging / metadata wired
- `EditClientModal` logs at INFO: `op=modal open={} clientId={}` on open/close transitions.
- `useEditClientModalForm` logs at INFO: `op=editClient attempt clientId={}` then `outcome=ok` / `outcome=error status={}` per HttpError.

## Concerns
- **6 pre-existing tsc errors** in `ClientListSearchBox.tsx`, `ClientListTable.tsx`, `clients/page.tsx` from tasks 7-6/7-7/7-8 — Next 16 typed-routes `RouteImpl<...>` not satisfied by raw string templates. Hygiene carry-forward; not introduced by this task. (`git stash && pnpm tsc --noEmit` at HEAD `7f9276c` reproduces same 6.)
- ESLint warning at session start about `vi-tsconfig-paths` plugin redundancy — separate harness concern.

## Next action
- Move to task 7-16 (PlaceholderCard + sklep/aktualnosci stubs) — handled inline by main session per close-out plan.
