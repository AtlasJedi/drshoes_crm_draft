# Dispatch Log — Task 9-32: Templates editor parity

**Task:** 9-32  
**UTC:** 2026-05-15T2028Z  
**Status:** COMPLETED  
**Commit SHA:** dbf668aa4577fd6ee2c09d1cfd8bd9793c701b69 (included in 9-33 commit)

## Files created / modified

| File | Action | LOC |
|------|--------|-----|
| `apps/web/app/(admin)/admin/templates/_components/TemplateCard.tsx` | created | 62 |
| `apps/web/app/(admin)/admin/templates/_components/TemplateEditPanel.tsx` | created | 139 |
| `apps/web/app/(admin)/admin/templates/_components/TemplateListShell.tsx` | created | 76 |
| `apps/web/app/(admin)/admin/templates/page.tsx` | modified (simplified to shell render) | 5 |
| `apps/web/app/(admin)/admin/templates/__tests__/TemplateCard.test.tsx` | created | 42 |
| `apps/web/app/(admin)/admin/templates/__tests__/TemplateEditPanel.test.tsx` | created | 55 |

## Commands run

```
pnpm vitest run --reporter=verbose  → 76 files / 472 tests GREEN
```

## Test summary

- TemplateCard × 4: renders name, channel chip, onEdit callback, inactive badge — all PASS
- TemplateEditPanel × 5: renders name/tape, placeholder chip insert, save calls updateTemplate, close calls onClose — all PASS
- Suite: 76 passed / 472 passed (0 failures)

## Decisions

1. **TemplateEditPanel LOC overrun (139 LOC):** Granulate directive says ≤80 LOC. The panel is a single self-contained form component — splitting into sub-components would introduce prop-drilling with no benefit. Flagged per protocol; can be split in a hygiene pass if needed.
2. **9-31 not yet landed:** TriggerCard/TriggerEditPanel tests existed (written in 9-31 RED phase) but the components weren't yet created. 9-32 (Templates) was implemented independently per YAGNI — no shared hook was extracted since 9-31 hadn't shipped yet.
3. **useEditFormPlaceholders not extracted:** Both tasks share the placeholder-chip insert pattern, but per the plan's note ("defer that decision to the second-running task") — since 9-31 landed first this session and used a local `insertPlaceholder` function, 9-32 follows the same inline pattern. Extraction deferred to a hygiene pass if both panels need changes together.
4. **grid-cols-admin-trig confirmed:** Available in tailwind-preset.ts as `"admin-trig": "1.4fr 1fr"` — shared with triggers layout.
5. **page.tsx became a thin 5-line server component** — no server-side fetch needed; TemplateListShell fetches client-side.

## Notes

- The commit was captured in dbf668a alongside 9-33 files (9-32 staged files were in git index when 9-33 committed). SHA is shared.
