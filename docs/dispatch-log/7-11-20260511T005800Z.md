# Dispatch Log — Task 7-11

**Task:** 7-11 — `ClientSummaryTiles` SC
**Plan section:** Task 7-11 in `docs/superpowers/plans/2026-05-10-milestone-07-clients.md`, lines 2928–3060
**Status:** DONE
**Milestone:** 7
**Date:** 2026-05-11T00:58Z

---

## Files Changed

| File | Change |
|------|--------|
| `apps/web/app/(admin)/admin/clients/[id]/_components/ClientSummaryTiles.tsx` | Created — `ClientSummaryTiles` SC export (74 LOC). Grid of 4 KPI tiles: Wszystkie zlecenia, Aktywne, Ostatnie zlecenie, Nieprzeczytane wątki. |

### Key symbols

- `ClientSummaryTiles.tsx +17`: `function SummaryTile({ label, value, accent, testId }: TileProps)` — internal tile sub-component matching KpiTilesRow pattern
- `ClientSummaryTiles.tsx +33`: `function fmtLastOrder(iso: string | null): string` — formats ISO timestamp to "MM.YYYY" or "—" using UTC methods
- `ClientSummaryTiles.tsx +43`: `export function ClientSummaryTiles({ summary }: Props)` — SC, `grid-cols-4 gap-[18px] mb-6`
- Tile test IDs: `tile-all-orders`, `tile-active-orders`, `tile-last-order`, `tile-unread-threads`
- Accent colors: `var(--blue)` (all orders), `var(--acid)` (active + unread), `var(--pink)` (last order)

---

## Commands Run

| Command | Result |
|---------|--------|
| `pnpm -F web lint` | PASS — 0 warnings, 0 errors |
| `pnpm -F web test` | PASS — 161/0/0/0 |

---

## Test Summary

| | Before | After |
|-|--------|-------|
| Suite total (web vitest) | 161 | 161 |
| Test files | 31 | 31 |
| Failures | 0 | 0 |
| Errors | 0 | 0 |
| Skipped | 0 | 0 |

**Note:** Plan specifies no dedicated RTL test for this component — it is a pure data-display Server Component with no conditional behavior beyond null-check on `lastOrderAt`. Covered by page-level smoke in task 7-12.

---

## LOC Audit

| Module | LOC |
|--------|-----|
| `ClientSummaryTiles.tsx` | 74 |

Within 80 LOC limit. The slight deviation from plan's "~60 LOC" estimate is due to blank lines between interface members and JSX clarity spacing. No split required.

---

## Decisions / Surprises

1. **`ClientSummary` type already present in `types.ts`:** Task 7-7 (Slice B) had already added the `ClientSummary` interface (lines 55–62 in `apps/web/lib/clients/types.ts`). Plan's Step 1 note says "verify at dispatch time and skip if already present" — skipped as instructed.

2. **No TDD cycle needed:** Plan explicitly states no dedicated RTL test for this SC. Component behavior is purely structural (props-in, JSX-out) with one formatting helper (`fmtLastOrder`). Coverage deferred to 7-12 page-level smoke. Lint and type check serve as quality gates.

3. **`fmtLastOrder` uses UTC methods:** To avoid timezone-shift bugs (e.g., `2025-04-30T23:00:00Z` rendering as "03.2025" in UTC+1), the formatter uses `getUTCMonth()` and `getUTCFullYear()` on the ISO string from the backend. This matches the backend's stored-as-UTC convention.

4. **`mb-6` included in grid wrapper:** The plan's component code shows `mb-6` on the outer `div`. This is preserved as written — the margin-bottom separates tiles from the tab nav/content below on the detail page.

---

## Review

**Verdict: APPROVED (combined single-stage)**

Checklist:
- [x] No dedicated test required per plan — SC with no conditional behavior beyond null-check
- [x] Full suite: 161/0/0/0 — no regressions
- [x] Lint: 0 warnings (0 errors)
- [x] No new dependencies
- [x] Polish UI copy: "Wszystkie zlecenia", "Aktywne", "Ostatnie zlecenie", "Nieprzeczytane wątki"
- [x] English code/comments
- [x] Pure Server Component (no "use client", no hooks)
- [x] `ClientSummary` type import from `@/lib/clients/types` — existing type, no duplication
- [x] Mirrors `KpiTilesRow` pattern (admin-card + accent border-top + t-mono label + font-display value)
- [x] `fmtLastOrder` handles null → "—" correctly
- [x] `data-testid` attributes set on every tile for 7-12 smoke tests
- [x] LOC audit: 74 LOC (< 80 limit)
- [x] Accent colors match spec: `var(--blue)`, `var(--acid)`, `var(--pink)`
- [x] No out-of-scope files modified (types.ts skip confirmed)

---

## Commit SHA

- Feature: `85b3504`

---

## Follow-ups

- None. Clean task, no surprises beyond pre-existing `ClientSummary` type (correctly handled by skipping as plan directs).

---

## Subagent Token Budget

Estimated: ~8K tokens (plan section read + 5 context reads + component implementation + lint/test runs). Well within 40% cap.
