# Dispatch Log — Task 2-18: MessageComposerModal

**UTC:** 20260508T212006Z
**Milestone:** 2 (Messaging)
**Task:** 2-18
**Plan:** docs/superpowers/plans/2026-05-08-milestone-02-messaging.md (lines 3254–3374)

## Files Touched

| File | Action |
|------|--------|
| `apps/web/app/(admin)/admin/orders/_components/MessageComposerModal.tsx` | Created |
| `apps/web/app/(admin)/admin/orders/_components/OrderDrawer.tsx` | Modified — import + render `<MessageComposerModal>` |

## Commands Run

```bash
pnpm typecheck   # pass (after fixing noUncheckedIndexedAccess on active[0])
pnpm build       # pass — 10 routes, all ƒ dynamic as expected
```

## Decisions

- **noUncheckedIndexedAccess fix:** Plan body used `active[0].id` directly inside `if (active.length > 0)` — TypeScript's strict mode requires assigning to a local `const first = active[0]` and narrowing via `if (first)`. Applied.
- **Log signature:** Plan snippet used `log.info({ op: ... })` (object-first) which is wrong for the project's string-first `LogFn`. Corrected to `log.info("op=send outcome=success", { ... })`.
- **Subject field:** Only shown when `channel === "EMAIL"` per spec.
- **Channel auto-fill:** Template selection drives channel; user can override via the select.
- **Composer placed outside drawer scroll container:** `<MessageComposerModal>` rendered as a sibling to `<Dialog.Content>` (still inside `<Dialog.Root>`) so its own `Dialog.Portal` teleports to `<body>` above the drawer's z-index.
- **onSent callback:** Calls `setRefreshKey(k => k + 1)` in OrderDrawer, which triggers OrderDrawerMessages to re-fetch the thread. `onOpenChange(false)` closes the modal on success.
- **Combined spec+quality review:** Pure client component, no backend changes. Single-stage review applied.

## Test Suite

Suite unchanged: **149/0/0/0** (backend tests — no new backend code shipped in this task).

## Commit SHA

`9be76ed` — `feat(web): MessageComposerModal + drawer wiring [milestone:2][task:2-18]`
