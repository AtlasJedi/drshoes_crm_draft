---
task_id: m10-fb-2
plan: inline (no plan file — small UI fix)
milestone: 10
agent: sonnet
start_utc: 2026-05-16T15:00:00Z
end_utc: 2026-05-16T15:33:00Z
status: DONE
commit_sha: a3be035
---

# Task m10-fb-2 — Render M10 LocationMoveChip in drawer history + header

## Files changed

- `apps/web/app/(admin)/admin/orders/_components/OrderDrawerTimeline.tsx` (modified, ~140 LOC)
  — Added `import { LocationMoveChip }` from `./_LocationMoveChip`. Renders `<LocationMoveChip>` under
  the actor line (above note blockquote) on every event row where `locationFrom != null || locationTo != null`.
- `apps/web/app/(admin)/admin/orders/_components/OrderDrawerHeader.tsx` (modified, ~80 LOC)
  — Added `location?: string | null` prop. Renders an acid-yellow `📍 location` pill with
  `aria-label="Aktualne miejsce"` beside the status `<Pill>` when `location` is truthy.
- `apps/web/app/(admin)/admin/orders/_components/OrderDrawer.tsx` (modified)
  — Passed `location={order.location}` to `<OrderDrawerHeader>`. `order.location` was already
  present on `OrderDto` (denormalized from `storage_locations` table, added in M10).
- `apps/web/app/(admin)/admin/orders/_components/__tests__/OrderDrawerTimeline.test.tsx` (modified)
  — Added `locationFrom: null, locationTo: null` to `baseEvent` fixture; added new describe block
  `OrderDrawerTimeline — LocationMoveChip rendering` with 2 new tests:
    1. chip renders for event with `locationFrom="półka 1"` + `locationTo="suszarka"`
    2. chip does not render when both null.
- `apps/web/app/(admin)/admin/orders/_components/__tests__/OrderDrawerHeader.test.tsx` (modified)
  — Added 2 new tests: acid pill renders when `location` prop present; does not render when absent.

## Commands run

```
pnpm --filter @drshoes/web test   # pre-stash: 565 pass / 2 fail (pre-existing useKanbanDnd)
git stash                         # verified 2 failures are pre-existing (561 pass / 2 fail on HEAD~)
git stash pop                     # restored changes
docker compose build web          # rebuilt bundle
docker compose up -d web          # restarted container
curl localhost:3000/admin/orders  # 307 (login redirect) — healthy
```

## Test summary

- Before: 563 tests (561 pass / 2 fail pre-existing in useKanbanDnd)
- After:  567 tests (565 pass / 2 fail same pre-existing)
- Net: +4 new tests, all GREEN
- Pre-existing failures: `useKanbanDnd.test.tsx` — 2 tests expect `changeStatus(id, status, version)` 3-arg call but impl now passes `sendTriggers` as 4th arg. This is a carry-forward from a previous milestone, not introduced here.

## Decisions / deviations

- **`order.location` already on DTO**: `OrderDto.location` (type `string | null`, line 61 of `lib/orders/types.ts`)
  was added in M10. No new API call or DTO field threading required.
- **`locationFrom`/`locationTo` already on `TimelineEvent` type**: Fields existed in `lib/timeline/types.ts`
  since M10 spec was written (marked M10/V020). No type changes needed.
- **Chip placement**: Placed chip in a `<div className="mt-1">` wrapper between the timestamp line and the
  note blockquote, matching the design archive showcase.jsx description (chip below actor, above note).
- **LOC check**: `OrderDrawerTimeline.tsx` is now ~140 LOC (was 133). Within the "flag at ~150" threshold
  from the brief. No split needed.

## Carry-forward hygiene

- `useKanbanDnd.test.tsx` 2 pre-existing failures — test stubs expect 3-arg `changeStatus` call but live
  hook passes 4 args (including `sendTriggers`). Flagged for a future cleanup task (unrelated to this fix).
- `OrderDrawerTimeline.tsx` at ~140 LOC — approaching the 80-LOC TS guideline. Consider splitting the
  `KIND_ICONS` + `KIND_LABELS_PL` maps into a `_timelineKinds.ts` constant file in a future polish pass.
