# Dispatch Log: 2-16 — /admin/triggers list + detail + toggle

**UTC:** 2026-05-08T211518Z
**Task:** 2-16
**Plan:** docs/superpowers/plans/2026-05-08-milestone-02-messaging.md (lines 3014–3136)
**Review mode:** combined spec+quality (mechanical Next.js scaffolding)

## Files Created

- `apps/web/app/(admin)/admin/triggers/_components/TriggerToggle.tsx` — client component; PATCH /admin/triggers/:id/enabled; useTransition + router.refresh()
- `apps/web/app/(admin)/admin/triggers/page.tsx` — server component; list all triggers; Polish event-label lookup; fetchError + empty-state guards
- `apps/web/app/(admin)/admin/triggers/[id]/page.tsx` — server component; trigger detail read-only view; TriggerToggle embedded in detail row

## Commands Run

```
pnpm typecheck   → 0 errors
pnpm build       → green; /admin/triggers + /admin/triggers/[id] both ƒ (Dynamic)
```

## Decisions

- Plan body passed object literals as first arg to `log.info/error` — incompatible with actual `createLogger` LogFn signature `(message: string, fields?: Fields)`. Fixed to `log.info("op=toggle outcome=success", { id, enabled })` pattern matching all other M2 modules.
- TriggerToggle button uses Tailwind classes (green-100/green-700 for enabled, admin-surface/admin-mute for disabled) instead of plan's bare `btn-on/btn-off` class names — those tokens don't exist in the design system.
- Detail page includes TriggerToggle (toggle-in-detail is natural UX); plan body showed toggle only on list but didn't explicitly exclude it from detail.
- Create/edit form is M3 debt per plan lock — no form scaffolded.

## Test Summary

suite=149/0/0/0 (unchanged — no backend changes in this task)

## Commit SHA

e14013d

## Refs

Milestone 2 plan: docs/superpowers/plans/2026-05-08-milestone-02-messaging.md
