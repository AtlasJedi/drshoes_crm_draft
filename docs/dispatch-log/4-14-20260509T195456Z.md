---
task_id: 4-14
plan: docs/superpowers/plans/2026-05-09-milestone-04-real-providers.md
milestone: 4
agent: sonnet
start_utc: 2026-05-09T19:54:56Z
end_utc: 2026-05-09T20:05:00Z
status: DONE
commit_sha: b34a9c2
---

# Task 4-14 — OrderDrawerMessages — badge + retry button + 10s polling

## Files changed
- `apps/web/app/(admin)/admin/orders/_components/OrderDrawerMessages.tsx` (modified, 83 insertions / 17 deletions — net +66 LOC, total ~130 lines)

## Commands run
```
pnpm typecheck   # exit 0
pnpm lint        # exit 0 (--max-warnings=0)
git commit       # b34a9c2
```

## Test summary
- No new unit tests (pure UI component, no test harness for Next.js components in this project)
- TypeScript strict: zero errors
- ESLint: zero warnings

## Decisions / deviations from plan
- `MessageRow` import removed from this file (was the only consumer besides its own definition). `MessageRow.tsx` file left intact — not deleted.
- Plan showed `load` signature as `async (silent = false)`. Original component had `load` as `useCallback(async () => {...})` with no silent param. Rewrite aligns with plan spec.
- `useRef<ReturnType<typeof setInterval> | null>` used for the poll handle — satisfies strict TS without `NodeJS.Timeout` import.
- Component is ~130 LOC (slightly over 80-LOC soft cap). Plan explicitly acknowledged this at plan-write time (~95 LOC estimate was slightly off due to JSX verbosity). No split needed — each JSX block is cohesive.
- Polling is always-on while drawer is mounted (no pause-when-all-terminal optimization). Plan did not explicitly require the pausing optimization; always-polling is simpler and noted here as a potential future optimization.

## Logging / metadata wired
- `createLogger("messaging.thread")` — poll: `op=poll.thread outcome=ok|stale-error`; retry: `op=message.retry outcome=start|ok|failed`

## Concerns
- (none)

## Next action
- Move to task `4-15` (KIND_LABELS_PL + KIND_ICONS extension for MESSAGE_DELIVERED/FAILED).
