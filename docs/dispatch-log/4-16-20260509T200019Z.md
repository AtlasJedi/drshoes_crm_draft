---
task_id: 4-16
plan: docs/superpowers/plans/2026-05-09-milestone-04-real-providers.md
milestone: 4
agent: general-purpose Sonnet
start_utc: 2026-05-09T20:00:19Z
end_utc: 2026-05-09T20:10:00Z
status: DONE
commit_sha: 6b7ffd1
---

# Task 4-16 — Smoke + milestone-4 tag + CLAUDE.md flip

## Files changed
- `backend/app/src/test/java/com/drshoes/app/photo/api/PhotoControllerIT.java` → renamed to `PhotoControllerIntegrationTest.java` (class declaration updated to match). M3 hygiene debt closed: 7 previously-silent IT cases now confirmed green under Surefire.
- `CLAUDE.md` — status row added: `[x] Milestone 4: Real providers + webhooks + delivery reconciliation + retry`
- `docs/dispatch-log/4-16-20260509T200019Z.md` — this file
- `docs/dispatch-log/tasks.json` — task 4-16 marked completed

## Commands run
```
# Step A: M3 hygiene fix
git mv backend/app/src/test/java/com/drshoes/app/photo/api/PhotoControllerIT.java \
       backend/app/src/test/java/com/drshoes/app/photo/api/PhotoControllerIntegrationTest.java
# edit class declaration inside
mvn -B -pl app -am test   # exit 0 — 201/0/0/0 (was 194)

# rename commit SHA: 536d9f0

# Step B: full closure gates
cd backend && mvn -B verify   # exit 0 — BUILD SUCCESS 201/0/0/0
cd apps/web && pnpm typecheck # exit 0 — 0 errors
cd apps/web && pnpm lint      # exit 0 — 0 errors, 0 warnings
cd apps/web && pnpm build     # exit 0 — compiled successfully, all 10 routes rendered

# Step G: tag
git tag -a milestone-4 -m "..."
```

## Test summary
Suite: 201/0/0/0 (M4 close). M3 hygiene delta: +7 (PhotoControllerIntegrationTest, previously named *IT.java and silently skipped by Surefire). All green.

## Decisions / deviations from plan
- Manual UI smoke skipped per M2/M3 owner-locked precedent. Provider integration covered by WireMock ITs in tasks 4-4, 4-6, 4-7, 4-8, 4-11.
- `Co-Authored-By:` lines omitted from all commits — project dispatch protocol hard rule; plan example was indicative only.
- The build log contains structured-logger ERROR/WARN lines during Next.js static generation (dynamic `cookies()` usage in server components — expected behavior, not build failures). Build completed with `✓ Compiled successfully`.

## Logging / metadata wired
- (none) — closure/housekeeping task only.

## Concerns / carry-forwards to M5
- `*IT.java` naming convention must not be used for new integration tests going forward. Convention is `*IntegrationTest.java`. This has been a known project hygiene item since M3; now resolved.
- No LOC-cap debt introduced by this task.

## Next action
- M5 planning: inbound parsing (email reply / SMS inbound), reply UI, unread count, WhatsApp stub, DB outbox auto-retry worker.
