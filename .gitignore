node_modules
.next
.vercel
.env
.env.local
.env.*.local
.DS_Store
*.log
dist
build
*.tsbuildinfo
.turbo

# Java / Maven
target/
*.class
.mvn/wrapper/maven-wrapper.jar
hs_err_pid*.log

# IDE
.idea/
*.iml
*.iws
.vscode/
*.swp

# External tooling clone (per CLAUDE.md: cd .superpowers && git pull)
.superpowers/

# Playwright E2E artifacts (binaries live in ~/.cache/ms-playwright, not here)
apps/web/test-results/
apps/web/playwright-report/

# M9 parity audit screenshots (generated at runtime by m9-parity-audit.spec.ts)
screenshots/m9-parity/
.playwright-mcp/

# DB backups (created by `make backup` / `make update`)
backups/
